---
tags: [aprendizados]
status: atual
---
# APRENDIZADOS.md — lições para os próximos agentes (arquivo vivo)

> Alimentado ao fechar milestones ([[b_process/skills/retrospective/SKILL|retrospectiva]]). 1 linha por lição, generalizável, honesta — inclua os SEUS erros. Lição repetida em 2+ projetos → promover a regra do kit ([[README]]).

## Herdadas (não repague estas)
- **Observe antes de construir:** nunca escreva parser/integração sem uma amostra REAL do payload/estrutura. Chutar a estrutura de uma fonte custou 6 ciclos de QA num projeto.
- **"Está quebrado" vs "falta dado":** cheque o estado do dado antes de caçar bug no código.
- **Declare a restrição da stack ANTES de modelar:** descobrir tarde o que o banco/framework não suporta custou 6 versões de schema em 7 dias. O perfil da stack entra no CONTEXT no dia 1.
- **Dinheiro é inteiro (centavos) e taxa é basis point desde a primeira migration:** trocar depois reescreve toda a camada de cálculo. `0.1 + 0.2 ≠ 0.3`.
- **Segredo é gerado por instalação, nunca versionado:** segredo de sessão fixo e boot com placeholder foram os dois achados mais graves de um projeto real. O boot deve recusar iniciar sem segredo próprio.
- **Amostra de segredo em documentação se inutiliza, não se isenta:** trocar o corpo do token por `XXXX` preserva o formato e mata o valor. Isentar a linha (`checar:ignore`, `# nosec`, `# gitleaks:allow`) cala só o *seu* scanner — o do GitHub barrou o push mesmo assim, e estava certo. Isenção serve para o que é comprovadamente inerte, não para o que só *você* sabe que é falso.
- **Fricção de segurança tem de casar com o uso real:** proteção que atrapalha o fluxo principal é removida pelo dono depois — proteja só o que ele quer proteger, e pergunte antes.
- **Máquina de usuário final consome artefato pronto, não compila:** build na máquina do cliente é lento e frágil; publique imagem e faça pull pinado com rollback.
- **Migration em produção é expand/contract:** aditiva primeiro, remoção só uma release depois — mais ainda se o deploy for automático.
- **Automação que roda sozinha precisa de RUNBOOK e rollback testado na máquina real** antes de ser ligada; senão é risco, não conveniência.
- **Doc que descreve comportamento tem de bater com o código:** doc desalinhada do comportamento é bug de doc, e conta como achado de QA.
- **Barato ≠ valioso:** feature fácil que não muda decisão nem número é cruft — pergunte "isso muda algo?" antes de construir.
- **Processo vivo tem cache:** servidor/build antigo mascara sua mudança — reinicie antes de julgar (e avise o dono).
- **Sandbox ≠ máquina real:** o portão final roda na máquina do dono; termine dizendo o que ele precisa rodar.
- **Rejeitar é o portão funcionando:** registre a rejeição com o número/motivo que matou.
- **Honestidade compõe:** reportar fraqueza gera mais confiança do que esconder — e não precisa ser desfeito depois.

## Deste projeto
- 2026-08-06 — **Resposta do dono pode ser internamente contraditória:** ele pediu "modo online" e "zero servidor" na mesma rodada de perguntas. Cheque coerência ENTRE as respostas antes de escrever o CONTEXT; escrever primeiro e descobrir depois teria congelado uma arquitetura impossível em D-NN.
- 2026-08-06 — **"É possível?" é pergunta factual, não decisão do dono:** diante da contradição, a saída não era escolher por ele nem devolver a escolha — era medir. O online a custo zero existe (P2P + sinalização pública), com falha de 15-30% sob CGNAT declarada como lacuna e virada em portão.
- 2026-08-06 — **Registrar QA-NN é obrigação, não pergunta:** perguntei "quer que eu registre o defeito do kit?" quando a regra 4 manda registrar. O que se pergunta ao dono é rumo e regra de negócio; achado se registra e se avisa. Parar antes de escrever em OUTRO repositório continua certo (regra 2) — o erro foi a frase, não o freio.
- 2026-08-06 — **Gatilho de revisão precisa de um número que alguém possa LER num relatório:** "se o bundle crescer" não fecha discussão nenhuma; ">= 8 MB na saída do build" fecha. Ao congelar D-NN, escreva o gatilho como uma medição que já existe no critério de aceite — assim o portão que reabre a decisão é o mesmo que já roda.
- 2026-08-06 — **Tabela nova em `c_decisions.md` que começa a linha com `| D-01 |` reprova o `check.py`:** a regra 11 lê a coluna 1 de QUALQUER tabela como definição de ID e acusa duplicata. Referência cruzada vai em crase (`` `D-01` ``); coluna 1 sem crase é só para a linha que CRIA o ID.
- 2026-08-07 — **O histórico é a prova do placar, e reconferi-lo é a constraint que um jogo sem banco pode ter:** `goals`/`taken` como resumo em que se confia deixa passar estado forjado; recontados a partir de `kicks` a cada `play`, viram total verificável (`D-19`). Sem banco, "invariante no lugar mais forte possível" quer dizer: derive do dado bruto em vez de acreditar no agregado.
- 2026-08-07 — **Cenário de teste calculado de cabeça erra, e o erro se disfarça de bug do código:** meu caso de "morte matemática com cobranças desiguais" encerrava antes da cobrança que eu queria testar, e a falha apareceu como exceção do motor. Antes de acusar o código, refaça a conta do cenário — o portão de M2 pegou isso porque o teste era específico (placar E contagem), não só `expect(finished)`.
- 2026-08-07 — **Portão que é um `grep` proíbe a agulha até no teste que o verifica:** escrever `Math.random` literal no teste da checagem de camada faria a varredura achar 2 ocorrências e reprovar o próprio portão de M1. Monte a agulha em tempo de execução (`['Math','random'].join('.')`) — vale para qualquer portão escrito como busca textual em `src/`.
- 2026-08-07 — **Asset de teste pequeno demais faz o portão passar sozinho:** o Vite embute arquivo < 4 kB como `data:` no HTML, e o que está embutido nunca dá 404 — a sonda de E-1 teria aprovado até com o `base` errado. Portão escrito como "carrega sem 404" exige conferir que o artefato virou ARQUIVO na saída, não só que a página abriu.
- 2026-08-07 — **Portão em `grep` proíbe a agulha também no COMENTÁRIO do módulo vigiado:** documentar "não persistimos no navegador aqui" escrevendo a palavra proibida dentro de `src/cpu/` reprova `grep -rn "<palavra>" src/cpu/` tanto quanto a chamada real — meu próprio teste de camada pegou isso. A regra anterior (montar a agulha em runtime) vale para o TESTE; no módulo, descreva a proibição sem escrever a palavra.
- 2026-08-07 — **Arquivar decisão libera menos do que o tamanho da linha removida:** todo ID citado fora de `c_decisions.md` precisa continuar com uma linha na coluna 1, senão `check.py` acusa "ID citado que não existe" nos arquivos que o citam. O ganho real é `linha − stub` (medido: D-01..D-05 ocupavam 955 e liberaram 353). Calcule antes de prometer a folga — e conte o que a própria sessão vai gravar.
- 2026-08-07 — **Teste que passa nas duas versões do código não prova a decisão que o motiva:** o teste de `D-26` (a CPU escolhe antes de observar) passaria igual na ordem invertida, porque com a semântica atual de `Q-08` os dois histogramas são disjuntos dentro de uma cobrança. Mantive o teste — ele é a armadilha que fecha quando `Q-08` for respondida — mas escrevi no próprio arquivo que ele **não** prova a ordem hoje. Teste que se anuncia como prova do que não distingue é pior que teste ausente: ele desliga a desconfiança.
- 2026-08-07 — **Módulo carregado por `import()` dinâmico não se testa com mock de módulo; injeta-se:** três correções sucessivas na espera do teste (relógio falso restrito, promessa de abertura em vez de giro do event loop, limpeza de canais entre testes) deixaram de pé uma falha **intermitente** — ≈1 em 3 execuções da suíte completa, a biblioteca real caindo no lugar do duplo e estourando em Node. Enquanto o defeito é intermitente, cada correção parece ter funcionado, e é assim que ele sobrevive a três rodadas de conserto. A saída foi parar de consertar a espera e tirar o carregador de módulos do caminho: uma costura de injeção, um duplo que é função comum, zero I/O sob teste. Regra prática: se o teste precisa que o carregador de módulos coopere, o teste está exercitando o carregador.
- 2026-08-07 — **Sandbox verde e máquina do dono vermelha: a regra 7 não é formalidade:** entreguei 205/205 verde no Linux e o dono recebeu 17 reprovas no Windows, com a mensagem acusando o código de produção. O que separou o diagnóstico do chute foi trocar a asserção muda por uma que **carrega o motivo** — status real + os avisos que o módulo emitiu. Espera de teste que desiste em silêncio, ou asserção que só diz "esperava X, veio undefined", transfere a culpa para o módulo errado e custa a sessão.
- 2026-08-07 — **Relógio falso total mata `import()` dinâmico, e o erro aponta para o lugar errado:** com `vi.useFakeTimers()` o carregador de módulos do Vitest para, porque ele anda em macrotask e o `setImmediate` foi falsificado junto; o canal fica em `'idle'` e 15 testes acusam o código de produção por um defeito que é da espera do teste. `toFake: ['setTimeout','clearTimeout']` falsifica só o relógio sob teste. E toda espera de teste deve **falhar alto quando desiste** — espera que devolve em silêncio transfere a culpa para o módulo errado e custa a sessão inteira.
- 2026-08-07 — **Skill escolhida certo pode ter regra sem objeto, e cumprir por cumprir é enfeite:** `microservice-sync` é a skill que o PLANO deu a M6, mas circuit breaker, retry com backoff e contrato versionado pressupõem HTTP entre serviços que sobem em tempos diferentes — nada disso existe num canal P2P com um peer e um build só. Declarei quais regras não se aplicam e por quê, em vez de implementar um breaker decorativo. O que a skill de fato contribuiu foi o STEP 0, o timeout documentado, o erro traduzido e a exigência de **ler o contrato real da biblioteca** (os `.d.mts` instalados) em vez de supor a API.
- 2026-08-06 — **O bug do baseline morto é argumento de stack, não dívida a herdar:** o `fezGOl` da v1 (typo que virou global silenciosa e corrompeu o placar por meses) foi o que justificou TypeScript em D-02. Defeito de versão anterior não vira QA-NN do projeto novo — vira evidência de decisão.
- 2026-08-07 — **Dois portões que não fecham juntos são pergunta para o dono, não escolha do agente:** `D-02` manda Phaser e o portão de `frontend-uiux` manda teclado, foco e leitor de tela — e `<canvas>` não dá nenhum dos três. A saída não foi ceder num dos dois nem decidir sozinho: foi expor as três saídas com o custo de cada uma e deixar o dono escolher. A híbrida (DOM na entrada, Phaser no desenho) fechou os dois e ainda derrubou o bundle inicial, porque `import()` dinâmico tira a biblioteca da primeira carga (`D-27`).
- 2026-08-07 — **Portão escrito como `grep` só é portão se algo o rodar:** os de camada e de licença de M7 viviam no contrato como comando de terminal, isto é, como coisa que ninguém roda. Viraram teste em `ui.test.ts` (varredura de `src/ui/` e de `src/assets/` contra a tabela de procedência) e agora reprovam sozinhos. Portão que depende de alguém lembrar de digitá-lo já falhou.
- 2026-08-07 — **Teste que diz cobrir um portão tem de usar o padrão DO portão, copiado:** reescrevi o `grep` de camada de M7 como regex "equivalente" mais estreito, e ele ficou verde enquanto o CI reprovava — `export type ModoJogavel = 'cpu' | 'local'` cai no padrão largo do workflow sem haver import de motor nenhum. Portão duplicado em duas redações é dois portões, e o mais frouxo só serve para adiar a descoberta. Copie o padrão, cite a origem, e acrescente um caso que prove que a cópia ainda morde.
- 2026-08-07 — **Padrão de portão largo transforma nome de valor em nome de camada:** com `(engine|cpu|net)` batendo em qualquer linha de `import`/`export`, a string `'cpu'` — que é um MODO do domínio, não um módulo — vira violação de fronteira. Em vez de afrouxar o portão, derive o tipo da porta que já o exporta (`Exclude<Mode, 'online'>`): some o literal, e o tipo ainda passa a acompanhar a fonte sozinho.
- 2026-08-08 — **Duplo de teste que avisa cedo demais inventa um defeito que não existe:** a rede falsa de T-13 avisava `onPeerJoin` em microtarefa, antes de M6 terminar de abrir a própria sala — o canal ia a `connected` e a continuação de `abrirSala` o devolvia a `waiting`, deixando o convidado represando toda jogada. Quatro testes reprovaram acusando o código de produção. Antes de depurar o módulo, pergunte se o duplo está simulando uma ordem que a realidade não produz (peer não entra no mesmo tique em que a sala abre).
- 2026-08-08 — **Roteiro determinístico de teste pode ser determinístico e impossível:** gerar chute e defesa com o mesmo passo coprimo (`i*7 % 3`) garante que as duas zonas NUNCA coincidem, toda cobrança vira gol e a morte súbita não termina — a disputa de referência rodava para sempre e o teste acusava o motor. Roteiro que alimenta os dois lados de uma regra precisa de fórmulas que às vezes colidam, senão ele testa só metade do espaço.
- 2026-08-08 — **Ponteiro repetido linha a linha custa mais que a decisão que ele aponta:** em `c_decisions.md`, `íntegra em [[decisions_archive]]` estava escrito 31 vezes — 716 caracteres, 6% do teto, para dizer uma frase que vale para toda linha `ARQUIVADO`. Arquivar mais decisões teria sido tratar sintoma: o corte certo era dizer a frase uma vez, no cabeçalho. Antes de retirar conteúdo por orçamento, meça quanto do arquivo é a mesma frase repetida — e note que o `check.py` não sabe medir isso, ele só sabe apontar as linhas mais antigas.
- 2026-08-08 — **Teto declarado sem régua declarada não é portão:** "2 frases por linha" numa tabela de sete colunas tem duas leituras, e elas discordam — somando toda célula, `QA-01` (141 caracteres, nunca acusada por ninguém) dá 7 frases e reprova; contando só as células de prosa, dá 2 e passa. A pista de qual leitura vale não estava na regra: estava em **quem o backlog acusava** (`Q-08`, `Q-07`, `Q-09`, `QA-04` — todas de prosa longa) e em quem ele nunca acusou. Antes de cumprir um teto, meça duas linhas que você sabe que estão certas e veja se a sua régua as absolve; régua que condena a linha inocente é a régua errada, não a linha.
- 2026-08-08 — **Num arquivo com orçamento, o bloco que sai não é o maior: é o que sai sozinho:** no `a_context_source.md` os três candidatos tinham tamanho parecido (critério de aceite 529, restrições 528, stack 446), e o desempate não foi char nenhum — foi *o que cada um arrasta junto*. As "Restrições" arrastam o "violou = inválido" que toda sessão precisa por padrão; a "Stack" arrasta regras de dado (inteiro, ISO-3166) cujo gatilho no Mapa não dispara para quem mexe em domínio. O critério de aceite saiu porque **limiar não é medição**: `< 8 MB` mora no tema e `90.320 B` fica no CONTEXT, e a fronteira entre os dois já estava escrita na regra do kit. Antes de escolher o bloco pelo tamanho, pergunte de cada um: quem deixa de ler isto por acidente, e o que ele quebra sem saber?
- 2026-08-08 — **O portão de `grep` não distingue chamada de comentário, e o primeiro a cair nele é quem acabou de consertar o defeito:** o portão de `T-15` era `grep -c 'hostRoom(' src/medicao.ts` sair de 2 para 1; o conserto tirou a única chamada errada e o portão continuou devolvendo 2, porque o comentário recém-escrito explicava o defeito citando `hostRoom()` por extenso. Uma hora antes, o mesmo arquivo de teste do repositório já ensinava a saída — `core.test.ts` e `ui.test.ts` montam as agulhas em tempo de execução exatamente por isso. Ao escrever a prosa que explica um defeito, pergunte se ela cai na varredura que cobra o conserto: o comentário é código para o `grep`.
- 2026-08-08 — **Custo declarado de uma tarefa raramente é a linha; é a forma do arquivo em volta:** `T-15` era uma linha (`joinRoom` no lugar do ternário), e o trabalho inteiro foi que `src/medicao.ts` é *entrada de página* — chama `montar()` no fim e mexe em `document`, então nenhum teste podia importá-lo para provar coisa alguma. O defeito não estava na derivação, que já era determinística e já estava certa: estava em quem a chamava. Antes de estimar "é uma linha", pergunte por onde o teste entra no arquivo — se não houver porta, a tarefa é abrir a porta.
- 2026-08-08 — **Meça o corte máximo ANTES de começar a cortar, senão você descobre no fim que o portão era inalcançável:** `A-12` pedia o registro abaixo de 9.600, e o critério declarado por `A-09` ("fica o que `src/` cita") permitia no máximo 10.055 — 455 acima. Somar os candidatos leva um minuto e transforma "vou cortando até caber" numa pergunta respondível pelo dono antes do primeiro `Edit`. O mesmo minuto teria evitado a segunda rodada de cortes, que veio de estimativa otimista: a linha nova de decisão e o cabeçalho custaram 420 caracteres que não entraram na conta.
- 2026-08-08 — **Quando a porta congelada não entrega o dado, procure a costura FORA do módulo antes de pedir para alargá-la:** `T-16` precisava do `getStats()` de uma `RTCPeerConnection` que `Channel` não devolve, e a saída óbvia — mais um método em M6 — é a mesma que `D-39` e `D-40` já tinham custado uma auditoria para recusar. A instância nasce de `new (rtcPolyfill ?? RTCPeerConnection)(...)` dentro da biblioteca, isto é, de um **global lido no momento da chamada**: embrulhar o global com `Proxy` sobre `construct` alcança tudo sem M6 saber que existe medição. A pergunta que abre esse caminho não é "que porta falta?", é "quem mais toca este objeto?" — e a resposta costuma estar no código de terceiro que você já leu para tipar a integração.
- 2026-08-08 — **Portão travado por falta de recurso costuma estar travado na pergunta errada:** `A-08` passou dias parada em "não consigo segunda operadora, todos os conhecidos usam Claro", e a pergunta real era se o P2P sobrevive à **CGNAT** — que é por operadora, e portanto mede-se com dois aparelhos da mesma. O que faltava não era um segundo chip, era desligar o IPv6 (APN em IPv4 nos dois), coisa de dois minutos em cada aparelho. Quando um portão está bloqueado por um recurso que você não tem, escreva o mecanismo que ele deveria exercitar e pergunte o que de fato o exercita: o recurso ausente costuma ser condição de uma versão mais forte da pergunta, não da pergunta.
- 2026-08-08 — **Medir na fatia favorável dá número verdadeiro e irrelevante, e ele passa porque é verdadeiro:** a taxa de conexão foi medida com IPv6 nos dois aparelhos — onde não existe NAT — e seria cobrada contra um corte que existe por causa de CGNAT. Já tinha acontecido no mesmo projeto com o 4/4 em Wi-Fi×Wi-Fi, que ao menos estava rotulado como controle. O antídoto é escrever, junto do portão, **qual mecanismo o número precisa exercitar**; sem isso qualquer amostra que conecte parece prova. E quando os dois lados precisam de uma condição para a fatia fácil existir, a fração relevante é `p²`, não `p` — com 50% de adoção, um quarto dos casos, não metade.
- 2026-08-08 — **Nome de categoria carrega uma suposição, e a suposição envelhece:** classifiquei `host↔host` como "mesma rede local" porque em IPv4 candidato `host` é endereço privado — e em campo o P2P fechou entre dois **IPv6 globais**, onde não há NAT alguma e `host` já é um endereço da internet. O melhor resultado possível saiu rotulado como o mais inútil, e o resumo dizia `travessia real de NAT em 0 de 1` para uma conexão fim-a-fim perfeita. O dado que desmentia o rótulo estava na mesma linha, calculado por mim: `faixaDoIp` já dizia "público" nos dois lados. Quando duas informações da mesma leitura discordam, é o rótulo que está errado — e classificar pelo **tipo** quando existe o **valor** é escolher a fonte mais fraca.
- 2026-08-08 — **"Isso é impossível" é hipótese, e eu a usei como evidência:** vi um `host↔host` entre dois celulares em 5G, declarei impossível, e o usei para reforçar um diagnóstico de leitura ruim. Era IPv6, e era a leitura mais correta da rodada. A causa real do defeito estava certa por outro caminho, então a conclusão sobreviveu ao erro — que é justamente o que torna esse tipo de engano barato de cometer e caro de encontrar. Impossibilidade afirmada sem checar o mecanismo é chute com voz de prova; se ela não for necessária para o diagnóstico, não a escreva.
- 2026-08-08 — **Instrumento que observa objeto de terceiro tem de filtrar por ESTADO, nunca por janela de criação:** `T-16` limpava a lista de `RTCPeerConnection` observadas no início de cada tentativa, o que parecia óbvio — "cada tentativa vê só as conexões dela" — e perdeu 11 de 12 leituras em campo. A Trystero mantém um pool de 20 conexões no fecho do módulo e o reaproveita entre salas: a conexão que fecha a tentativa de hoje nasceu na de ontem. Eu tinha lido `peer.mjs` para achar o construtor e **não** li `strategy.mjs`, onde `offerPool ||= new OfferPool(...)` diz o tempo de vida. Ler a biblioteca para descobrir *como* o objeto nasce é metade do trabalho; a outra metade é *quanto ele vive*.
- 2026-08-08 — **Ausência precisa dizer o motivo, senão ela custa a ida a campo inteira:** "par não lido" onze vezes seguidas não distinguia "nenhuma conexão observada" de "nenhuma conectada" de "conectada sem par no relatório" — três defeitos diferentes com a mesma cara. O dono voltou do campo com 12 tentativas e zero diagnóstico. Todo caminho que devolve "não sei" num instrumento tem de devolver junto **o que faltou**: é a diferença entre uma correção e outra viagem.
- 2026-08-08 — **`lib.dom` tipa `RTCStatsReport` só com `forEach`, embora todo navegador entregue um `Map`:** escrever o parser contra `ReadonlyMap` compila na cabeça e reprova no `tsc`. Tipar pelo que a plataforma **promete**, e montar o índice por conta própria, custou três linhas e deixou um `Map` comum servir de duplo no teste sem um `as` — o mesmo princípio do "leia o contrato real da biblioteca", aplicado ao contrato do navegador.
- 2026-08-08 — **Menu de saídas escrito por quem achou o defeito herda a suposição que causou o defeito:** a nota de `QA-08` listou três saídas, e as três partiam de que o anfitrião precisa chamar `hostRoom` — a mesma assimetria host/guest que pôs o defeito lá. Duas das três então propunham mexer na porta congelada para restaurar uma simetria que o módulo **já tinha**: `hostRoom` e `joinRoom` caem no mesmo `createChannel`, e o comentário do próprio arquivo dizia isso. Antes de escolher dentro de um menu de saídas, leia a porta e pergunte qual suposição as opções compartilham; quando todas pedem para alargar o contrato, a suspeita certa é a suposição, não o contrato.
- 2026-08-12 — **Portão que varre texto conta o comentário que o explica:** o portão de M1 conta as ocorrências do nome do gerador nativo nos arquivos de `src/`, e a linha mais natural a escrever no `T-17` era um comentário em M5 avisando "não use o gerador nativo aqui" — que nomeia a função e faz a contagem ir a 2, reprovando o portão pelo comentário que o defende. Os testes do próprio kit já sabiam disso: `core.test.ts` monta a agulha com `['Math','random'].join('.')` e `net/index.ts` fala do gerador nativo por perífrase. A regra geral: quando o portão é `grep` sobre o código, a documentação **dentro** do código é matéria do portão, e o `QA-05` (que registra exatamente isso nos termos de marca) é o precedente — antes de escrever o nome do que é proibido, verifique se alguém o está contando.
- 2026-08-12 — **Atributo da plataforma só funciona enquanto a folha do projeto não o contraria:** `hidden` esconde por uma regra do NAVEGADOR (`[hidden] { display: none }`), e no cascade a regra do AUTOR ganha — então `.aviso { display: flex }` fez todo `elemento.hidden = true` do projeto virar enfeite, com a caixa de erro vazia na tela desde `T-10` sem ninguém notar (`QA-16`). O engano é barato de cometer porque o código lê como se funcionasse: `hidden = true` é uma frase que promete o resultado, e o defeito só aparece em pixel, num navegador que a suíte não tem. A regra geral: ao escrever `display` em qualquer classe que um dia possa levar `hidden`, escreva junto o `[hidden] { display: none }` dela — e desconfie de todo estado de UI que o teste não consegue enxergar, porque é exatamente ali que a promessa do código e o comportamento real se separam sem ruído.
- 2026-08-12 — **Exceção escrita como REGRA de formato é afrouxamento com cara de rigor:** o catálogo de M4 precisava aceitar um código que o ICU não resolve, e a linha mais curta era `if (/^[A-Z]{2}-[A-Z]{3}$/.test(code)) return;` — que parece portão, tem regex, e na verdade abre a porta para qualquer subdivisão futura entrar com nome digitado, exatamente o buraco que `D-23` fechou. Como **lista fechada de tamanho 1**, o mesmo trabalho vira o oposto: a segunda entrada não passa calada, ela derruba o carregamento do módulo, e o teste consegue dizer *qual* é o literal permitido em vez de só conferir um formato. A regra geral: quando o dado tem uma exceção conhecida, enumere a exceção; regex que a descreve aceita também todas as exceções que ninguém decidiu ainda — e ela precisa de um validador **exportado**, senão o teste só inspeciona a lista de hoje e nunca a próxima linha errada.
- 2026-08-12 — **Asset pequeno demais é embutido pelo bundler, e isso troca o TIPO do dado sem avisar:** M4 promete que `flag` é "caminho local", e 24 dos 32 SVGs estavam abaixo do limite de inline do Vite — sem `?no-inline`, o mesmo campo sairia ora `/assets/br-<hash>.svg`, ora um `data:` de 3 kB, conforme o tamanho do arquivo. O que torna isso caro é onde o defeito **não** aparece: a suíte roda em modo dev, onde nada é embutido, então todo teste passa e a divergência só existe em produção. A regra geral: quando um valor de domínio é produzido pelo build, o formato dele é decisão de configuração, não de código — force o formato explicitamente e cobre a configuração na **fonte** (o teste pode ler o próprio arquivo), porque o ambiente que roda o teste não é o ambiente que gera o dado.
- 2026-08-12 — **"Some sozinho quando X chegar" é previsão, não conserto — e ela envelhece dentro do registro:** `QA-18` dizia que o disco com o código sumiria quando as bandeiras chegassem, porque `marcaSelecao` já devolvia `ehBandeira`. Chegaram, e não sumiu: quem desenha é outra função, que escreve o campo como texto e ignora a chave — o achado só mudou de forma, de código curto para caminho de arquivo. A regra geral: quando fechar um achado depender de trabalho em outro módulo, a linha do registro diz **quem** faz e **onde**, nunca que o problema se resolve por consequência; e a sessão que entrega o X vai **conferir** a previsão em vez de assumi-la — confirmar custa um `grep`, e a previsão errada some do radar exatamente no dia em que ela deveria ser cobrada.
- 2026-08-13 — **Campo que carrega dois formatos precisa de um discriminante que alguém LEIA, e o dia em que ele passa a importar é o dia em que o dado muda:** `marcaSelecao` devolvia `texto` (código ISO ou caminho do SVG) e `ehBandeira` para dizer qual — e por três tarefas ninguém leu `ehBandeira`, porque enquanto `flag` era `null` os dois ramos coincidiam e a tela ficava certa por acidente. `T-19` entregou os arquivos e a mesma linha passou a pintar `/assets/br-<hash>.svg` dentro de um disco de 34px, em 64 cartões. Valor exportado sem leitor não é API defensiva, é uma decisão adiada com prazo escondido no dado: ou o consumidor lê o discriminante, ou ele sai. E o antídoto que escrevi aqui em 12/08 para o `[hidden]` — "escreva a linha `[hidden]` de cada classe" — é do mesmo formato frágil: lista por classe é o que a próxima classe esquece de entrar (`.campo__sem-canvas` esqueceu). O que se mantém é um **invariante global que cabe numa frase que o teste lê**, não uma lista que depende de disciplina.
- 2026-08-13 — **Arte como DADO no código resolve três problemas de uma vez, e o do meio é o que ninguém antecipa:** os bonecos do campo viraram grade de caracteres em `sprites.ts` em vez de PNG em `assets/`, e com isso a procedência de licença some (nada a rastrear), a arte fica diffável e **testável sem navegador** — mas o ganho decisivo foi outro: a camisa virou **parâmetro**. O pedido "cada goleiro com a cor da sua seleção" seria 32 arquivos, ou um `tint` global que tingiria pele e chuteira junto; como dado, é um caractere da grade que muda. A regra geral: quando um asset tem uma dimensão que varia com o domínio, gerá-lo de dado é mais barato que armazená-lo — e o teste que sobra ("toda linha da grade tem a mesma largura") pega a classe de erro que o olho perde. O contrapeso, declarado: o agente não consegue VER o resultado no sandbox, e desenhar sem ver é o que fez os três primeiros sprites saírem blocos sem braço — foi preciso escrever um encoder de PNG só para olhar antes de embarcar, e isso deveria ser o primeiro passo, não o terceiro.
- 2026-08-19 — **A folha do NAVEGADOR é a metade da direção visual que ninguém escreve, e ela só aparece medindo o elemento vivo:** os `<fieldset>` do jogo carregavam `border: groove` cinza e 12px de padding desde `T-10` — a moldura de formulário de 1998 estava na tela, e nenhuma linha de `estilo.css` a pedia; ela também comia 31px da largura do cartão de seleção, então o sintoma parecia "o nome não cabe" e o culpado parecia ser o tamanho da bandeira. A regra geral: quando a tela parece prototípica, meça `getComputedStyle` dos CONTÊINERES antes de mexer no que você escreveu — o defeito costuma ser um padrão de UA que a folha nunca cobriu, e ler o CSS do projeto nunca o mostra, porque ele não está lá. E o corolário do dia: um bloco `prefers-reduced-motion` que zera só a **duração** é incompleto assim que a folha ganha cascata — com `animation-delay` intacto e preenchimento `both`, o elemento fica invisível durante o atraso, que é mais movimento percebido, não menos.
- 2026-08-19 — **Estado derivado que consome acaso tem de ser decidido UMA vez e gravado; recalculá-lo é mover o gerador ao olhar a tela:** a classificação dos grupos de M8 pode terminar em sorteio, e a versão óbvia — ordenar o grupo toda vez que alguém pede a tabela — deixaria `group()` consumir `Rng` a cada leitura, fazendo a linha do tempo do torneio depender de quantas vezes a tela foi aberta. O detalhe que fecha a armadilha é que **nada disso reprova nos testes usuais**: determinismo passa (a mesma sequência de chamadas dá o mesmo resultado) e a serialização passa (o número de sorteios consumidos é gravado). Só quebra no aparelho, onde ninguém abre a tabela o mesmo número de vezes duas vezes. A regra geral: separe *derivar* de *sortear* — o que consome acaso acontece num instante nomeado do fluxo e vira dado gravado; o que só lê recomputa à vontade, e o teste que prova isso é `consumed` antes e depois de N leituras.
- 2026-08-19 — **Contra um módulo que já é determinístico, o teste que pega o defeito de arquitetura é um ORÁCULO que refaz a simulação, não uma comparação consigo mesmo:** `D-66` (um `Cpu` por disputa) não aparece em nenhum teste de determinismo, de serialização ou de contagem — reusar a instância consome o mesmo número de sorteios e produz um torneio igualmente reprodutível, só que **errado**. O que pega é o teste reescrever, do zero, o que o módulo deveria fazer numa disputa (criar as duas CPUs novas, sortear quem cobra, alternar `pick`/`observe`) e comparar disputa a disputa. A regra geral: quando a decisão é sobre o **tempo de vida** de um objeto interno, nenhuma saída pública a revela isoladamente — só um segundo implementador da mesma regra revela, e ele cabe no arquivo de teste. Conferido por mutação, que é o passo que falta quando um teste desses passa de primeira.
- 2026-08-19 — **Dado ausente não é um valor, é uma propriedade da LINHA, e por isso a tela decide célula a célula:** a tabela do grupo em `T-14` tem duas metades — a linha do jogador, cujos gols são zero **estrutural** (as 3 disputas dela são justamente as que voltam sem placar), e as outras três, cujo número é verdadeiro e parcial. Mostrar `0 × 0` na primeira era inventar; esconder a coluna inteira era jogar fora as outras. `—` na linha que não tem dado, soma nas que têm, e uma nota abaixo da tabela: a assimetria da tela é a forma do dado, não gosto (`D-67`/`Q-13`).
- 2026-08-19 — **Antes de escolher entre "apertar o cinto" e "afrouxar o teto", meça o pool: um dos dois pode já não existir, e a sessão inteira estava sendo gasta na comparação errada:** `A-16` chegou como "arquivar mais ou subir o teto", e a medição mostrou que arquivar rendia **0 caractere** — não pouco, zero — porque toda linha viva era citada por algum `.md`. As duas alternativas que restavam rendiam 788 e 821 contra as 975 do portão, ou seja, também não eram saída. A regra geral: quando a decisão é entre duas políticas, a primeira coisa a medir é o **tamanho do que cada uma consegue mover**; e uma opção que já foi REJEITADA volta à mesa não por insistência, mas quando o custo que a matou foi pago por outra decisão no meio-tempo — aqui, o pedágio de manifesto de `D-50`. O corolário caro: um teto que só se resolve subindo tem prazo, e o prazo tem de entrar na própria decisão em sessões, senão a próxima sessão redescobre o problema como se fosse novo.
- 2026-08-19 — **Portão de etapa se confere item a item, e passagem de aparelho de OUTRO card não vale por ele:** E-5 chegou como "falta só declarar" porque `T-20` e `A-14` tinham sido aprovados no aparelho no mesmo dia. Conferidos os cinco itens, três passaram com prova barata e reproduzível (64 disputas por instrumentação; SHA-256 dos 32 SVGs arquivo a arquivo; `grep` da lista-morta em 0) — e os dois que faltavam eram justamente os que **só existem em aparelho**, que é onde a passagem já feita **não tinha ido**: `A-14` cobria o painel do sorteio, não o torneio. A regra geral: quando um portão cita N itens, o custo de declará-lo fechado é N conferências, e a mais perigosa é a que **parece** coberta por um card vizinho da mesma data — leia o portão do card vizinho, não a data dele. O corolário barato: a conferência que passou vale escrita no card novo com a prova, senão a próxima sessão de aparelho refaz as três de graça.
- 2026-08-19 — **Tabela de custo se confere contra o `dist/`, não contra o `package.json`: o endpoint que falta é o que nenhuma linha de código escreveu.** A tabela de [[stack]] tinha as 5 linhas certas para as 2 dependências e os 2 hosts que o projeto **escolheu** — e não tinha os 4 servidores STUN que o `defaultIceServers` de `@trystero-p2p/core` põe no ar porque M6 só sobrescreve `turnConfig`. Nenhuma leitura do `src/` acharia: não há `stun:` em lugar nenhum da árvore. O que achou foi `grep` de `stun|https?://|wss?://` sobre os chunks do `dist/`, que é o único artefato que sabe o que de fato vai ao ar. A regra geral: portão que fala do que o build **alcança** se cobra do build, e o default de biblioteca é a categoria inteira que a revisão de dependência não vê. O corolário: no mesmo `grep` conferi a contagem que a tabela declarava (46 relays contra 47 no pacote) — número copiado de documentação envelhece na versão seguinte.
- O README é o único doc que ninguém relê por estar sempre "pronto": chegou ao dia da entrega com os placeholders do template intactos, num repositório público por exigência do Pages. Varredura de entrega abre o README como se fosse a primeira vez — e confere cada comando contra o `package.json`, porque escrever `npm run dev` sem olhar é o mesmo defeito, uma camada acima.
- 2026-08-19 — **Quando duas saídas mexem no mesmo contrato congelado, o desempate barato não está no contrato: está em qual das duas já rodou em campo, e em qual delas o RELÓGIO começa:** `Q-11` chegou como "as duas mexem em porta congelada, escolha uma", e as duas de fato mexiam. O que decidiu foi (a) `src/medicao.ts` já implementar a saída (1) inteira — sortear o ID antes da sala, e os dois lados entrarem por `joinRoom` — com os 17/17 de `A-08` saindo dela, e (b) `armarTimer()` rodar dentro de `createChannel`, o que faz a saída (2) entregar o ID **depois** de o prazo de 20 s já correr. A regra geral: uma decisão de porta se lê na ORDEM dos eventos que ela impõe ao chamador, não só na forma da assinatura — e a página de medição, a de teste ou o script de operação costumam ser o protótipo já validado da saída que a discussão trata como hipotética. O corolário caro: o defeito que a comparação revelou (`QA-22`) não pertence a nenhuma das duas saídas e sobreviveria à escolha certa — decisão que resolve a assinatura e cala sobre o relógio entrega uma tela que não conecta.
- 2026-08-19 — **"O pool acabou" é uma afirmação sobre a OPERAÇÃO que você mediu, não sobre o arquivo: mudar o verbo muda o número.** `A-16` mediu "virar ponteiro puro" (manter a linha, encolher o texto) e achou 788, abaixo do portão; concluiu que arquivar tinha acabado e subiu o teto. `A-20` mediu "retirar a linha" — porque a íntegra dela já estava no arquivo, 15/15 conferidas — e achou **1.536** no mesmo arquivo, no mesmo dia. A regra geral: antes de declarar um pool esgotado, escreva qual **operação** foi medida; duplicata e compressão parecem a mesma economia e diferem por 2x. O corolário: a checagem que aceita ID definido só no arquivo morto (a 10) é o que torna a retirada segura — leia o **script** antes de supor que a convenção escrita no cabeçalho é a que a máquina cobra, porque aqui as duas divergiam.
- 2026-08-20 — **Antes de negociar o valor de um prazo, meça ONDE ele começa: o defeito costuma estar no instante em que o relógio arma, e não no número.** `QA-22` chegou como "20 s não cabem num convite por link", e a saída óbvia era subir o teto. O que matou a pergunta foi ler duas linhas de disco — `armarTimer()` é síncrona dentro de `createChannel`, e M5 só cria o canal dentro de `createSession` — e então achar o mesmo fluxo **já validado em campo**: a página de medição sorteia o ID, fecha o canal na hora e só arma o relógio no toque, e foi assim que os 17/17 saíram com mediana de 266 ms. Um prazo 75x maior que a mediana não é um prazo curto: é um prazo armado cedo demais. O corolário: quando o remédio é "chamar mais tarde", ele não custa um byte do módulo congelado, e a decisão certa é a que NÃO muda código.
- 2026-08-20 — **Tipo que exclui um caso para dizer "ainda não sei fazer isto" vira mentira no dia em que a tarefa chega — e o portão textual que guarda o caso antigo não o percebe:** `ModoJogavel = Exclude<Mode,'online'>` estava certo enquanto o online não tinha tela, e três ramos escritos como `modo !== 'cpu'` dependiam disso em silêncio (a derivação da vez e a frase do sorteio mandariam "passe o aparelho" para um adversário em outra cidade). Ao ampliar o tipo, o compilador não acusa NENHUM deles — são todos válidos. O que os encontra é procurar por `!== 'cpu'` e `=== 'cpu'` antes de ampliar, e reescrever cada um em torno do caso que de fato é diferente (`=== 'local'`).
- 2026-08-20 — **Estado que a porta congelada não transporta ainda pode viajar no LINK: antes de reabrir contrato, pergunte o que o convite já leva.** `A-22` mostrou dois aparelhos com seleções diferentes na mesma disputa, e a leitura óbvia era "o canal precisa carregar as seleções" — o que é `Move` novo, `D-13` reaberto e sessão de backend. O link do convite é de M7, já atravessa o mensageiro e não é contrato de ninguém: a mesma correção custou um parâmetro e zero byte em `src/net`. O critério para saber se cabe: o dado é **rótulo** (nenhuma regra deriva dele) e o link já existe.
- 2026-08-20 — **Quando um comentário do código é a razão de um conserto ser palpite, o teste que fecha a lacuna é o que MUTA o código para tornar o comentário verdadeiro e vê a medição reprovar.** A lacuna de `QA-25` vinha de uma frase em `aoMove` ("`seq` menor é reenvio da fila de M6") que ninguém tinha conferido. Testar só o comportamento atual provaria que hoje não acontece, sem provar que o teste pegaria se acontecesse. Mutar `escoarFila` para reenviar o histórico — a hipótese literal do comentário — e ver 4 dos 6 testes reprovarem é o que separa medição de coincidência. A mutação vive minutos e é revertida por `git checkout`; o que fica é o número. Corolário: o argumento que fecha a pergunta não é "os testes passam", é o par de estados mutuamente exclusivos que eles exibem — um lado só anda consumindo o `seq` que a fila entrega uma vez só.
- 2026-08-20 — **Antes de comparar as saídas de um defeito, pergunte qual delas o próprio conserto deixa de meia tela:** em `QA-25` os DOIS lados ficam presos (cada um descarta o `seq` do outro), e uma saída que só desperta o lado veterano deixaria o que voltou olhando para "Esperando o outro jogador…" para sempre. O item que separa as portas não foi custo nem elegância, foi essa segunda metade — e ela saiu de graça na porta M5 porque `close()` vira `onPeerLeave` do outro lado, que rearma o relógio que já existe. Corolário: num defeito simétrico, "quem mais fica preso?" é a primeira pergunta do portão, não a última.
- 2026-08-20 — **Status sintetizado só sobrevive se você o declarar TERMINAL: o passo seguinte do próprio conserto costuma apagá-lo.** `D-80` põe `link='failed'` à mão e fecha o canal — e `close()` avisa os assinantes com `'closed'` no tique seguinte, o que devolveria a tela travada que a guarda acabara de resolver, agora sem timer para socorrê-la. A linha que faz o conserto funcionar não é a detecção, é `if (link === 'failed') return` no tradutor de status. Corolário: quando uma camada passa a **inventar** um valor que antes só repassava, o primeiro teste a escrever é o que roda o resto da sequência e confere que o valor inventado ainda está lá no fim.
- 2026-08-20 — **O conserto anterior declarou a lacuna dele; leia essa frase antes de auditar o defeito seguinte — ela costuma ser o defeito.** `D-80` escreveu que não cobre a sessão que reentra ANTES de qualquer cobrança fechar, e `QA-26` é exatamente esse recorte: com `kicks.length=0` nos dois lados, a pré-condição do discriminador nunca chega. Auditar sem reler a lacuna teria proposto reforçar `D-80`, que é provadamente inalcançável ali.
- 2026-08-20 — **Teste que passa pode estar passando na camada errada: cheque por onde o evento realmente entra antes de comemorar.** No portão de `D-81` escrevi um teste para "lado de terceiro tipo segue morrendo em silêncio" injetando `side: 'C'` na rede falsa — e ele passava sob TODAS as mutações, porque `isMove` derruba o payload em M6 e ele nunca chega à guarda de M5 que o teste dizia medir. Guarda repetida na fronteira é barata e certa de existir, mas ela é **inalcançável pelo fio**, logo não é testável por ali. Corolário: um teste que sobrevive à mutação do código que ele nomeia não é um teste robusto — é um teste que mede outra coisa; ou reescreva o que ele afirma, ou ele sai.
- 2026-08-20 — **Portão escrito antes do experimento também erra, e o erro aparece na hora de medir: compare o número do portão com o mecanismo que a porta escolhida realmente tem.** O item 1 de `QA-26` pedia os DOIS aparelhos em `D-35` em ≤2 s; a porta D é assimétrica por construção (quem recebe cai no tique, quem tocou cai pelo relógio de 20 s do `onPeerLeave`), e o portão do MESMO mecanismo em `A-24` já tinha escrito "em até 20 s". Entregar sem apontar isso mandaria o dono reprovar em campo uma porta que funciona. Corolário: ao implementar contra portão escrito por outra sessão, releia cada número como previsão a conferir — e quando ele não bater com o mecanismo, o achado é do portão, não do código, e vai para o dono ANTES da medição.
- 2026-08-20 — **Número que anda na tela é regressão de acessibilidade se você o puser numa região viva: o leitor de tela passa a recitá-lo por cima de tudo.** O contador de `T-22` mora ao lado de uma faixa `aria-live="polite"`, e herdar isso teria feito o leitor anunciar "19 s… 18 s… 17 s" sem parar, engolindo o próprio aviso que importa. A saída é dividir os papéis: a faixa anuncia UMA vez o que mudou ("o outro jogador parou de responder"), e o número fica com `aria-live="off"`, visual. Corolário: antes de animar ou tiquetaquear qualquer texto, pergunte o que ele soa — a regra 6 da skill cobra foco e contraste, mas é o `aria-live` que quebra em silêncio.
- 2026-08-20 — **Portão escrito para o sandbox cita observáveis que o campo não tem: traduza o observável ANTES de mandar o dono medir, ou ele mede a coisa errada.** O portão de `T-23` pedia "5 cobranças sem um único descarte **no console**", e celular não tem console. A tradução não é afrouxar: descarte de `Move` não é silencioso no resultado, é divergência de estado — foi o mecanismo de `QA-25` —, então **placar final idêntico nos dois aparelhos** prova o mesmo com o que a mão do dono alcança. Sem essa tradução o dono ou reprova por não conseguir medir, ou responde "passou" sobre um observável que não olhou.
- 2026-08-20 — **Portão de melhoria que mede uma MÉDIA por sessão não é cobrável por uma regra que age por LINHA: confira a aritmética dos dois antes de aceitar o limiar.** O §8 de `A-21` pedia `≤ +400/sessão` com um mecanismo de `400/linha`; qualquer sessão de duas decisões reprova por construção, e o retroativo deu +407. E o baseline do portão (+638) era de antes do último corte: portão escrito numa sessão e medido três sessões depois compara contra um número que já não existe.
- 2026-08-20 — **Tabela de evidência medida no começo da sessão envelhece dentro da própria sessão: reconte contra o disco antes de congelar a lista num script.** O §2.2 de `A-21` nomeou 18 linhas acima de 400, mas o corte de `D-74` no QA (§1, mesma sessão) já tinha arquivado 4 delas; a isenção de `D-83` saiu com 14, medidas na hora.
- 2026-08-20 — **Fechar achado custa caracteres no mesmo orçamento que ele mede: escreva a coluna "Fechado em" já contando o corte que ela dispara.** Fechar `QA-24`/`QA-28`/`QA-30` somou 309 e levou o QA de 6.141 para 6.450, acendendo o aviso de 80% que a sessão não tinha antes — o conserto não é encurtar a evidência, é rodar o corte de `D-74` que o próprio fechamento habilita (os três saíram para o arquivo e o QA caiu a 4.935). Corolário: isenção congelada por ID acompanha o corte — linha arquivada não é linha viva, então `ISENTAS_LINHA_MAX` encolhe junto, senão a tupla vira lista de IDs que não existem mais na tabela.
- 2026-08-20 — **Relógio de UI em P2P só é seguro se o estouro produzir a MESMA jogada que um dedo produziria: se ele produzir um veredito, os dois relógios divergem.** Em `T-24` a saída ingênua era cada aparelho contar 15 s e **resolver** a cobrança; sem árbitro, 15,05 s aqui e 14,90 s lá resolvem a mesma cobrança de dois jeitos e os estados racham em silêncio. A saída que passou não toca o fio: aos 15 s a tela sorteia a zona e a manda por `choose()` como qualquer toque — o outro lado nem sabe que houve estouro. Corolário: antes de pôr contagem regressiva numa tela sem servidor, pergunte o que ela **emite** no zero; se for decisão, é regra de negócio e porta de módulo, não UI.
- 2026-08-20 — **Coluna nova numa `<table>` de largura fixa não custa só bytes: custa a largura que a coluna do NOME tinha, e o estouro só aparece no conteúdo mais longo do catálogo.** Em `T-25` a quinta coluna ("Pts") levava a tabela a 359px dentro de 336px em 360x640 com `Costa do Marfim` na linha do jogador — invisível no estado zerado que a tela mostra ao começar o torneio, e rolagem horizontal reprovada por `T-20`. Meça o pior caso injetando o texto mais longo por DOM antes de fechar, e faça a célula de texto ceder com `max-width: 0` — as colunas de número são `width: 1%` + `nowrap`, então elas nunca cedem sozinhas.
- 2026-08-20 — **Animar um elemento que a tela RECONSTRÓI a cada desenho é animar do zero a cada desenho: antes de escrever a `@keyframes`, ache quem chama a função de render e quantas vezes.** Em `T-26` a marca do painel de sorteio era refeita com `limpar()` + `marca()` a cada `desenhar()`, e no `online` `aoNotificacaoDeRede()` chama `desenhar()` até em notificação sem novidade — o giro teria recomeçado várias vezes antes do 1º toque, sem nada disso aparecer em `cpu`, onde o sandbox olha. O conserto é um booleano de transição escondido -> visível, não um `animation-fill-mode`. Corolário de sandbox: a pane de navegador escondida não compõe quadros, então `animation.finished` nunca resolve e `await` nela trava a sessão — leia o quadro final fixando `currentTime`, e declare que quadros são portão de aparelho.
- 2026-08-20 — **Portão de dono com 4 itens volta como 1 "tudo certo": se o card quer número, ele tem de pedir UM número, não quatro sins.** `A-25`, `A-26`, `A-27` e agora `A-28`/`A-29` fecharam as cinco por confirmação global, e as cinco deixaram itens sem observação separada — o placar dos dois lados em `A-25`/`A-27`, o `prefers-reduced-motion` real em `A-29`. Escrever "anote o placar" no meio de uma lista de quatro não muda o comportamento; o que mudaria é o card ter **um** observável que só existe se a pessoa olhou (um número a transcrever, uma foto), e os demais itens declarados desde já como cobertos por construção. Corolário: enquanto o card não mudar de forma, a lacuna de forma se declara no fechamento — nunca se finge que a confirmação global cobriu item a item.
- 2026-08-20 — **`margin: auto` para centrar um bloco DESLIGA o esticamento dele em grade e em flex: o item passa a se dimensionar pelo conteúdo, e um botão capado vira um botão encolhido.** Em `T-27` a regra `max-width: 420px; margin-inline: auto` no bloco de ação entregou um "Começar" de **100px** — alvo menor que o do celular, dentro de uma folha três vezes maior, e nada no CSS parece errado ao ler. Para capar sem encolher use `justify-self: center` (grade) ou `align-self: center` (flex de coluna) COM `width: 100%` e o teto. Corolário do mesmo card: seletor que não casa com nada é pior que seletor ausente, porque parece cobertura — `.grupo.empurra` foi escrito para um bloco que é só `.empurra`, e só a medição no navegador denunciou; confira o seletor contra o DOM real, não contra a memória de como as outras telas montam o bloco.
- 2026-08-21 — **Quando um layout novo se aplica "onde faz sentido", escreva o critério como pergunta sobre o COMPONENTE, não sobre a tela: "esta tela tem par a formar?" deixa a mesma grade em duas larguras.** `D-86` alargou seleções e torneio porque as duas emparelhavam blocos, e deixou a tela de começar o torneio de fora porque ela escolhe UMA seleção — mas é a **mesma** `grade()` de 32 cartões, e o dono viu 4 numa tela e 2 na outra no primeiro minuto de uso. O sandbox não pega isso: eu medi cinco viewports, e todos na tela certa. Corolário que vale mais que a regra: card de aparelho que pede **um número** devolve o número E os achados de redor — `A-30` trouxe dois defeitos que nenhuma medição minha tinha, e as cinco conferências anteriores, pedidas como listas de sim/não, não trouxeram nenhum.
- 2026-08-21 — **"O toque não se perde" é medível no sandbox, e a medida é `elementFromPoint` no centro do alvo com a sobreposição no pico do quadro: não deixe esse item ir para o card do dono como "coberto por construção".** Em `T-26` o item (2) de `A-29` fechou sem número porque eu o declarei coberto por construção — a animação não tocava `pointer-events` nem os botões. Em `T-28`, com uma sobreposição de tela cheia de verdade, bastou fixar `currentTime` no pico e perguntar ao documento quem está debaixo do centro de cada zona: as três devolveram o próprio `<button>`, nos dois viewports. Corolário sobre a escolha de projeto: entre `pointer-events: none` e "sair ao primeiro toque", só a primeira não perde o toque — a segunda gasta o toque para se fechar, e é justamente esse o toque que o portão protege.
- 2026-08-21 — **Pedir UM número não basta se ele vier no mesmo pedido que as observações curtas: as observações são mais fáceis de responder, e engolem o número.** `A-30` funcionou porque o número era quase tudo o que o card pedia; `A-31` pediu o mesmo número MAIS duas observações, e voltou com as duas observações respondidas e um *"aparentemente tudo correto aqui"* no lugar do inteiro — a confirmação global de `A-25`..`A-29` de volta pela porta dos fundos. O número veio na primeira vez em que foi perguntado **sozinho**. Corolário para o formato do card: o observável que é PORTÃO vai numa pergunta isolada, e as observações de redor vão depois, declaradas como não-portão — nunca na mesma lista.
- 2026-08-21 — **Portão herdado é hipótese, não proteção: antes de montar o dado que ele deveria barrar, meça se ele PODE reprovar alguma coisa.** `P-6`(b) travava as 32 cores nacionais em "cada cor tem de passar no teste de contraste de `T-20`", e a varredura dos 360 matizes mostrou que o pior caso é 7,28:1 contra um limite de 4,5:1 — saturação e luminosidade são fixas por papel, então o matiz nunca decide contraste e o portão não podia fechar. O que realmente barrava o card era outra coisa, e o card não a mencionava: 14 das 32 seleções têm vermelho como cor nacional, e a tabela "curada" dá 15 matizes onde o hash dava 30. A premissa escrita no pedido ("tabela curada não colide por construção") era falsa, e só a medição a derrubou. Quando um card afirma um portão E uma premissa, meça os dois — é a premissa não medida que costuma ser a que trava.
- 2026-08-21 — **Quando um dado novo entra num canal que já existe, olhe primeiro o que o canal CEDE quando há conflito: é ali que o dado novo é destruído, não na entrada.** Em `T-29` a cor nacional entrou num pipeline cujo desempate (`matizDistinto`) resolvia colisão jogando o matiz do lado B para o oposto do círculo. Com cor de hash isso não custava nada — a cor não significava nada. Com cor nacional, o mesmo desempate transformava Portugal em ciano contra a Espanha, ou seja, o mecanismo de conserto apagava exatamente o dado que a tarefa veio introduzir. O conserto foi trocar a MOEDA do desempate (cede o padrão, não a cor), e isso só apareceu porque perguntei "o que essa função sacrifica?" antes de escrever a tabela. Vale para qualquer campo novo que entre num lugar com fallback, normalização ou desambiguação já escrita.
- 2026-08-21 — **Portão de dono que pede UM número tem de pedir o número do mecanismo que a decisão ACABOU de criar; senão o inteiro certo reprova a entrega certa.** Em `A-32` eu perguntei "quantas CORES de camisa diferentes você vê?" e declarei `2` como aprovação — mas `D-88`, do dia anterior, tinha decidido que as duas camisas ficam na MESMA cor e que quem separa é o PADRÃO. A resposta literalmente correta era `1`, e o card a teria reprovado. Passou porque o dono descreveu em vez de contar. A regra que faltava não é "peça um número" (`A-30`) nem "peça o número sozinho" (`A-31`): é **releia a decisão que acabou de ser tomada e pergunte pela variável que ELA move**. Portão escrito antes da decisão, ou por hábito do card anterior, mede o mundo velho.
- 2026-08-21 — **Isolar a pergunta conserta o NÚMERO, não a OBSERVAÇÃO: pedir "duas coisas curtas" sozinhas, em mensagem separada, ainda volta como "tudo correto".** Em `A-32` apliquei o conserto de `A-31` à risca — número primeiro e sozinho, observações depois e sozinhas — e as observações voltaram globais assim mesmo, pela sexta vez desde `A-25`. O custo não é uniforme: a observação (a) era genérica e a confirmação global a cobre; a (b) pedia um CASO ESPECÍFICO (uma disputa de seleção verde), e "tudo correto" não diz se o caso chegou a aparecer. A regra que sobra: **observação que depende de um caso específico não pode ser pedida como observação** — ou ela vira um portão com procedimento próprio ("jogue o México e responda"), ou aceite que ela fica coberta por medição e declare isso.
- 2026-08-21 — **Texto que declara uma lacuna à pessoa é dívida com data de vencimento: quem entrega o dado tem de apagar a frase, senão a tela passa a mentir e ninguém vê.** `QA-21` viveu 7 dias — `T-19` entregou as 32 bandeiras em 12/08 e a tela inicial seguiu dizendo que elas "ainda não entraram", dois toques antes de mostrá-las. Sobreviveu a `T-20`, `T-14`, `T-25`..`T-29`, que passaram por M7 sem varrer. O conserto que fecha o buraco não é apagar a linha, é o TESTE: uma varredura de `src/ui/` inteiro por frases mortas, mais um teste do dado que as desmente — e esse par tem de nascer junto com a frase, no dia em que a lacuna é declarada, não no dia em que alguém repara.
- 2026-08-21 — **Num card cujo portão é uma CONTAGEM, não pendure observação sobre "aparece tal frase?": contar convida a enumerar só o que se conta, e o silêncio sobre o resto não é negativa.** Em `A-33` o número era "quantos botões de modo aparecem" e a observação perguntava se o rodapé ainda trazia a frase de `QA-21`. O dono listou os botões e o interruptor, e não citou a frase — mas ela é um `<p>`, não um botão, então a resposta não diz nada sobre ela. Observação tem de ser de MESMA natureza que o número (contar com contar, ver com ver), ou vir em card separado.
- 2026-08-21 — **Configuração NÃO declarada é decisão tomada por outro: o padrão de 5.000 ms do Vitest era um portão que ninguém escolheu, e ele reprovava por CPU disputada, não por comportamento.** `QA-32` viveu com a suíte inteira dependendo de a máquina do dono estar ociosa: as provas que só se verificam por repetição (teto de 70% de M3, 20.000 sorteios de ID em M6, 1.000 cobranças de M2) passam em 1,5 s livres e estouram com build ao lado. A regra que sobra vale para todo teto herdado: **se o número decide se a entrega passa, ele tem de estar escrito no repositório, com o porquê ao lado** — e o porquê é o par de bordas, não o gosto (aqui: ~13× o pior caso medido, e ainda uma ordem de grandeza abaixo de um travamento real). Corolário barato: teto novo se prova com uma sonda descartável no meio da faixa (um teste de 7 s entre os 5 s antigos e os 20 s novos), rodada e apagada — sem isso você confia que o arquivo de configuração foi lido, e `root`/`defineConfig` errado faz um `test:` inteiro ser ignorado em silêncio.
- 2026-08-21 — **Prompt com placeholder por preencher (`<TOM ESCOLHIDO>`) é a regra 5 chegando pela porta da frente: o valor que falta é justamente o dado curado, e escolher por ele seria inventá-lo.** O prompt do `amarelo` trazia portão, arquivos e IDs — tudo menos o número. A saída não é parar de mãos vazias nem chutar: é fazer **toda** a medição que não depende da escolha e devolver a pergunta já resolvida em tudo o mais. Aqui a varredura mostrou que **1.836 de 1.872** combinações de amarelo passam nos três testes — ou seja, o portão não estava escolhendo nada, o gosto do dono estava — e o que a medição achou de útil foi um efeito colateral que ele não veria: `corDaListra` vira em `l >= 55`, então um tom mais escuro trocaria a listra das 4 seleções amarelas de escura para clara. **Recomendação sem esse achado é catálogo; com ele é escolha.**
- 2026-08-21 — **"O sandbox não compõe quadros" não era a trava das telas de M7: a trava era um `await` num tween de Phaser.** Sete cards declararam a mesma lacuna e mandaram o número para o dono. O que segura a cobrança é `await cena.animar(...)`, e Phaser só anda em `requestAnimationFrame`, que não dispara com a pane escondida — trocá-lo por `setTimeout` ANTES de a cena montar faz a disputa inteira andar, e o portão de `T-32` (três revanches, zerar, torneio sem contador) virou medida em vez de pedido. Antes de herdar uma lacuna declarada, pergunte QUAL linha ela trava: "o ambiente não dá" costuma ser uma chamada específica, e chamada específica tem contorno.
- 2026-08-21 — **Portão de campo que pede UM número fica mais forte quando o número tem consistência interna verificável: a resposta passa a se conferir sozinha, de graça.** `A-35` pediu só o "em N partidas", e o dono devolveu a linha inteira — "Egito 1 × 2 Coreia do Sul — em 3 partidas". O `1 × 2` soma exatamente `3`, e placar da série e contagem de partidas saem do mesmo ponto, então a coerência entre os dois é uma SEGUNDA medida que ninguém pediu. Ao escrever o portão, prefira a grandeza que carrega uma redundância aritmética com o que já está na tela — o custo para o dono é o mesmo (um número), e o que volta é um número mais uma checagem. Corolário: peça o inteiro, mas não proíba a frase em volta; foi o contexto dela (as seleções escolhidas por ELE, não pelo sandbox) que mostrou que a medida não dependia do caso que o sandbox tinha montado.
- 2026-08-21 — **Antes de escolher SVG embutido para um ícone, cheque as varreduras da própria suíte: `data:image/svg+xml` carrega um `xmlns` de `w3.org`, e portão de privacidade que proíbe endereço externo não distingue desenho de rastreador.** Em `T-30` o SVG era a saída óbvia para uma luva de goleiro; ele reprovaria o teste de M9 que varre `estilo.css` por `//host.tld`, e a tentação seria abrir exceção na varredura — afrouxar um portão de privacidade em troca de um ícone. A luva saiu de três camadas de `background` com `currentColor`, por +144 B. Lição geral: quando o desenho não couber na restrição, redesenhe o desenho, nunca a restrição — e descubra isso ANTES de codar, lendo o que a suíte já cobra do arquivo que você vai tocar.
- 2026-08-21 — **Card de campo devolve mais do que o portão pede, e o excedente costuma valer mais que o inteiro: `A-36` pediu quantos dedos e o dono respondeu `4` **e** que não dá para ver a bola. O `4` fecha uma tarefa; o resto abriu um defeito de duas tarefas atrás.** O portão media a SILHUETA (a variável que `T-30` moveu) e a silhueta passou — mas o dono estava olhando a tela inteira, não a variável, e viu o símbolo branco `#eef2f9` sumindo nas marcações `0xf4faf5` do campo, que já eram assim desde `T-28`. A lição não é "peça mais": é que **o portão isola a variável de propósito, e por isso é cego para o que estava errado antes dela** — quando a resposta vier com um extra, ele é achado (`QA-NN`), nunca ruído, e não reprova o card que passou. Corolário de honestidade: registre o que ele DISSE, não o que a pergunta esperava ouvir — em `A-36` o "os ícones estão certos" aprovou as formas, mas não é resposta à pergunta literal da leitura sem rótulo, e tratar como se fosse seria inventar medição.
- 2026-08-21 — **Dar `color` próprio a um `::before` que pintava com `currentColor` apaga, em silêncio, todo comportamento que dependia de a cor DESCER por herança — e nenhum teste existente reprova, porque a regra que sumiu nunca esteve escrita.** Em `T-33` o `[disabled]` de `.zona` apagava o símbolo de graça: `color: transparent` no botão descia para o pseudo-elemento. Bastou uma declaração de cor nas regras de papel para a herança perder para a declaração, e o símbolo passaria a sobreviver ao botão travado com a suíte verde. A regra geral: **comportamento que existe por herança é comportamento não testado** — ao cravar o valor herdado em qualquer nível abaixo, escreva a regra explícita E o teste no mesmo delta, e faça a regra vencer por ESPECIFICIDADE (`.zona[disabled][data-papel]::before`, 3 contra 2), nunca por ordem na folha, que a próxima edição reordena sem perceber. Corolário de desenho, do mesmo card: para silhueta feita de camadas de `background` (a luva de `T-30`), `box-shadow` contorna a CAIXA e `filter: drop-shadow()` segue o alfa composto — só o segundo enxerga o vão entre os dedos.
- 2026-08-21 — **Fechar um `QA-NN` com o relato do campo REPROVA o portão de `D-83`: a linha da tabela tem teto de 400 caracteres, e resultado de campo é a evidência mais longa que um achado recebe.** Em `QA-36` a célula de status já vinha com o defeito medido; acrescentar "o que `T-33` fez" e "o que `A-37` devolveu" levou a linha a 459 e o `check.py` barrou depois de tudo escrito. A ordem certa é a inversa da intuitiva: **o relato do campo nasce na nota `e_qa/<slug>.md`, e a linha só encolhe para o ponteiro + data + o inteiro** ("`T-33` corrigiu; `A-37` devolveu **3**"). Vale para todo achado que já tem nota — se ela existe, a linha não é onde a história mora, e reescrever a célula de fechamento é o momento em que isso é mais fácil de esquecer.
- 2026-08-21 — **Antes de escrever card que manda o dono PUBLICAR, leia o workflow e o reflog: em repositório com `on: push` para o Pages, a publicação já aconteceu, e o card certo é de CONFIRMAÇÃO.** `A-38` nasceu "publicar o `dist/` de `HEAD`" e `.github/workflows/pages.yml` publica a cada push em `main`, com `git reflog show origin/main` registrando um push por commit até `HEAD`. Mandar `npm run build` seria pedir ao dono um passo que a infraestrutura já deu, e pior: o build local **não** responde à pergunta do card, que é sobre o endereço público. Corolário que vale para qualquer portão de campo: **ancestralidade de commit é medida de graça** — um inteiro que prova o ÚLTIMO commit prova todos os anteriores no mesmo bundle, e é por isso que a lacuna de `A-34` (amarelo em commit POSTERIOR ao medido) não se repete aqui.
- 2026-08-21 — **"O ar está em dia até `<hash>`" envelhece entre o card e a resposta do campo: prove com `git log --oneline -- src/`, não com o `HEAD` que o card anotou.** `A-38` foi escrita dizendo que o último push era `c748e9c` = `HEAD`; quando o dono respondeu, `HEAD` já era `f3f4b7d`, e um leitor apressado concluiria que o ar estava um commit atrás. Os dois commits do meio mexem só em documento — o último commit que toca `src/` continua sendo `ae03a5e`, e é ELE que o número do campo confirma. A regra: **o estado do ar se mede pelo último commit que produz bundle, não pelo topo do log** — em repositório onde documentação e código dividem a mesma branch, o `HEAD` anda sozinho e a diferença é sempre falso negativo. Corolário de sessão de registro de campo: o prompt pode AFIRMAR que o dono devolveu o número sem trazê-lo; sem o valor na mão não há o que registrar, e perguntar custa um turno, enquanto supor custa o registro inteiro (regra 5 do CLAUDE.md).
- 2026-08-21 — **"Não abra 2º tipo de payload" (`D-79`) é regra sobre o que o fio JÁ dá, não sobre o fio: quando o dado não existe no aparelho em forma nenhuma, o precedente não transfere e citá-lo seria usar rejeição antiga para matar decisão nova.** Em `D-90` a tentação era herdar o veredito de `D-79`/`D-32` e procurar o discriminador de graça que `D-80` achou. Não havia: o código de país do outro lado nasce **depois** do link, e o link é de mão única. A regra geral: **antes de reaproveitar uma rejeição, releia o ARGUMENTO dela, não o veredito** — `D-79` morreu por comprar o que era grátis, e "grátis" é uma medição do estado de hoje, não uma propriedade do desenho. Corolário que salvou a porta: quando o payload TEM de crescer, o que se defende já não é o número de tipos, é o número de **métodos** — `Move` **ou** `Pick` pela mesma `onMove` mantém os 4 de `D-13`, e um `onPick` teria comprado o 5º que `D-39` e `D-73` recusaram duas vezes.
