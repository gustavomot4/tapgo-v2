---
tags: [backlog]
status: atual
---
# BACKLOG.md — quadro de tarefas (fonte única)

> **Só existe UM backlog: este.** Estado numérico mora no [[a_context_source|CONTEXT]]; aqui, só tarefas.
> Todo card com portão: como se sabe que terminou.

## Ações do dono (máquina real)
- [x] A-32 — **as camisas na cor nacional, em campo** (`T-29`/`D-88`) · **✅ DESTRAVADA em 2026-08-21: `T-29` está no ar, pode responder** · **Por que é do dono, e a causa agora está medida:** no sandbox `document.visibilityState` é `hidden` mesmo com a aba fronteada, então `requestAnimationFrame` não dispara, Phaser não renderiza e **não existe pixel a ler** — o campo em quadros é olho humano, e não por falta de tentativa · **Por que é do dono:** o sandbox não compõe quadros — a pane do navegador fica escondida, e nenhuma tela de M7 tem teste (`vitest` roda em Node sem DOM). Se duas camisas de vermelhos diferentes são **distinguíveis a 18 pixels em 360x640** é olho humano, e mais nada · **Procedimento:** com `T-29` no ar, começar uma disputa **entre duas seleções da mesma cor** — Espanha × Portugal é o pior caso da tabela (10° de distância), e é ele que interessa · **Portão — UM número, e ele vem SOZINHO (a lição de `A-31`):** primeiro a pergunta do número, em mensagem separada; as observações só depois, nunca junto — em `A-31` as observações curtas engoliram o número, e ele só apareceu quando foi pedido sozinho · **A pergunta:** **quantas cores de camisa diferentes você vê em campo?** Responder com o inteiro. **2** é a tabela funcionando com os dois lados distinguíveis; **1** é as duas camisas terem saído iguais, e aí o card reprova · **Duas observações a mais, e só duas, cada uma em uma palavra ou frase curta, e SÓ depois do número:** (a) a cor de cada camisa bate com a seleção, ou alguma saiu de cor errada? (b) numa disputa de seleção verde (México, Senegal, Irã, Nigéria, Argélia), o boneco some no gramado? · **O que reprova:** uma cor só em campo; camisa em cor que não é a da seleção; ou boneco verde indistinguível do gramado · **Trava `T-29`:** sem este número ela fica SEM CAMPO, como `T-24`..`T-28` ficaram até `A-27`..`A-31` · **✔ FECHADA em 2026-08-21 — o dono conferiu no aparelho e o card PASSOU.** Relato: *"verifiquei corretamente esses dois times e deu certo, 1 time fica com a camisa listrada e o outro com ela toda vermelha; testei em todas seleções que tem a cor vermelha predominante"* · **O portão está cumprido, e por mais do que ele pedia:** o card mandava conferir Espanha × Croácia, e o dono varreu o **grupo vermelho inteiro** — as 12 seleções que dividem a mesma cor, que é exatamente onde `camisasDaDisputa` trabalha. A garantia que a suíte cobra sobre as 32x32 tem agora correspondência em quadros de verdade, que é o que o sandbox não produz (`visibilityState` é `hidden`, `requestAnimationFrame` não dispara, Phaser não renderiza) · **A resposta expôs um defeito do MEU portão, e ele é a lição deste card:** a pergunta foi *"quantas CORES de camisa diferentes você vê?"*, com **2** declarado como aprovação e **1** como reprovação. Mas `D-88` decidiu justamente o contrário: na saída (B) as duas camisas ficam na **mesma cor** e quem separa é o padrão. A resposta literalmente correta à minha pergunta era **1**, e o meu próprio card a teria reprovado. O dono passou por cima da pergunta e descreveu o que via — e a descrição provou o que o número teria negado · **Não é a lição de `A-31` de novo:** lá o número era pedido junto das observações e sumia; aqui ele veio sozinho e ainda assim estava errado, porque a pergunta media a variável ERRADA. Pedir um inteiro só ajuda se o inteiro for observável do mecanismo que a decisão criou — e `D-88` tinha acabado de trocar cor por padrão como canal · **O que continua sem resposta, e é pouco:** as duas observações curtas que este card previa — (a) a cor de cada camisa bate com a seleção e (b) o boneco de seleção verde some no gramado. A (b) é a única medida desta entrega que não tem confirmação humana: no sandbox ela é `72,4` de distância RGB contra as 4 faixas, com teste, mas distância medida não é olho
- [x] A-31 — **a bandeira de tela cheia de `T-28`, no aparelho** (`P-8`/`P-6`(a)/`D-87`) · **Por que é do dono:** o sandbox mede geometria e estilo computado, nunca quadros — a pane do navegador fica escondida e não compõe, e o quadro final só saiu fixando `currentTime`. Se a bandeira grande AGRADA, e se ela não atrapalha quem já ia tocar, é olho humano; e o caso que o guarda de `D-85` existe para cobrir (`online`, `desenhar()` chamado sem novidade) precisa de dois aparelhos · **Procedimento:** começar uma disputa contra o computador e olhar a tela ANTES do primeiro toque · **Portão — UM número, não três sins (a forma de `A-30`, que funcionou):** **quantas vezes a bandeira grande aparece em tela cheia, do começo da disputa até o seu primeiro toque?** Responder com o inteiro. **1** é a sobreposição funcionando; **0** é ela não tendo nascido; **2 ou mais** é o guarda de `D-85` furado, e aí o card reprova · **Duas observações a mais, e só duas, cada uma em uma palavra ou frase curta:** (a) dá para tocar numa zona ENQUANTO a bandeira grande está na tela, e o toque vale? (b) nos botões de chutar/defender aparece um símbolo (triângulo cheio ou arco vazado) além da cor da borda? · **O que reprova:** bandeira que aparece mais de uma vez antes do 1º toque; toque perdido enquanto ela está na tela; ou botão sem símbolo nenhum · **Trava `T-28`:** sem este número ela fica SEM CAMPO, do mesmo jeito que `T-24`..`T-27` ficaram até `A-27`..`A-30` · **✔ FECHADA em 2026-08-21 — o dono respondeu `1`, e o card PASSOU.** As duas observações curtas vieram junto: (a) *"sim dá"* — o toque numa zona vale com a bandeira grande na tela; (b) *"sim apareceu"* — o símbolo está nos botões além da cor da borda · **A forma de `A-30` funcionou pela segunda vez, e desta vez com uma emenda que vale registrar:** o dono respondeu (a) e (b) primeiro e fechou com *"aparentemente tudo correto aqui"* — a confirmação global de `A-25`..`A-29` de volta, e sem o número. O número veio quando foi pedido **sozinho**, em pergunta separada das observações. A lição não é "peça UM número", é **"peça o número separado das observações"**: as duas observações curtas competem com ele e o engolem, porque são mais fáceis de responder · **Com ela `T-28` tem campo:** o guarda de `D-85` segurou em quadros de verdade — a sobreposição nasce UMA vez antes do 1º toque · **O que continua sem número, e agora é lacuna menor:** o caso do `online` (`aoNotificacaoDeRede()` chamando `desenhar()` sem novidade), que pede dois aparelhos — o `cpu` conferido aqui chama `desenhar()` uma vez só, então o `1` prova a sobreposição, não prova o guarda sob repetição. Fica coberto pela varredura de fonte e por `D-85`, que já mediu o mesmo guarda
- [ ] A-30 — **as duas colunas do desktop, no monitor de verdade** (`T-27`/`D-86`) · **Por que é do dono:** o sandbox mediu geometria e estilo computado, e nada mais — a pane do navegador fica escondida e não compõe quadros, então **não houve captura de tela nenhuma** nesta sessão. Se duas colunas de 485px ficam BONITAS, e se a comparação entre as duas seleções realmente se lê melhor assim, é olho humano; e a direção foi escolhida pelo dono, então a conferência também é dele · **Procedimento:** abrir o jogo no navegador do computador, com a janela MAXIMIZADA, e ir em "Contra o computador" · **Portão — UM número, não quatro sins (a lição de `A-25`..`A-29`):** na altura da primeira linha de cartões, **quantos cartões de seleção aparecem lado a lado atravessando a tela inteira?** Responder com o inteiro. **4** é o layout novo funcionando; **2** é o CSS de `D-86` não tendo pegado, e aí o card reprova · **Duas observações a mais, e só duas, cada uma em uma palavra ou frase curta:** (a) as duas grades ficam lado a lado ou empilhadas? (b) em alguma tela apareceu barra de rolagem **horizontal** — a de baixo? · **O que reprova:** 2 cartões por linha; grades empilhadas com a janela maximizada; ou qualquer barra horizontal · **Trava `T-27`:** sem este número, `T-27` fica SEM CAMPO, do mesmo jeito que `T-24`..`T-26` ficaram até `A-27`..`A-29` · **✔ FECHADA em 2026-08-21 — o dono respondeu `4`, e o card PASSOU.** Relato: *"aparecem 4 seleções, duas minhas e duas do adversário"* · **A forma funcionou, e é a primeira vez em seis cards:** pedir UM inteiro em vez de quatro sins devolveu um número verificável, e as duas observações curtas vieram junto — as grades ficaram lado a lado, sem barra horizontal · **E o número trouxe um achado que o sandbox não tinha:** *"já no TAP GO Cup aparecem apenas 2"* — a tela de começar o torneio usa a MESMA `grade()` de 32 cartões e ficou de fora de `D-86`, porque eu só alarguei o que tinha PAR a formar. Corrigido no mesmo dia (a grade sozinha vira `.solo` e passa a 4 colunas, 241px por cartão contra os 238px das emparelhadas) · **Junto veio a foto do `QA-31`**, que esta sessão tinha registrado e não consertado: o "Sair da disputa" de 144px ficou órfão no meio do cartão largo. Consertado — era `grupo` faltando na classe do bloco
- [x] A-29 — **a moeda de `T-26` EM MOVIMENTO, no aparelho** (`P-5`/`D-85`) · **Por que é do dono:** o sandbox mediu geometria e estilo computado, não quadros — a pane do navegador fica escondida e não compõe, tanto que `animation.finished` nunca resolve lá; o quadro final só foi lido fixando `currentTime`. Se o giro AGRADA, e se ele não atrapalha quem já ia tocar, é olho humano · **Procedimento:** começar uma disputa contra o computador e olhar o painel do sorteio antes do 1º toque; repetir uma vez em `local` e, se houver com quem, uma em `online` · **Portão:** (1) a bandeira gira **uma vez** e para na seleção que cobra primeiro — nunca pisca, nunca recomeça no meio (o caso do `online`, onde `desenhar()` roda várias vezes antes do toque) · (2) o giro não atrasa o jogo: dá para tocar numa zona durante ele, e o toque vale · (3) em 360x640 nada rola para o lado enquanto a moeda gira · (4) com **"reduzir movimento" LIGADO no sistema**, a bandeira aparece parada e inteira — nunca invisível, nunca meio virada · **O que reprova:** giro que recomeça sozinho; bandeira que fica de lado ou some no fim; rolagem horizontal durante o giro; ou toque perdido enquanto a moeda gira · **✔ FECHADA em 2026-08-20 — o dono conferiu no aparelho e passou.** Relato: *"tudo certo o A 28 e A 29"* · **Com ela `T-26` tem campo:** o giro existe em quadros de verdade, que é exatamente o que o sandbox não produz — a pane do navegador fica escondida e não compõe, e lá só deu para medir geometria e estilo computado · **LACUNA DECLARADA de forma, a mesma de `A-25`, `A-26` e `A-27`:** o relato veio como confirmação **global**, não item a item — os quatro itens do portão não têm observação separada. É a **quarta** vez seguida que isso acontece, e a lição disso está em `d_agent_learnings.md`: card de dono com portão de 4 itens recebe confirmação de 1 · **O que fica sem número, se um dia reaparecer:** o item (4), `prefers-reduced-motion` no sistema do dono — no sandbox ele foi medido por simulação das três declarações do bloco global, não pela preferência real; e o item (2), toque aceito durante o giro, que o código cobre por construção (a animação não toca `pointer-events` nem os botões de zona) — mas construção não é medição
- [x] A-28 — **a tabela da TAP GO Cup com pontos REAIS, no aparelho** (`T-25`/`P-2`) · **Por que é do dono:** nenhuma tela de M7 tem teste (`vitest` roda em Node sem DOM) e o sandbox não avança a tela de cobrança — a tabela só foi vista com o grupo **zerado** · **Procedimento:** começar um torneio, jogar as **3 disputas do grupo** e olhar a tabela entre elas · **Portão:** (1) a coluna "Pts" mostra sempre **3 x a coluna "V"** — 0, 3, 6 ou 9, nunca outro número · (2) a ordem das quatro linhas nunca contraria os pontos: pontos maiores em cima, sempre · (3) em 360x640 nada rola para o lado, e o nome da seleção não fica cortado nas quatro linhas · **O que reprova:** rolagem horizontal na tabela; "Pts" diferente de 3 x V; ou uma linha com mais pontos abaixo de uma com menos · **✔ FECHADA em 2026-08-20 — o dono conferiu no aparelho e passou.** Relato: *"tudo certo o A 28 e A 29"* · **Com ela `T-25` tem campo:** a tabela foi vista com pontos REAIS depois de disputas jogadas, e não só com o grupo zerado, que era a lacuna declarada do card · **LACUNA DECLARADA de forma:** confirmação **global**, não item a item — mesma forma de `A-25`/`A-26`/`A-27` · **O que fica sem número:** o item (3), o nome da seleção não cortado nas quatro linhas com o catálogo real — no sandbox o pior caso (`Costa do Marfim`) foi injetado por DOM e sobrou 0 px, mas quais quatro seleções caíram no grupo do dono ninguém anotou
- [x] A-27 — **o portão de `T-24`: dois aparelhos reais, um parado de propósito por 15 s** (`D-84`) · **Por que é do dono:** P2P e tela de M7 não existem em sandbox — mesmo motivo de `A-08`, `A-22`, `A-24`, `A-25` e `A-26` · **Procedimento:** disputa `online` de pé (anfitrião pelo MENU, convidado pelo link) e, numa cobrança, **não tocar em nada** no aparelho 2 até o número zerar · **Portão:** (1) o aparelho parado mostra o número caindo de **15 s** e, no fim, "O tempo acabou: sua escolha foi sorteada" — a cobrança acontece, ninguém fica preso · (2) as duas telas terminam a disputa com o **MESMO placar** (é o observável de `A-25`: divergência de estado não é silenciosa no resultado) · (3) o aparelho que tocou dentro do prazo nunca vê número nenhum depois de escolher · **O que reprova:** placar diferente entre as telas; número preso na tela; o aparelho parado seguindo travado depois dos 15 s; ou o contador de `T-22` (20 s, peer sumido) aparecendo junto do de `T-24` na mesma tela · **Anote o placar final dos dois lados** — `A-25` fechou com a coincidência confirmada mas sem o número, e é a lacuna que aquele card declarou · **✔ FECHADA em 2026-08-20 — o dono mediu nos dois aparelhos e passou.** Relato: *"testei e tudo já conforme o esperado, tudo está funcionando corretamente"* · **`T-24` tem campo, e com ele o portão do card está cumprido:** o relógio de 15 s corre na tela real, o estouro vira cobrança sorteada e a disputa segue nos dois aparelhos — nada disso o sandbox produz, porque nenhuma tela de M7 tem teste · **LACUNA DECLARADA de forma, a mesma de `A-26`:** o relato veio como confirmação **global**, não item a item, e **o placar final não foi anotado** — era o item 2 e o pedido explícito do card, e é a segunda vez que ele escapa (`A-25` fechou igual). Fica escrito para não se perder: se um dia aparecer divergência de placar no `online`, este card foi fechado por confirmação global e o item 2 é o que não tem número · **O que NÃO foi observado separadamente:** o número sumindo para quem tocou dentro do prazo (item 3) e a ausência do contador de 20 s de `T-22` junto do de 15 s — os dois são cobertos por construção no código (o relógio para no toque aceito e para em `'waiting'`), mas construção não é medição
- [x] A-01 — criar o repositório remoto no GitHub e apontar o local para ele · **Portão:** `origin` = gustavomot4/tapgo-v2; `main` rastreia `origin/main`; push 2a34a40 ✔
- [x] A-02 — `git init` + hook de pre-commit · **Portão:** commit 6ef539b rodou o `check.py` sozinho ✔
- [x] A-03 — ler o CONTEXT inteiro e concordar com cada linha · **Portão:** aprovado integralmente em 2026-08-06 ✔
- [x] A-04 — responder Q-03: seleções, formato do chaveamento, nome do torneio e origem das bandeiras · **Portão:** linha Q-03 do DECISIONS sai de "aberta" ✔ **respondida em 2026-08-12** — **32 seleções** pelo corte do snapshot de 20/07/2026 (`D-51`), **8 grupos de 4 + mata-mata com 3º lugar = 64 disputas** e desempate confronto direto → saldo → gols → sorteio (`D-53`), bandeiras do **flag-icons** MIT (`D-54`) e o nome **TAP GO Cup** (`D-55`). **O que era decisão acabou; o que sobrou é execução** e virou dois cards: `T-18` (os 32 códigos, com o `GB-ENG` de `D-52`) e `T-19` (os 32 SVGs com a licença). A lista curada, as duas fontes e a data de leitura estão em [[m4_lista_das_32]] — **a lista não se digita de memória**
- [x] A-05 — responder Q-04: o que acontece quando o peer some no meio da disputa · **Portão:** linha Q-04 sai de "aberta" ✔ **respondida em 2026-08-08: disputa morre SEM RESULTADO** (`D-35`) — a saída que não vira regra de disputa e não toca M2
- [x] A-09 — arquivar decisões antigas: `c_decisions.md` fechou T-13 com **5 caracteres** de folga · **Portão:** o registro volta abaixo de 9.600 (onde o `check.py` para de avisar), com as íntegras em `e_qa/decisions_archive.md` e os IDs preservados ✔ **9.347 em 2026-08-08** — 14 linhas retiradas da tabela (11 `ARQUIVADO` que nenhum arquivo vivo cita, `D-34`, `Q-06`, `QA-02`) e o ponteiro `íntegra em [[decisions_archive]]`, repetido 31 vezes, dito uma vez no cabeçalho. As três REJEITADAS (`D-06`..`D-08`) ficaram de propósito: são a lista-morta que a sessão de evolução varre, e ela nunca lê `e_qa/`. Critério e íntegras em [[decisions_archive]]
- [x] A-10 — encolher as **questões abertas** do `c_decisions.md`, que é onde sobrou a gordura: `Q-08` tem 613 caracteres, `Q-07` 406, `Q-09` 346 e `QA-04` 416, todas violando o "teto de 2 frases por linha" que o próprio arquivo declara. Mover a justificativa longa para `e_qa/<slug>.md` e deixar a pergunta · **Portão:** registro abaixo de 9.000 com toda linha `Q-NN`/`QA-NN` cabendo em 2 frases, e nenhuma questão saindo de "aberta" no caminho. **Não é arquivamento** — questão aberta não se arquiva, e por isso não coube em `A-09` ✔ **8.808 em 2026-08-08** — as justificativas longas foram para [[questoes_abertas_notas]] e as sete questões seguem abertas, com a pergunta e o prazo intactos no registro. O portão "toda linha em 2 frases" alcançou mais do que os quatro IDs nomeados: `Q-03`, `Q-05`, `Q-10`, `Q-11`, `QA-05` e `QA-06` também estouravam e entraram (o dono aprovou a extensão na abertura). Dois achados saíram daqui: `QA-07` (prazo de `Q-08` e `Q-09` vencido com a questão aberta) e a **régua ambígua** do próprio teto — a frase "2 frases por linha" vive em dois arquivos e um deles é template do kit, fora do escopo (regra 2), então a redação ficou como estava e a decisão é do dono. Critério de contagem em [[questoes_abertas_notas]]
- [x] A-11 — o `a_context_source.md` está a **17 caracteres** do teto de 4.000, que é FALHA e não aviso: a próxima sessão que escrever uma linha de estado não consegue commitar · **Portão:** CONTEXT abaixo de 3.600 (onde o `check.py` para de avisar), com o excedente virando `a_context/<tema>.md` citado no Mapa de leitura — nunca prosa comprimida ✔ **3.542 em 2026-08-08** — o bloco "Critério de aceite" saiu inteiro para [[portao_de_aceite]] (`D-37`), com gatilho largo no Mapa: "antes de fechar tarefa, declarar etapa fechada ou entregar". Os seis critérios foram conferidos um a um no destino e nenhum número de estado migrou junto — bundle, suíte e versão seguem só no CONTEXT. O `check.py` ficou **sem nenhum aviso** pela primeira vez. Folga curta, declarada: 58 caracteres até o aviso de 3.600 e 458 até a FALHA de 4.000; o próximo bloco movível é "Stack + representações" (−408, e `[[stack]]` já está no Mapa)
- [x] A-12 — arquivar decisões de novo, **e isto é urgente**: a sessão de auditoria de `QA-08` somou **1.998 caracteres** ao `c_decisions.md` (9.434 → **11.432**), que passou o aviso de 9.600 e está a **568 da FALHA de 12.000** — a próxima sessão que escrever duas linhas pode não conseguir commitar · **Portão:** registro abaixo de 9.600, com as íntegras em [[decisions_archive]] e nenhum ID sumindo nem virando prosa comprimida. **Custo declarado da auditoria** — quatro `D-NN` (um adotado, três rejeitados) e dois `QA-NN` cabiam no orçamento, mas não cabem duas vezes: `T-15` ainda vai escrever o fechamento de `QA-08` ✔ **9.405 em 2026-08-08** (−2.483; `check.py` sem nenhum aviso). O portão de 9.600 era **inalcançável pelo critério 3 de `A-09`** ("fica o que `src/` cita"): medido antes de mexer, o corte máximo honrando-o dava 10.055. `D-43` o supera — sai da tabela quem nenhum `.md` **vivo** cita, que é a régua do próprio `check.py`; `A-09` já o excedera ao retirar `D-33`, citado em `vite.config.ts`. Saíram 11 linhas (9 ponteiros só-`src` cuja íntegra já morava no arquivo desde T-08/T-09/T-13, mais `QA-01` e `QA-03`), 7 decisões perderam só a coluna de evidência, os 3 QA fechados em `T-15` viraram ponteiro, e a tabela "Gatilhos de revisão" foi para o tema que cada gatilho mede (`D-01`→[[online_p2p]], `D-02`→[[stack]]). **Ficaram de propósito:** as seis REJEITADAS (`D-06`..`D-08`, `D-39`..`D-41`), que são a lista-morta da sessão de evolução, e `D-42`, portão de `A-08`. **Folga curta, declarada:** 195 até o aviso — menos que uma linha de decisão, ver `A-13`
- [x] A-13 — **decidir a estrutura do registro**, porque o arquivamento acabou: `A-09` deu 9.347, `A-10` deu 8.808, `A-12` deu 9.405, e cada passagem durou dias. As nove linhas retiradas em `A-12` eram as últimas cuja íntegra já morava no arquivo — daqui em diante arquivar custa mover texto de verdade, e sobrou **195 de folga**. O diagnóstico é estrutural: o `c_decisions.md` carrega **três registros de ciclo de vida diferente** num orçamento só (decisão permanente, questão aberta, achado de QA), e é a seção de QA que cresce mais rápido — 2.992 caracteres em 9 achados, **6 deles abertos** e portanto não arquiváveis · **Saídas levantadas em `A-12`, nenhuma escolhida:** (a) QA vai para `a_context/d_qa.md` — exige mexer no `check.py`, que crava `DECISOES` como o único arquivo que define ID; (b) subir o teto de 12.000/9.600 — barato, mas não muda a curva; (c) aceitar a folga curta e arquivar a cada 2 sessões · **Portão:** a saída escolhida vira `D-NN` com o custo declarado (rebuild de script? migração de ID? retrabalho de documento?), e o número de folga projetado para 30 dias de trabalho. **É decisão do dono** — muda um portão do kit, não um módulo do jogo · ⚠️ **Achado em 2026-08-12 que muda a urgência: o orçamento conta espaço em branco, e o editor do dono o gera sozinho.** Ao salvar o `c_decisions.md`, o formatador de Markdown realinhou as colunas das tabelas e somou **2.048 caracteres de padding puro** — 17% do arquivo, sem uma palavra nova. O arquivo saltou de 11.470 para **13.806** e o `check.py` passou a FALHAR, num commit que só respondia uma questão. Foi preciso normalizar as células à mão para voltar a 11.756. Consequências para a escolha de `A-13`: (1) o teto de 12.000 mede **formatação**, não conteúdo, e por isso é instável entre editores; (2) qualquer saída escolhida deve normalizar o padding no `check.py` ou medir só o texto das células, senão o problema volta no próximo `Ctrl+S`; (3) **a linha de `QA-NN` que registraria este achado não coube no registro** — sobravam 244 caracteres. Ficar sem poder registrar um defeito por falta de orçamento é o argumento mais direto que `A-13` vai receber · ✔ **EXECUTADA em 2026-08-12 pela saída (a) — `D-50`.** Os 13 `QA-NN` foram para [[d_qa|QA]] com teto próprio de 8.000/6.400, e o `check.py` ganhou a normalização de padding que o card exigia de qualquer saída. Medido depois: registro **8.926/12.000**, QA **4.852/8.000**, **53 IDs, zero perdido e zero duplicado** entre os dois; `check.py` verde com um aviso só (o CONTEXT a 92%). Evidência e método em [[a13_estrutura_do_registro]] — **o link que faltava e que deixava a nota órfã**. **Dois custos declarados, os dois abertos:** (1) a folga somada ficou em **6.222** contra **27.285** projetados para 30 dias, ou seja **~7 dias corridos** — a divisão adia o problema, não o resolve, e a saída (b) volta à mesa como complemento, não como alternativa; (2) o `check.py` **saiu do sha do kit** (`604fe5f3…` no `.kit-manifest`) e deixa de receber correção por upgrade — o caminho limpo é subir o patch ao `project-pipeline-kit` e re-vendorizar, que é release de kit e outro repositório
- [x] A-16 — **o registro de decisões fechou `T-14` com 3 caracteres de folga** (11.997/12.000): a próxima decisão do projeto estoura o teto e `check.py` reprova · **Por que é do dono:** o corte de `D-43` está **esgotado** — nenhum `D-NN` vivo deixou de ser citado —, então o que resta é rever o teto, e o teto é do kit, não do módulo (regra 2). `T-14` já pagou o que dava: a evidência longa de `D-67` e `D-68` foi para [[m7_tela_notas]] e as duas linhas do registro ficaram do tamanho de um ponteiro. Encolher mais passa a custar o que a decisão diz. · **Portão:** `python scripts/check.py` volta a avisar (e não a reprovar) com folga para pelo menos 3 decisões novas, sem que nenhum `D-NN` perca a linha dele ✔ **FECHADA em 2026-08-19 — `D-69`: teto 12.000 → 16.000, aviso 10.800 → 14.400.** **A escolha entre as duas saídas do card foi medida, não opinada** — o arquivamento não perdeu para o teto, ele acabou: o pool de `D-43` é **0 linha**, e afrouxar a régua (tratar a seção "Feito" do backlog, e depois `e_qa/`, como histórico) solta **uma** linha de 77 caracteres, `D-07`, que é lista-morta e `A-09`/`A-12` guardaram de propósito · **As duas saídas de arquivamento que ainda existiam rendem menos que o portão pede** (3 decisões = 975): pôr as 15 linhas `ARQUIVADO` como ponteiro puro dá **788**, e mandar as 6 REJEITADAS para arquivo próprio dá **821** — e esta segunda ainda cobraria uma linha no Mapa do CONTEXT, que tem 118 de folga · **O que autorizou re-propor a saída (b), reprovada em `A-13` com P=0,30:** o pedágio de manifesto que a matava já foi pago em `D-50` (o `.kit-manifest` guarda `604fe5f3…` contra o sha real `78d6019e…`), e a instabilidade de padding que `A-13` pôs acima dela também saiu em `D-50` · **Portão cumprido:** `check.py` sem nenhum aviso do registro, folga de **3.501** = 10,8 decisões = **3,6×** o que o card exige, e nenhum `D-NN` perdeu a linha dele — nada foi arquivado nesta sessão · **Custo declarado:** 5 constantes/strings no `check.py`, `README.md` da raiz e `e_qa/README.md` atualizados, e a linha de estado do CONTEXT passa a `/16.000` (o `ORCAMENTOS` do `check.py` é chaveado pelo teto) · **Prazo declarado:** a curva não mudou (**+466/sessão**), então o aviso volta em ~4 sessões e a parede em ~7,5; e o gargalo seguinte não é o registro, é o **CONTEXT** · método, tabelas e o que o dono roda em [[a16_teto_do_registro]]
- [x] A-14 — **passar os olhos no painel do sorteio no celular real**, nos modos `cpu` e `local` (`T-17b`, `D-49`) · **Por que é do dono:** o sandbox do agente não tem navegador — layout, altura e toque não são verificáveis lá, e `vitest` roda em Node sem DOM, então nenhuma tela de M7 é coberta por teste automatizado · **Portão:** em 360x640, (1) o painel "Sorteio: X cobra primeiro" aparece **antes** do 1º toque nos dois modos, (2) some depois dele, (3) as três zonas continuam alcançáveis sem rolar a tela, e (4) recomeçar algumas partidas mostra o painel nomeando **os dois** lados — se nomear sempre o mesmo, o sorteio não está chegando à tela · **Se falhar (3)**, a causa provável é `QA-16` somada à altura nova: a caixa de erro vazia ocupa ~30 px que ninguém pediu ✔ **FECHADA em 2026-08-19 — o dono conferiu no aparelho e os 4 itens passaram.** O painel de `D-49` aparece antes do 1º toque nos dois modos, some depois dele, as três zonas seguem alcançáveis **sem rolar** em 360x640, e recomeçar partidas nomeia os dois lados — ou seja, o sorteio de `T-17` está chegando à tela, que é a única via de prova que existe para ele (nenhuma tela de M7 tem teste; `vitest` roda em Node sem DOM) · **O item (3) passou, e isso diz algo sobre `QA-16`:** o card previa que a caixa de erro vazia (~30 px) somada à altura do painel poderia empurrar a 3ª zona para fora da dobra. Não empurrou — `QA-16` continua **aberto** como defeito, mas deixa de ser risco de layout na tela de cobrança · **O que isto NÃO fecha:** a aprovação visual de `T-20`, que o card manda acontecer "junto de `A-14`" mas é escolha de gosto e de produto (regra 6) — segue do dono, e é o que ainda prende E-5
- [x] A-17 — **jogar a TAP GO Cup até o campeão no celular real, e fechar o navegador no meio** · **Por que é do dono:** são os dois únicos itens do portão de E-5 que o sandbox não produz — `vitest` roda em Node sem DOM e nenhuma tela de M7 tem teste; `A-14` conferiu o **painel do sorteio** nos modos `cpu`/`local`, e não o torneio · **Os outros 3 itens de E-5 já estão conferidos (não repetir):** (a) **64 disputas com um campeão** em 6 sementes, contadas por instrumentação (`played`), mais restaurar-no-meio chegando à MESMA linha do tempo (`D-57`) — suíte **531/531** · (b) **procedência dos 32 SVGs** reconferida arquivo a arquivo por SHA-256 e bytes contra a tabela de [[licenciamento]] — os 32 batem, a soma dá os **278.646 B** declarados, e o texto MIT com o aviso de copyright está em `src/assets/flags/LICENSE.txt` · (c) **`grep` da lista-morta em 0** fora de `src/tests/`, com `NOME_TORNEIO` (`src/ui/rotulos.ts:176`) como fonte única de **TAP GO Cup** · **Portão:** (1) em 360x640, do menu ao campeão **só por toque** — abrir a TAP GO Cup, escolher a seleção, jogar as disputas do jogador e chegar à tela do campeão, sem rolagem horizontal e sem alvo que não responda · (2) **fechar e reabrir o navegador** no meio (fechar mesmo, não recarregar a aba) e o torneio continuar de onde parou, com a mesma seleção, o mesmo grupo e a mesma tabela · **Se (2) falhar, o suspeito não é M8** — o retrato é cobrado por teste — é a gravação de M7 no `localStorage`, ou o navegador limpando a chave ao fechar · **Enquanto este card estiver aberto, E-5 não fecha, e E-6 tampouco** ✔ **FECHADA em 2026-08-19 — o dono jogou a TAP GO Cup no aparelho e os dois itens passaram.** (1) Do menu ao campeão **só por toque**, sem alvo que não respondesse · (2) fechar e reabrir continua de onde parou · **Com E-5 fechada, o portão da etapa deixa de ser hipótese: os cinco itens têm prova, três dela reproduzível no sandbox e duas no aparelho** · **O achado da ida, e ele não é defeito (`D-70`):** no **Chrome do desktop**, fechar o navegador inteiro perdia o torneio — fechar só a **aba** guardava, e o **Edge** do mesmo desktop e o **Chrome do celular** guardavam nos dois casos. Edge é Chromium e usa o mesmo `localStorage`, então um defeito de gravação de M7 reprovaria os três; o que sobra é a política de dados do **perfil** daquele Chrome (limpar dados do site ao fechar as janelas), que é config de máquina e não roda no código · **Por isso não virou `QA-NN`:** achado que o registro aceitaria é defeito, e este tem causa fora da árvore com três controles passando — se um dia o Edge ou o celular também perderem, aí é QA e o suspeito já está nomeado aqui
- [x] A-06 — responder Q-05: o torneio roda também no modo online? · **Portão:** linha Q-05 sai de "aberta"; sem isso o desenho de M8 é suposição ✔ **respondida em 2026-08-12: só `cpu` e `local`** (`D-56`) — a sala de 8 fica adiada, e desceu para Ideias. O efeito no plano é o que a questão prometia: o chaveamento **não** vira estado compartilhado entre aparelhos e a camada 3 não muda. **Destrava T-12**
- [x] A-18 — **abrir `https://gustavomot4.github.io/tapgo-v2/` no celular e jogar uma disputa e uma TAP GO Cup pela página PUBLICADA** · **Por que é do dono:** a última conferência da página no ar é `A-07`, de 2026-08-07, sobre o esqueleto de 4.599 B — depois dela entraram os 32 SVGs, o chunk do Phaser por `import()`, os 3 WAVs e as 3 telas do torneio, e nada disso foi visto servido pelo Pages. É a falha que o PLANO nomeia em M9: com `base` errado o asset dá **404 só em produção**, verde no `npm run dev` · **Portão:** (1) o console do celular sem nenhum 404 ao abrir o menu, ao entrar na cobrança (é aí que o Phaser e os WAVs carregam) e ao abrir a TAP GO Cup · (2) as bandeiras aparecendo nas telas de seleção e de grupo — bandeira quebrada é o 404 mais visível · (3) a versão no ar sendo a do commit atual · **Enquanto este card estiver aberto, E-6 não fecha:** entregar é a página no ar funcionando, e o `dist/` local não prova isso ✔ **FECHADA em 2026-08-19 — o dono declarou que conferiu pela página publicada, no telefone, e está tudo correto e atualizado.** A conferência é a mesma passagem de `A-17`: jogar a TAP GO Cup até o campeão **exige** a tela de cobrança, que é onde o chunk do Phaser e os 3 WAVs são buscados, e exige as telas de seleção e de grupo, que é onde as 32 bandeiras aparecem — ou seja, os três sinais do portão (bandeira, cobrança, versão no ar) foram exercitados pelo próprio jogo, não por inspeção · **O que isto fecha, e é o que faltava desde `A-07`:** o `base` `/tapgo-v2/` continua certo para as URLs que nasceram DEPOIS do esqueleto — asset de `import.meta.glob` com `?no-inline` e chunk de `import()` dinâmico —, e o Actions publicou o commit atual · **Registro do que é a prova:** a declaração do dono sobre o aparelho dele, que é o que [[stack]] define como prova; nenhum log de console foi lido, e o portão original pedia um que o Chrome do Android não abre — os sinais visuais o substituem e cobrem o mesmo 404
- [x] A-19 — **subir a versão para `2.0.0` e republicar** · **Por que é do dono:** o bump sozinho é uma linha, mas ele só vale acompanhado de `npm run build` e `git push` — o Actions é que leva a versão ao ar, e árvore em `2.0.0` com página no ar em `2.0.0-dev` é o defeito de estado divergente que a Fase 6 existe para pegar · **O que muda:** `package.json` (`2.0.0-dev` → `2.0.0`) e a linha **Versão** do CONTEXT, nesta ordem · **Portão:** `npx tsc --noEmit && npm run build` verdes, `push` na `main`, o Actions verde, e `https://gustavomot4.github.io/tapgo-v2/` reaberto no celular — as bandeiras aparecem, a cobrança anima com som e a TAP GO Cup está no menu (os mesmos três sinais de `A-18`) · ✔ **2026-08-19 — `typecheck`, `test` e `build` verdes na máquina do dono** (531/531 em 12 arquivos; bundle 408.094 B = 5,10%), `push` de `efd7b41` feito. **O bump ficou mais barato do que este card supunha:** a string de versão **não entra no build** — `2.0.0-dev` não aparece em byte nenhum do `dist/` —, então subir para `2.0.0` não muda a página no ar e dispensa a reconferência dos três sinais no celular. Sobra `commit` + `push`, e o Actions republica saída idêntica
- [x] A-08 — **medir a taxa de conexão** em `/tapgo-v2/medicao.html`, dois aparelhos, **rede de operadora** (Wi-Fi desligado nos dois) · **Portão:** as DUAS taxas registradas — sem TURN e na configuração que vai ao ar —, cada uma com o nº de tentativas; `Q-10` sai de "aberta" e E-4 fecha ou não pelo corte de 70%. **Sem este número, T-11 não fecha E-4** — o sandbox do agente não o produz [Fonte: a_context/b_plan.md → M6] · ✅ **DESBLOQUEADA em 2026-08-08 por `T-15`** (`bd68d0f`, `31b39d9`) — era: o anfitrião abria sala sorteada em vez da rotacionada, toda tentativa expirava e a medição daria 0%, **confirmado em campo** com 0% no Wi-Fi (controle) e 0% em dados móveis, nenhum dos dois medição, ver [[m6_transporte_notas]]. **Rede já escolhida:** Claro nos dois, 5G nos dois · **Antes de cada toque, confira que os DOIS aparelhos mostram a mesma linha `#índice · 6 chars · modo`** (guarda de `QA-09`): se divergirem, estão em salas diferentes e os 20 s entram como falha de rede que não houve — zere os contadores nos dois e recomece · **Portão estatístico (`D-42`, substitui o piso de 30):** pare quando o limite inferior 95% passar de 70% — **0 falhas → 9 tentativas**, 1 falha → 14, 2 → 19, 3 → 24; tabela em [[m6_transporte_notas]] · **1ª ida a campo em 2026-08-08, não fechou:** 4/4 Wi-Fi×Wi-Fi (controle, não fala de NAT), 4/4 Claro 5G×Wi-Fi (mista, a mais representativa), 5/5 Claro 5G×Claro 5G — 100% nas três, mas 5/5 só garante 54,9% e o contador "config que vai ao ar" segue **0/0**, que é o que o corte de 70% cobra · **Próxima ida: operadora DIFERENTE nos dois aparelhos** — Claro×Claro pode ser o mesmo pool de NAT e não responde aos 15-30% de CGNAT de [[online_p2p]] · `QA-12` achado nesta ida e já consertado · **`T-16` mudou o que a próxima ida entrega (`D-44`):** a tela e o resumo agora dizem o **tipo do par** de cada tentativa, então Claro×Claro deixa de ser cego — `srflx↔srflx` com IPs públicos DIFERENTES é travessia real de NAT e conta; o MESMO IP público é hairpin e não fala de CGNAT; `relay` não é P2P direto. Operadora diferente segue sendo o caso mais forte, mas **não é mais pré-requisito para a ida render número** · **2ª ida em 2026-08-08, Claro 5G nos dois — a rede passou, o instrumento não:** 12/12 sem TURN, mediana 281 ms, **77,9% de limite inferior 95%** (acima dos 70% que `D-42` pede), mas `travessia real de NAT em 0 de 12` — 11 pares não lidos e um `host↔host` impossível entre dois 5G. Causa em `QA-13` (pool da Trystero reaproveitado entre salas), corrigida; **o contador "vai ao ar" segue 0/0, então E-4 não fecha e `Q-10` continua aberta** · **3ª ida:** repetir o mesmo cenário e conferir na TELA, já na 1ª tentativa, que a linha do par não diz "par não lido" — se disser, ela agora traz o motivo, e é o motivo que vem para cá · **3ª ida feita em 2026-08-08, e ela mudou a pergunta:** a leitura funcionou (`QA-13` confirmado em campo) e mostrou `host↔host` entre dois **IPv6 globais** de prefixos diferentes — P2P direto **sem NAT nenhuma**, não rede local, que era o que o instrumento dizia (`QA-14`, corrigido). **Antes da 4ª ida, `Q-12` precisa de resposta:** se conexão IPv6 conta para o corte de 70%, a medição em dois celulares com IPv6 nunca vai exercitar o CGNAT que motivou o corte — e o número, por mais alto que fique, não fala do usuário só-IPv4. Medir mais tentativas antes disso é acumular dígito sem responder a pergunta · ✅ **`Q-12` RESPONDIDA em 2026-08-08 (`D-46`): a ida agora tem DUAS rodadas, e a que vale para o corte é a segunda**

  · **Rodada 1 — `IPv6/sem NAT` (a que você já rodou).** Deixe os aparelhos como estão. Serve para registrar que o caminho sem NAT funciona; **não** é cobrada contra os 70%. Já tem 1/1 e não precisa de mais.
  · **Rodada 2 — `IPv4/com NAT` (esta é o portão).** Nos DOIS aparelhos: *Configurações → Rede móvel → Nomes de pontos de acesso (APN) → o APN em uso → **Protocolo APN: IPv4** e **Protocolo de roaming APN: IPv4***. Salve, reselecione o APN e, se o IPv6 insistir, reinicie. **Confirme em `test-ipv6.com` que os dois dizem "no IPv6 address" ANTES de medir** — sem essa conferência, a rodada pode ir por IPv6 e você mede o caminho errado sem saber. Depois rode o critério de parada de `D-42` normalmente (0 falhas → 9 tentativas).
  · **O que a tela vai dizer, e o que cada coisa significa:** `srflx↔srflx` com IPs públicos DIFERENTES = atravessou a CGNAT, é o resultado bom · `srflx↔srflx` com o MESMO IP = hairpin, os dois saíram pelo mesmo endereço e a tentativa não fala de travessia · `host↔host` em 100.64/10 (`host-cgnat`) = os dois dentro da rede interna da operadora, também não atravessou · `relay` = só com TURN ligado, e não conta como P2P direto.
  · **Não esqueça de desfazer o APN** depois. IPv4 forçado degrada a rede do aparelho no dia a dia.
  · **Limite declarado desta rodada:** Claro×Claro com IPv4 mede "atravessar a CGNAT da Claro", não "Claro×Vivo, duas CGNATs". É menos do que o ideal e é muito mais do que zero — hoje o caminho com NAT nunca foi exercitado. Se um dia aparecer segunda operadora, é ela que fecha o resto.
  · **`Q-10` sai de graça:** passou dos 70% no contador IPv4, TURN não é necessário e E-4 fecha; não passou, TURN vira obrigatório e `Q-10` está respondida pelo número, não por opinião

  ✔ **MEDIDA em 2026-08-12 — passou.** `IPv4/com NAT`, sem TURN, APN forçado nos dois: **17/17 = 100%**, mediana 266 ms, **limite inferior 95% de 83,8%** contra o corte de 70% (`D-42` pedia 9 tentativas com 0 falhas; foram 17). **Os 17 por travessia real de NAT** — `srflx↔srflx` com IPs públicos diferentes (`187.70.x.x` × `191.56.x.x`) —, **zero** relay, **zero** hairpin, **zero** IPv6. O `sem NAT/IPv6: 0` é a prova de que o APN em IPv4 pegou nos dois aparelhos: nenhuma tentativa foi pelo caminho onde a NAT não existe · **Claro nos dois** — a lacuna que sobra é só o caso entre operadoras DIFERENTES · **`Q-10` respondida → `D-47`** (TURN fora de escopo) e **`QA-13` fechado** (17 pares lidos, zero "par não lido") · **O que este número NÃO diz:** `srflx` público não prova ausência de CGNAT — aparelho atrás de CGNAT também mostra endereço público, o da operadora. O provado é que a NAT do caminho é **atravessável**, que é o que o portão pergunta · **E 17/17 não é 100% no mundo real:** o que a amostra sustenta é ≥ 83,8%, então até ~1 em 6 ainda pode falhar e fica com a mensagem de erro, sem relay (`D-47`)
- [x] A-07 — publicar: `Q-06` respondida (repositório público, `D-21`), Source = "GitHub Actions" · **Portão:** `https://gustavomot4.github.io/tapgo-v2/` abre com o veredito verde "asset carregado — sem 404", `base` `/tapgo-v2/` e o asset `base-probe-BWPWGS0k.svg` — o mesmo hash do build local. Conferido no celular do dono em 2026-08-07 ✔ **E-1 fechada**

- [x] A-20 — **decidir o teto dos TRÊS orçamentos, que fecharam juntos nesta sessão**: `d_qa.md` em **7.933/8.000** (67 de folga, um quinto de uma linha de achado), `c_decisions.md` em **15.690/16.000** (310, menos que uma decisão) e o [[a_context_source|CONTEXT]] em **4.000/4.000** — folga ZERO, e ele é FALHA e não aviso: a próxima sessão que escrever uma linha de estado não commita · **Por que agora:** uma sessão que responda uma questão e registre um achado — exatamente o que esta fez — já não cabe, e ficar sem poder registrar defeito por falta de orçamento é o argumento que `A-13` recebeu e que voltou · **Precedente pronto:** `D-69` (`A-16`) mediu que o pool de `D-43` é ZERO linha e subiu o teto em vez de arquivar; o `check.py` hoje sugere `D-73` como única candidata, que é a decisão mais nova e a mais citada — sugestão que o critério de `D-43` não sustenta · **Saídas:** (a) subir os dois tetos com prazo declarado em sessões, como `D-69`; (b) mover para [[decisions_archive]] os `QA-NN` **fechados** (9 dos 20), deixando ponteiro — é o corte que `d_qa.md` ainda tem e `c_decisions.md` não; (c) as duas · **Portão:** a saída vira `D-NN` com a folga projetada **em número de linhas**, não em caracteres, e nenhum ID some nem vira prosa comprimida. **É decisão do dono** — muda portão do kit, não módulo do jogo ✔ **fechada em 2026-08-19 por `D-74`: nenhum teto subiu.** Medido antes de comparar, como `D-69` pediu: o pool de `D-43` é ZERO pela **terceira** vez, mas o pool de **duplicata** — linha `ARQUIVADO` cuja íntegra já estava no arquivo, 15/15 conferidas — rende **2.229** no registro, e `A-16` não o viu porque mediu "virar ponteiro puro" (788) em vez de "sair da tabela" (1.536). Saída **(b)+(c) sem a (a)**: registro **14.229/16.000** (3,5 decisões), QA **4.292/8.000** (10,6 achados), CONTEXT **3.554/4.000** (~21 sessões) — os três abaixo do aviso pela primeira vez desde 12/08, e nenhum ID sumiu. O que sobrou é `A-21`: a **inclinação** (+638/sessão contra os +466 que `D-69` projetou), que este corte não toca. Íntegra em [[a20_tres_orcamentos]]
- [x] A-22 — **jogar uma disputa `online` pelo link, em DOIS aparelhos reais** (`T-21`) · **Por que é do dono:** conexão P2P não existe em sandbox — é o mesmo motivo de `A-08`, e é o último portão de `T-21` · **Portão:** no aparelho 1, "Jogar com um amigo" → seleções → o link aparece ANTES de qualquer espera (nada contando) · o link colado no aparelho 2 abre a tela "Você foi convidado" · com os dois na tela, tocar em conectar/entrar e a disputa começar em segundos · uma cobrança inteira ida e volta, com "Esperando o outro jogador…" aparecendo entre os dois toques · fechar o navegador do aparelho 2 no meio e o 1 dizer "O outro jogador saiu", sem placar inventado · **e o caso feio:** tocar em conectar SEM o outro lado aberto, esperar os 20 s, ver a falha honesta e "Tentar de novo" funcionar na MESMA sala
  · **✔ 1ª rodada em 2026-08-20 — conectou e jogou a disputa inteira.** As duas listas de cobranças saíram idênticas e o placar bateu. Dois achados: seleções divergentes entre os aparelhos (→ `D-77`, corrigido) e o número dobrado no resumo (→ `QA-23`, corrigido). **Falta a 2ª rodada, com o link novo:** confirmar que o convidado vê o MESMO confronto, e rodar os dois casos feios do portão acima (peer que sai; conectar sem o outro lado e retentar na mesma sala)
  · **◑ 2ª rodada em 2026-08-20, com o link novo.** **Passou:** o confronto é o MESMO nos dois aparelhos (`D-77` confirmado em campo, que era o achado da 1ª rodada) · o link aparece antes de qualquer contagem (`D-73`/`D-75` confirmados) · conectar sem o outro lado dá a falha honesta com o texto certo ("Não foi possível conectar com o outro aparelho / Confira se a outra pessoa abriu o link e toque em Tentar de novo"). **O peer que sai PASSA como o portão o escreveu, e reprova no que ele não previu:** medido em campo, esperar os 20 s **sem** reabrir o link entrega "O outro jogador saiu / A disputa terminou sem resultado", sem placar inventado — `D-35` chega à tela. O que quebra é **reabrir o link dentro dos 20 s**: `onPeerJoin` guarda só `'closed'`/`'failed'`, então quem volta é aceito com sessão zerada contra um lado no meio da disputa. Virou **`QA-25`** (ALTO — é divergência de estado, não atraso de mensagem), não consertado de carona (regra 4) · **✔ FECHADA em 2026-08-20 — o último item passou:** depois da falha honesta, "Tentar de novo" reabre com o **mesmo `roomId`**, que é a 2ª metade do portão de `D-75` e a condição para o link já enviado sobreviver ao primeiro timeout · **O que isto fecha:** `T-21` tinha o portão de campo como último pendente, e todos os 4 itens do card foram vistos em aparelho real — o `online` conecta, joga a disputa inteira, mostra o mesmo confronto nos dois lados (`D-77`), termina sem resultado quando o peer some (`D-35`) e falha honestamente sem o outro lado (`D-47`) · **O que isto NÃO fecha:** devolver ao Objetivo do CONTEXT a promessa "por link de convite" que `D-72` tirou. A condição literal de `D-72` (`Q-11` com `D-NN`) está cumprida por `D-73`, mas `QA-25` está ABERTO e é divergência de estado — publicar a promessa com ele em aberto é decisão de produto, logo `D-NN` do dono (regra 6)

- [x] T-23 — **fechar `QA-26` pela porta M5: par espelhado vira falha honesta** (`D-81`) · **Módulo:** M5 (`src/session/index.ts`) e só ele · **Etapa:** pós-E-6 · **Skill:** backend-domain · **A decisão já está tomada em `D-81`** (auditoria de 2026-08-20): `aoMove` trata `m.side === localSide` como pareamento espelhado — marca `abandonada`, sintetiza `'failed'` e chama `canal.close()`, a forma exata de `D-80`, que `A-24` já confirmou em campo · **Quatro saídas morreram com número, e estão em [[qa26_lado_do_convite]]:** lado no link `&l=` (cobre **0 dos 3** casos), M7 lembrar a sala (pede a 3ª exceção nominal ao portão de privacidade de M9, o preço de `D-71`), perguntar na tela (+1 toque em **100%** dos convites, contra o portão de 2 toques de `D-49`) e negociar o lado no fio por `peerId` — esta **reprovada por falta de dado**, não por mérito, e é a única que faria os 3 casos funcionarem · **Portão (escrito ANTES do experimento, na nota):** (1) suíte de **563/563** para 563+N, com os 5 testes de `D-80` e os 4 da fila verdes · (2) falseamento: sob mutação `===` → `!==` o teste novo **reprova**, e `Move` legítimo (`side === remoteSide`) não dispara a guarda · (3) `tsc` limpo, bundle relido de `dist/` com **+≤150 B** e zero asset · (4) zero byte em `src/net` e `src/ui` — **uma mudança por vez**, sem carona de `A-21`, `QA-27`, `QA-28` nem `T-22` · (5) campo em dois aparelhos → `A-25` · **O falsificador do card foi MEDIDO em campo em 2026-08-20, e a porta passou:** o dono viu os dois aparelhos na mesma seleção, os dois no papel de **defesa**, e o toque levando a "esperando o adversário" — ou seja, o `Move` **sai**, que é a pré-condição da guarda. As 3 previsões do modelo bateram; íntegra em [[qa26_lado_do_convite]] · **✔ FECHADA em 2026-08-20 — o código entrou, o portão de sandbox passou inteiro.** Delta em **um** arquivo de produção (`src/session/index.ts`) + o teste: a guarda de lado deixa o `=== localSide` passar, e **depois** das guardas de forma e de **fase** ele marca `abandonada`, sintetiza `'failed'` e chama `canal.close()`. Suíte **563 → 569/569**, `tsc` limpo, bundle **415.713 → 415.817 B (+104 B, teto de 150)**, zero asset, **zero byte em `src/net` e `src/ui`**. **Três falseamentos rodados:** `===` → `!==` reprova **17** testes (os 5 de `D-81` entre eles); a guarda inalcançável reprova **4**; a guarda **acima** da de fase reprova o teste que existe só para essa ordem — e o par são completa **5 cobranças com zero descarte**. · **ACHADO que o dono precisa ler ANTES de `A-25`:** o item 1 do portão pede os **dois** aparelhos em `D-35` em ≤2 s, e a porta D não entrega isso dos dois lados — quem **recebe** a jogada espelhada cai no tique, quem **tocou** fica em "Esperando o outro jogador…" por **até 20 s** (o `close()` vira `onPeerLeave` → `'waiting'` → relógio, exatamente o caminho que `A-24` mediu e **aceitou**). Encurtar exigiria `src/net` (proibido pelo item 4) ou matar a queda-e-volta de ~5 s (`D-78`). Releitura proposta em [[qa26_lado_do_convite]]; o `D-NN` dela **não cabe** no registro (`A-21`)
- [x] A-25 — **abrir o MESMO link nos dois aparelhos e tocar numa zona** (`D-81`, item 5 do portão de `T-23`) · **Por que é do dono:** P2P não existe em sandbox — mesmo motivo de `A-08`, `A-22` e `A-24` · **Portão:** com os dois aparelhos no mesmo link, ao **primeiro toque de zona** em qualquer um deles, os **dois** chegam à mensagem de `D-35` ("O outro jogador saiu / A disputa terminou sem resultado") em **≤2 s** — um lado só sair da trava **reprova**, o conserto seria de meia tela · e o par são (anfitrião pelo menu + convidado pelo link) completa **5 cobranças** sem descarte · **Mede também o falsificador, e isto vale sozinho:** anotar **se tocou numa zona antes de travar** — é o que separa a porta M5 da porta do `peerId`, e custa 30 segundos · **Destrava:** devolver ao Objetivo a promessa "por link de convite" que `D-72` tirou, com a frase honesta junto (o link é de uso único e do convidado) — `D-NN` do dono · **◑ METADE 1 MEDIDA EM CAMPO E PASSOU — 2026-08-20.** Os dois pelo mesmo link, "oponente encontrado", um toca numa zona: o **outro aparelho, sem ter tocado em nada**, mostra a mensagem de `D-35` inteira — que é `D-81` disparando, porque antes de `T-23` ele não tinha caminho nenhum até essa tela aqui. O que tocou saiu da frase "Esperando…" em **~10 s** (o código arma 20 s; estimativa a olho, e qualquer valor ≤20 s passa). **Nenhum dos dois preso, nenhum placar inventado.** · **FALTA A METADE 2, e `QA-26` não fecha sem ela:** o **par são** — anfitrião entrando pelo **MENU** (não pelo próprio link) + convidado pelo link — completando **5 cobranças** sem descarte. É o item que prova que `D-81` não matou a disputa legítima · **⚠ LER ANTES DE MEDIR (achado de `T-23`, 2026-08-20):** o "os **dois** em ≤2 s" acima está mais apertado do que a porta D promete. O mecanismo é assimétrico por construção: quem **recebe** a jogada espelhada cai em `D-35` **no tique**; quem **tocou** recebe o `close()` do outro como `onPeerLeave` → `'waiting'` e segue em "Esperando o outro jogador…" por **até 20 s** — o MESMO caminho que o portão de `A-24` escreveu e aceitou ("o aparelho 2 sai da tela travada em até 20 s"). Encurtar exigiria mexer em `src/net`, que o item 4 do portão de `T-23` proíbe, ou tratar `'waiting'` como terminal, que mataria a queda-e-volta de ~5 s (`D-78`). **Cobre assim:** recebeu → na hora; tocou → em até 20 s; **reprova de verdade** é algum dos dois seguir em "Esperando…" **passados os 20 s**, ou qualquer placar aparecer. A releitura formal do item 1 é `D-NN` seu e **não cabe** no registro hoje (`A-21`) — íntegra em [[qa26_lado_do_convite]] · **✔ FECHADA em 2026-08-20 — a METADE 2 passou em campo e `QA-26` fecha.** Par são medido nos dois aparelhos: anfitrião pelo **MENU** (`Jogar com um amigo` → seleções → *"Convide quem vai jogar"* → `Já mandei o link — conectar`) e convidado **pelo link** (*"Você foi convidado"* → `Entrar na disputa`), com os dois toques de conexão com os aparelhos na mão (`D-75`). **Os 5 pontos cobrados passaram:** abertura pelo menu+link · a disputa terminou com **placar final** · o placar é o **MESMO nos dois aparelhos** · `D-35` **não** apareceu · bandeiras distintas e alternância de cobrança. **Por que o placar coincidente substitui o console** (que celular não tem): descarte de `Move` não é silencioso no resultado, é divergência de estado — foi o mecanismo de `QA-25`; dois placares iguais no fim de uma disputa inteira só existem sem jogada perdida · **Lacuna declarada:** o relato veio como confirmação dos 5 pontos, não como números livres — **o valor do placar não foi anotado**, só a coincidência, que é o que este portão cobra · **Com as duas metades, o item 5 do portão de `T-23` (campo em dois aparelhos) está cumprido e `D-81` está provado nos dois lados: mata o par espelhado, não mata a disputa legítima** · **O que NÃO fecha:** devolver "por link de convite" ao Objetivo — a condição de `D-72` está cumprida, mas a promessa volta com a frase honesta junto (o link é de uso único e do convidado) e isso é `D-NN` do dono, que não cabe no registro hoje (`A-21`) · a porta C (`peerId`) segue viva e sem dado — é a única que faria as 3 aberturas degeneradas **funcionarem** em vez de falharem com honestidade · íntegra em [[qa26_lado_do_convite]]
- [x] A-26 — **ver o contador de `T-22` correndo, em DOIS aparelhos reais** · **Por que é do dono:** peer que some no meio da disputa não existe em sandbox — mesmo motivo de `A-08`, `A-22` e `A-24` · **Procedimento:** disputa `online` de pé (anfitrião pelo MENU, convidado pelo link), e então **modo avião no aparelho 2** · **Portão:** (1) o aparelho 1 mostra "O outro jogador parou de responder." e, abaixo, o número **caindo de 20 s** a cada segundo · (2) **modo avião DESLIGADO antes do fim** — o número **some** da tela e a disputa segue, sem número preso (é a queda-e-volta de ~5 s, o valor de `D-78`) · (3) deixando o prazo acabar, a frase para de contar e vem a mensagem de `D-35` · **O que reprova:** número preso na tela depois da volta do peer; contagem que reinicia sozinha; qualquer palavra sobre conexão/reconexão; ou contador aparecendo no caso de `D-81` (dois aparelhos no MESMO link), que não espera relógio nenhum · **Pode rodar na MESMA rodada de `A-25`**, depois da metade do par são: são dois aparelhos já na mão, e a ordem importa — o par são primeiro, com a disputa limpa · **✔ FECHADA em 2026-08-20 — o dono mediu nos dois aparelhos e passou.** Relato: *"já testei aqui e deu tudo certo, apareceu a contagem e tudo"* · **Item 1 confirmado com palavra do dono sobre o observável:** a contagem APARECE na tela do aparelho 1 quando o peer some — que é o defeito de `A-24` fechado, o silêncio de "Esperando o outro jogador…" virando número · **Itens 2 e 3 cobertos pelo "deu tudo certo", e isto é LACUNA DECLARADA de forma:** o relato veio como confirmação **global**, não item a item; o número sumindo na volta do avião (item 2) e a chegada de `D-35` no fim do prazo (item 3) não foram relatados como observações separadas · **O que isso custa, escrito para não se perder:** se um dia aparecer número **preso** na tela depois da volta do peer, o suspeito já está nomeado aqui — este card foi fechado por confirmação global, e o item 2 é o único dos três que sandbox não cobre por teste puro · **O que NÃO reapareceu:** nenhuma palavra sobre conexão/rede (há teste que reprova "Reconectando…"), e nenhum contador no caso de `D-81`, que não espera relógio nenhum · **Com este card, `T-22` tem campo e o silêncio da espera deixa de existir nos dois lados: a estimativa "~10 s" a olho de `A-25` era exatamente o que faltava de número em tela**
- [x] A-21 — **cobrar por script o teto de "2 frases por linha" dos dois registros** · **Por que:** `D-74` cortou o intercepto e declarou que não toca a inclinação — medido, a curva do registro é **+638/sessão** contra os +466 que `D-69` projetou, e a causa está na linha: mediana **308**, mas as 6 mais recentes custam **509** e `D-73` sozinha custa **922**. `D-67`/`D-68` mandaram a evidência para o tema e custaram **141**/**175** — a regra funciona quando é seguida, e hoje nenhum script a cobra · **Portão (escrito antes do experimento, §8 de [[a20_tres_orcamentos]]):** checagem nova reprova linha de `D-NN`/`QA-NN` acima de **400** caracteres medidos; uma mudança por vez (nada de mexer nos tetos junto); o limiar é a média por sessão cair de +638 para **≤ +400** em **3 sessões de escrita** seguidas; nada regride — o que sai da linha entra em `e_qa/<slug>.md`, e linha viva acima de 400 **não** é reescrita (append-only) · **Custo declarado:** mexe no `check.py` (código), o docstring "14 falhas / 12 avisos" muda, e o kit segue fora do sha original (pedágio já pago em `D-50`) · **É decisão do dono** — muda portão do kit, não módulo do jogo ⚖ **AUDITADO em 2026-08-20 — reprovado COMO ESCRITO, reparado e devolvido ao dono** ([[a21_limite_por_linha]]). Três números mataram o portão do §8: (1) o experimento pede ~1.200 caracteres de escrita e o registro tem **18** — inclinação atrás de intercepto esgotado; (2) o `400` reprova **18 linhas vivas** (9 no registro, 9 no QA) que o próprio portão proíbe reescrever, e não há isenção congelada escrita em lugar nenhum; (3) o limiar `≤ +400/sessão` é inalcançável pelo mecanismo `400/linha` sempre que a sessão escreve duas linhas — retroativo às 6 sessões desde `D-74` dá **+407**, e o baseline de +638 já é velho (a curva real é **+535/sessão**, e a linha caiu de 683 para 438 sem script nenhum, contra a parede). **Reparo (§5.1):** isenção congelada por ID no `check.py` (verde na hora), limiar observável — **as próximas 10 linhas ≤ 400, nenhuma perdendo evidência** — e o custo corrigido (15→16 falhas; a frase `14 falhas · 12 avisos` está na linha 70 do README, não no docstring: `QA-29`). **Achado que o card não reivindicava:** o teto por linha é o que **refaz o pool de corte de `D-74`** — das 13 linhas vivas acima de 350, só 2 delegam a íntegra a uma nota. **Ordem:** o teto do registro (§5.2) vem **primeiro** — é a única coisa que deixa a próxima decisão existir, e o ângulo novo é que os três pools estão em zero ao mesmo tempo, medido. As duas linhas estão rascunhadas e **não couberam**: são `D-NN` do dono. **✔ EXECUTADO em 2026-08-20, no mesmo commit:** `D-82` (teto 16.000 → 20.000, aviso 18.000) e `D-83` (falha 16 no `check.py`, 400 por linha medidos). A isenção congelada ficou com **14** IDs, não 18 — a tabela do §2.2 foi medida antes do corte de `D-74` no QA e ainda contava `QA-22`/`QA-23`/`QA-25`/`QA-26`, já arquivados. **Portão:** `python scripts/check.py` verde na hora (registro **16.676/20.000**), e o teste negativo — `D-62` fora da tupla — reprova com o número. **O portão de `D-83` continua correndo:** as próximas 10 linhas de `D-NN`/`QA-NN`, todas ≤ 400 e nenhuma perdendo evidência.
- [x] A-23 — **declarar a porta que fecha `QA-25`: M5 ou M6** (ou não mexer) · **Por que é do dono:** muda a porta entre módulos e a promessa do Objetivo — regra 6, e é o que `A-22` deixou explicitamente aberto · **O que já não é palpite:** a lacuna da fila foi MEDIDA em 2026-08-20 (`escoarFila` não produz `seq=0` em disputa andada; 6 testes, os 4 de falsificação reprovam com a mutação), então `seq=0` × `kicks.length>0` discrimina sessão zerada de reconexão legítima. **Duas saídas já morreram com número:** `D-78` (matar o rearme de `D-31` — o modo avião de `A-22` se recupera sozinha hoje) e `D-79` (identificador de sessão no fio — compra o que o fio dá de graça, e é o argumento de `D-39`) · **As portas vivas, com custo completo e P(passar), em [[qa25_reentrada_na_janela]]:** **M5** (guarda + docstring em `src/session/index.ts`, zero byte em `src/net` e `src/ui`, e o preço é `LinkStatus` passar a significar vínculo da disputa e não transporte — **65%**) · **M6** (`Channel.fail()`, o 5º método: tipo honesto pagando a superfície de porta que `D-39` e `D-73` recusaram — **25%**) · **não mexer** (ALTO aberto e a promessa de `D-72` fora do Objetivo — **15%**) · **Portão (escrito ANTES do experimento):** os 5 itens comuns da nota — `'failed'` no mesmo tick no lado preso; o lado que voltou também sai da tela travada; os 4 testes de `escoarFila` e a queda-e-volta que se recupera seguem verdes; suíte verde, `tsc` limpo, bundle relido de `dist/`; e campo em dois aparelhos, reabrindo o link no meio da disputa · **uma mudança por vez** — a porta entra sozinha, sem carona de `A-21` nem de `QA-24` · **É decisão do dono**, e a implementação é OUTRA sessão · **✔ DECIDIDA em 2026-08-20 — `D-80`, porta M5.** A implementação entrou na mesma data: guarda em `aoMove`, `abandonarPorReentrada()`, `'failed'` terminal em `aoStatus` e o preço escrito na docstring de `Session.subscribe`. `src/net` e `src/ui` com **0 byte alterado**. **Portão:** (1) e (2) verdes por teste, (3) os 4 de `escoarFila` intactos e a queda-e-volta do modo avião terminando a disputa inteira sem `'failed'`, (4) suíte **563/563**, `tsc` limpo, bundle **415.713 B** lido de `dist/` (+208 B). **Falta (5): campo em dois aparelhos → `A-24`**
- [x] A-24 — **reabrir o link no meio de uma disputa `online`, em DOIS aparelhos reais** (`D-80`) · **Por que é do dono:** é o 5º item do portão de `A-23`, e conexão P2P não existe em sandbox — mesmo motivo de `A-08` e `A-22` · **Portão:** com a disputa andada (ao menos 1 cobrança fechada), **fechar o navegador** do aparelho 2 e reabrir o MESMO link **antes dos 20 s**; ao primeiro toque de zona no aparelho 2, o aparelho 1 mostra "O outro jogador saiu / A disputa terminou sem resultado" **na hora**, sem contagem; e o aparelho 2 sai da tela travada **em até 20 s**, com a mesma mensagem — **os dois**, senão o conserto é de meia tela · **O que reprova:** placar inventado em qualquer um dos lados; algum dos dois seguir em "Esperando o outro jogador…" indefinidamente; ou a queda-e-volta **sem** fechar o navegador (modo avião ~5 s, o número de `D-78`) passar a terminar a disputa — essa tem de continuar se recuperando sozinha · **Destrava:** devolver ao Objetivo do CONTEXT a promessa "por link de convite" que `D-72` tirou, que é `D-NN` do dono · **✔ FECHADA em 2026-08-20 — `D-80` confirmado em campo, os DOIS itens.** (1) O aparelho 1 virou **no instante do toque de zona** do aparelho 2, com o texto de `D-35` inteiro e sem placar inventado — o dono confirmou que tocou, e é isso que separa a medição do caminho velho de `A-22` (relógio). (2) O aparelho 2 escolheu o lado, ficou em "Esperando o outro jogador…" e recebeu a mesma mensagem segundos depois — a metade que `D-80` prometia de graça pelo `close()` → `onPeerLeave` → 20 s. **Lacuna declarada:** a queda-e-volta do modo avião (~5 s, o número de `D-78`) **não foi re-medida em campo depois de `D-80`** — hoje ela é coberta só pelo teste "portão (3)", que roda em rede perfeita. **Dois achados de campo nesta rodada:** `QA-26` (os dois aparelhos abrindo o mesmo link viram lado B) e o pedido do contador de segundos (`T-22`)
- [x] T-22 — **mostrar os segundos que faltam antes de a conexão cair** · **Módulo:** M7 (`src/ui/tela_cobranca.ts`), e só ele se a saída (a) bastar · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **Pedido do dono em 2026-08-20**, na mesma rodada de `A-24`: hoje "Esperando o outro jogador…" não distingue "ele está pensando" de "ele caiu e o relógio está correndo" · **A dificuldade real não é desenhar o número, é de ONDE ele vem:** o prazo mora em `CONNECT_TIMEOUT_MS` (`src/net/index.ts:94`) e M7 não pode importar `src/net` (portão de camada) — `src/medicao.ts` importa, mas é página de instrumento, não a tela · **Duas saídas, com o custo:** **(a)** M7 conta sozinho, começando quando `subscribe` entrega `'waiting'`, e o texto diz "até ~20 s" — zero byte fora de M7, e a aproximação fica declarada porque o relógio de M7 não é o de M6 · **(b)** M5 reexporta `CONNECT_TIMEOUT_MS` como valor, do jeito que já reexporta `newRoomId` por `D-73` — 1 linha, a porta de 4 métodos de `D-13` intacta, e o número deixa de ser copiado · **A tensão que `D-80` criou, e que precisa estar na decisão:** desde `D-80` o `'failed'` que M7 recebe pode ser **sintetizado** por M5 com o transporte de pé, então um contador rotulado "reconectando" mentiria nesse caso — o texto tem de falar da **disputa**, não da conexão · **Portão:** o contador aparece quando o peer cai no meio da disputa e some quando ele volta (a queda-e-volta do modo avião não pode deixar número preso na tela) · nenhuma promessa de prazo em tela para o caso de `D-80`, que não espera relógio nenhum · jogável por toque em 360x640 · suíte verde e `tsc` limpo · **Não inclui** mexer no prazo: `D-75` já decidiu que os 20 s não mudam · **✔ FECHADA em 2026-08-20 pela saída (b), escolhida pelo dono.** M5 reexporta `CONNECT_TIMEOUT_MS` ao lado do `newRoomId` de `D-73` — uma linha, a porta de 4 métodos de `D-13` intacta, e o prazo deixa de ser número copiado (a saída (a) teria posto o `20` como constante local em M7, que passaria a mentir sozinha no dia em que `D-75` for revisto). **O delta são 3 arquivos de produção:** o export em M5, três funções PURAS em `src/ui/rotulos.ts` (`segundosRestantes`, `textoDaEspera`, `AVISO_PEER_SUMIU` — puras porque é assim que a suíte as alcança sem DOM) e o `<p>` do contador em `tela_cobranca.ts`, com o relógio nascendo no `'waiting'` e morrendo no `'connected'`. **Os três itens do portão, um a um:** (1) o contador aparece quando o peer some e some quando ele volta — `'waiting'` liga, `'connected'` desliga, e `'waiting'` repetido NÃO reinicia o relógio, senão a tela daria à pessoa mais prazo do que existe; (2) nenhuma promessa de prazo no caso de `D-80`/`D-81` — a queda sintetizada chega por `'failed'` **sem passar por `'waiting'`**, logo nunca acende contador; (3) 360x640 por toque: nenhum controle novo, só uma linha de texto abaixo da faixa. **O texto fala da DISPUTA, nunca da conexão** — desde `D-80` o `'failed'` pode ser sintetizado com o transporte de pé, e há teste que reprova as palavras "reconectando", "sinal", "rede" e "internet" nas 4 frases. **Acessibilidade:** o contador tem `aria-live="off"` de propósito (região viva mudando a cada segundo faria o leitor recitar o número sem parar); quem anuncia UMA vez é a faixa acima. **Sandbox:** suíte **569 → 574/574**, `tsc` limpo, bundle **415.817 → 416.549 B (+732 B)**, zero asset. **Três falseamentos:** `ceil` → `floor` reprova 1 (a tela roubaria 1 s de quem espera); a frase trocada por "Reconectando…" reprova 2; a guarda do negativo removida reprova 1. **Não medido em sandbox, e por isso vira `A-26`:** o contador só existe quando um peer some no meio da disputa, e isso pede dois aparelhos

## A fazer

> O [[b_plan|PLANO]] está **congelado** (`D-13`). As tarefas abaixo saem dele e seguem a ordem das etapas E-1..E-6. O portão de cada uma é o portão do módulo, e não se repete aqui. Mudança de rumo é `D-NN` novo — não replanejamento.

- [x] T-15 — consertar `QA-08` **e** `QA-09` na página de medição · **Módulo:** `src/medicao.ts` só — **`src/net/index.ts` não muda um byte** · **Etapa:** E-4 · **Skill:** backend-bff · **Saída decidida em 2026-08-08 (`D-38`):** os DOIS lados entram por `joinRoom(idDaTentativa(base, n), ice)`. As três saídas da nota foram REJEITADAS (`D-39` porta congelada, `D-40` superfície de M6, `D-41` viés para cima) — auditoria em [[m6_transporte_notas]] · **Portão, em dois commits separados:** (1) `D-38` — teste provando que anfitrião e convidado derivam o MESMO ID para `n` = 0..29 e que `joinRoom` o aceita; `grep -c 'hostRoom(' src/medicao.ts` sai de **2 para 1** — sobra a do botão "Sortear sala", nenhuma dentro de `tentativa()`; suíte inteira verde. (2) `QA-09` — a tela mostra, nos dois aparelhos, o índice da próxima tentativa e os 6 primeiros chars do ID que ela vai usar · **Custo declarado:** `medicao.ts` não exporta nada e chama `montar()` na linha 261, então a derivação do ID precisa virar importável antes de existir teste — isso é o grosso do trabalho, não a linha do conserto. **Destrava A-08** ✔ **2026-08-08** — os dois portões verdes: `grep -c 'hostRoom(' src/medicao.ts` = **1** (a do botão; nenhuma em `tentativa()`), `src/net/index.ts` intacto, suíte **265/265** em 4 rodadas e `tsc` limpo. O custo declarado confirmou-se inteiro: a derivação virou `src/medicao_sala.ts` (módulo puro, sem DOM e sem `src/net`) porque `medicao.ts` é entrada de página — sem isso não havia por onde o teste entrar. Os dois portões de origem são cobrados por leitura do disco em `src/tests/medicao.test.ts`, e o de `grep` reprovou primeiro o **comentário** do próprio conserto, que citava `hostRoom()` por extenso. **Uma correção acima do pedido, vetável:** o rótulo de `QA-09` diz também o modo, senão dois aparelhos em contadores diferentes (`sem TURN` × `com TURN`) leriam "sincronizado" com a configuração medida diferente. Bundle 90.320 → **90.835 B**
- [x] T-16 — **instrumentar a medição**: hoje cada tentativa vale 1 bit (`ok`/`ms`) e joga fora a informação que decide `A-08` · **Módulo:** `src/medicao.ts` — **`src/net/index.ts` não muda um byte**, mesma restrição de `T-15` · **Etapa:** E-4 · **Skill:** backend-bff · **Por que agora:** o 5/5 Claro×Claro é ambíguo nos dois sentidos — se fechou por `srflx↔srflx` com IPs públicos diferentes houve travessia real de NAT; se foi hairpin no mesmo pool CGNAT, o 5/5 **não fala de CGNAT** e não vale para o corte de 70%. Hoje é indecidível pelo dado gravado, e o dono não consegue segunda operadora (todos os conhecidos usam a mesma) · **O que entra:** (1) por tentativa, o par de candidatos selecionado via `RTCPeerConnection.getStats()` — `local/remoteCandidateType` (`host`/`srflx`/`prflx`/`relay`) e os IPs públicos, na tela e no resumo copiável; (2) a contagem separada por tipo de par, porque sucesso via `relay` **não** é sucesso de P2P direto · **Portão:** uma tentativa real em cada modo mostra o tipo do par nos dois aparelhos; `src/net/index.ts` intacto; suíte verde e `tsc` limpo; o resumo copiável distingue `srflx↔srflx` de `relay` sem o dono ter de interpretar nada · **Não muda portão nenhum** — `D-42` e o corte de 70% seguem como estão; isto só torna a mesma tentativa legível. **Não** inclui sonda de NAT nem estimativa modelada: aquilo trocaria taxa medida por modelo e seria `D-NN` do dono ✔ **2026-08-08** — `src/net/index.ts` **0 byte alterado** (`git diff --stat` vazio, e um teste cobra que M6 não tenha `getStats`/`RTCPeerConnection` em código), suíte **317/317** em 3 rodadas, `tsc` limpo. A via até o `getStats()` é `D-44`: embrulhar o **global** `RTCPeerConnection` com `Proxy` sobre `construct`, já que `Channel` não devolve a conexão e a quinta porta é o que `D-39`/`D-40` recusaram. A extração e a classificação foram para `src/medicao_par.ts` (módulo puro), pelo mesmo motivo de `medicao_sala.ts` em `T-15` — `medicao.ts` é entrada de página e o teste não tem por onde entrar. **Uma decisão acima do pedido, vetável (`D-45`):** o card manda pôr os IPs no resumo colável, e o resumo vira linha de registro num repositório público — então o texto sai com 2 octetos e uma caixa liga o IP inteiro; a comparação que decide hairpin usa o endereço inteiro de qualquer forma, e a tela também o mostra inteiro. Bundle 90.929 → **96.547 B**, o acréscimo inteiro no chunk da medição. **Custo declarado:** as duas decisões levaram o registro a 9.986, passando o aviso de 9.600 — `A-13` virou o próximo obstáculo real
- [ ] T-17 — **sorteio de quem cobra primeiro** (`D-48`) · **Módulo:** M2 (a constante `FIRST`), M5 (passar o resultado) e M7 (a tela da moeda) · **Skill:** backend-domain, depois frontend-uiux para a tela · **O que entra:** hoje `src/engine/index.ts` tem `const FIRST: Side = 'A'` e `createMatch()` não recebe nada — um dos lados sempre cobra primeiro. Passa a sair de sorteio com o `Rng` de M1 (`rng.int(2)`), **nunca** do gerador nativo, senão cai o portão de M1 e o critério "roda 2x com o mesmo resultado" · **A ordem NÃO alterna** depois de sorteada, nem nas 5 regulares nem nas alternadas — é a regra da IFAB e é o que M2 já faz, então essa metade custa zero · **Custo declarado:** `createMatch()` ganha parâmetro, e isso é porta congelada por `D-13`; `D-48` é o `D-NN` que autoriza · **`online` é o caso difícil e depende de `Q-11`:** os dois aparelhos têm de sortear o MESMO lado, e hoje não compartilham semente (`SessionConfig.seed` é de quem chama). O valor que os dois já têm em comum é o `roomId` — que M5 não recebe, que é exatamente `Q-11`. Ou seja: o sorteio online **não** abre um bloqueio novo, ele pega carona no que já trava a tela de convite · **Portão:** sorteio uniforme sobre milhares de sementes (nenhum lado acima do esperado), mesma semente = mesmo primeiro cobrador, a ordem constante do início ao fim em disputa completa com alternadas, `grep` do gerador nativo em `src/` continuando em 1, e nos modos `cpu`/`local` a tela mostrando o resultado antes da 1ª cobrança

  **◐ METADE BACKEND FEITA em 2026-08-12 (`cpu`/`local`) — falta a tela.** `createMatch(first: Side)`, parâmetro **obrigatório**: um padrão `= 'A'` devolveria em silêncio o defeito que a tarefa remove, e sem padrão quem esquece é reprovado pelo `tsc`. **Quem sorteia é M5**, com `rng.int(2)` sobre o `Rng` de M1 — M2 recebe o resultado e continua sem conhecer gerador, senão `play` deixaria de ser pura. O sorteio é a **primeira** leitura do gerador da sessão, antes de qualquer `pick` da CPU, e é isso que faz `cpu` e `local` tirarem o mesmo primeiro cobrador para a mesma semente · **Portão cumprido:** uniformidade **A=1.998 · B=2.002 em 4.000 sementes** (faixa de ±5%, determinística — o teste reprova regressão, não mede estatística), mesma semente = mesmo cobrador, ordem constante em disputa completa de 12 cobranças com alternadas (e a alternada começando com o sorteado), `grep` do gerador nativo em `src/` = **1** · **Verificado de fato:** revertendo o sorteio para constante à mão, 2 testes reprovam · **Suíte 351/351** (eram 337) em 3 rodadas, `tsc` limpo, bundle 99.442 → **99.590 B** · **A não-alternância custou zero linha**, como `D-48` previa · **`online` NÃO sorteia** e há teste que reprova quem tentar: sem semente compartilhada os dois aparelhos divergiriam na 1ª cobrança — segue em `'A'` até `Q-11` · **Dois testes antigos que fixavam `'A'` foram consertados** (`session.test.ts` lia `k.dive` fixo; `net.test.ts` fixava o vencedor) e a semente `12345` saiu do teste online por sortear `'B'` · **`QA-15` aberto, não consertado de carona:** `src/ui/rotas.ts` ainda promete que `'A'` começa — é a metade `frontend-uiux`, abaixo
- [x] T-17b — **tela da moeda** (`QA-15`) · **Módulo:** M7 · **Skill:** frontend-uiux · **O que entra:** nos modos `cpu`/`local`, mostrar quem cobra primeiro **antes da 1ª cobrança**, e corrigir `src/ui/rotas.ts`, que declara `LADO_DO_HUMANO` com a promessa de que `'A'` cobra primeiro — com o sorteio vivo o humano pode começar defendendo · **Portão:** o resultado do sorteio visível antes da 1ª cobrança nos dois modos, jogável por toque em 360x640, e nenhuma promessa de ordem fixa sobrando no código de M7

  **◑ ENTREGUE em 2026-08-12 — falta a passada no aparelho real do dono.** O anúncio sai de `state().turn` por `sorteioDoPrimeiro()`, função **pura** em `rotulos.ts`: nenhum lado literal entrou em M7, e é isso que o teste sabota (fixar `'A'` reprova 2 testes) · **`D-49`, vetável:** é **painel dentro da tela de cobrança**, não tela própria — tela própria custaria o 3º toque num fluxo crítico com portão de **2 toques** declarado em `tela_inicio.ts`. O painel sai sozinho na 1ª cobrança (no `local`, já no 1º toque: entre chute e defesa o aparelho trocou de mão) · **Promessas removidas: 3, não 1** — o comentário de `rotas.ts` (o `QA-15` de origem) e mais duas achadas em `tela_selecoes.ts` (rótulos "Quem cobra primeiro/depois" do `local`, hoje "Seleção da esquerda/direita"; e "Você cobra primeiro" do `cpu`). O texto virou **portão varrido por teste** sobre `src/ui/`, com caso negativo escrito para não ficar verde à toa · **Suíte 370/370** (eram 351), `tsc` limpo, bundle 99.590 → **100.735 B** (+1.145 B) · **`QA-16` aberto, não consertado de carona:** `.aviso { display: flex }` vence o `[hidden]` do navegador, e a caixa de erro vazia está visível desde `T-10` em 3 telas — o `.sorteio` novo já traz a própria linha `[hidden]` · **NÃO cobre o `online`**, que segue em `'A'` por `Q-11`, e **não cobre o portão do celular real**: sandbox sem navegador não vê layout (`A-14`)
- [x] T-12 — torneio e chaveamento · **Módulo:** M8 · **Sorteio dos grupos é CEGO (`D-59`)** — sem potes, sem cabeça de chave; 50,2% das sementes juntam duas das quatro primeiras num grupo e isso é o pretendido · **Dificuldade constante (`D-60`)**: o nível escolhido no início vale do grupo à final, sem subir de rodada · ⚠️ **A pergunta que `D-60` NÃO responde:** se M8 reusar o mesmo `Cpu` nas 3 a 7 disputas do jogador, o histograma de `D-10` atravessa o torneio e a CPU fica difícil sozinha na final — progressão implícita, que é o que `D-60` recusou. Um `Cpu` por disputa, ou um só? É a 1ª das 3 perguntas do PLANO e precisa de `D-NN` antes do código · **Etapa:** E-5 — **destravada em 2026-08-12** (A-04 e A-06 respondidas) · **Skill:** backend-domain · **O que entra (`D-53`, `D-57`):** 8 grupos de 4 + mata-mata de 16 com disputa de 3º lugar, somando **64 disputas**; a disputa do jogador sai por `current()` e volta por `report()`, e as outras M8 simula com M2 + M3 — M5 não serve, porque `choose()` espera a escolha deste aparelho. **A conta:** o jogador disputa de **3 a 7** (3 de grupo + até 4 no mata-mata), então são **57 a 61** simuladas, nunca 63 · **A porta serializa:** `toJSON()` / `restoreTournament()`, e quem grava no `localStorage` é M7 · **Custo declarado, e é o não-óbvio (`D-58` aprovou a saída):** restaurar o chaveamento **não** restaura o gerador — a porta de M1 não tem cursor, então `TournamentState` guarda a semente **e quantos sorteios foram consumidos**, e a restauração refaz o `Rng` descartando essa quantidade. Dar cursor a M1 foi **rejeitado** em `D-58`: porta congelada e mais dependida do projeto, por necessidade de um módulo só · **Portão:** exatamente 64 disputas com um campeão, contadas por instrumentação · mesma semente + mesma seleção do jogador = mesmo campeão em 2 execuções · **serializar no meio e restaurar produz a MESMA linha do tempo até o campeão** que não recarregar · um teste por critério de desempate, na ordem, com o sorteio só alcançado quando os três anteriores empatam · nenhum par repetido **dentro da fase de grupos** · `current()` sempre devolve par que contém a seleção do jogador · teto de 70% da CPU nas simuladas · `grep Math.random src/` = 1 e `grep localStorage src/tournament/` = 0 · **O portão "sem par repetido" de `D-13` NÃO vale mais** (`D-57`): grupo e mata-mata reencontram o par por construção
  **✔ FECHADA em 2026-08-19 — a suíte cobre o portão inteiro; o dono roda `npm test` na máquina real.** A pergunta que o card cobrava virou **`D-66`: um `Cpu` por disputa**, os dois histogramas nascendo e morrendo com ela — reusar a instância nas 3 a 7 do jogador daria a progressão *implícita* que `D-60` recusou. **O portão, item a item:** 64 disputas com um campeão em 6 sementes, contadas pelo `played` do único ponto que resolve disputa · mesma semente + mesma seleção = mesmo retrato em 2 execuções · **serializar antes de cada uma das 7 disputas do jogador e restaurar dá o retrato final idêntico** ao de quem não recarregou · um teste por critério de desempate na ordem, com o sorteio pedido **0 vezes** nos quatro primeiros e **2 vezes** (Fisher-Yates de 3) só no empate total · as 48 de grupo são as 6 combinações de cada grupo, cada seleção uma vez por rodada · sorteio cego medido em 2.000 sementes: cada grupo dentro de 4 sigmas de 1/8, **9,85%** juntam a 1ª com a 2ª e **50,9%** juntam duas das quatro primeiras — os números de `D-59` · `current()` sempre traz o par com o jogador · varredura de `src/tournament/` sem armazenamento do navegador e sem import de M5. **O desenho que sustenta a restauração:** o retrato guarda o sorteio já feito (`entrants`), os resultados, a classificação dos grupos e **quantos sorteios foram consumidos** (`D-58`); a fila das 64 é derivada, não gravada, e por isso o retrato é só código de país e inteiro — nem nível nem fase viajam como texto, que é o que `T-14` precisa. **O teste que prova `D-66` é um oráculo:** o próprio teste refaz o sorteio das 32 e simula, do zero, cada disputa até a 1ª do jogador (14, na semente 9) — CPU reusada divergiria na segunda, e o número de sorteios consumidos seria o mesmo, então nenhum outro teste veria. Conferido por mutação: reusar o `Cpu` derruba 8 testes, o oráculo **e** os 7 de recarregamento. **`Q-13` aberta, e é o custo declarado:** `report(winner)` não traz o placar (porta congelada por `D-13`/`D-58`), então a disputa do jogador entra na tabela **sem gols** — ausente, nunca `0 x 0` — e o saldo do grupo dela compara 3 placares com 2. **Suíte 435 → 503/503**, `tsc` limpo, **bundle inalterado em 389.344 B**: M8 ainda não tem quem o importe, e entra no número quando `T-14` o ligar.
- [x] T-20 — **identidade visual: o jogo tem de parecer um jogo** · **Módulo:** M7 (`src/ui/estilo.css` e as 4 telas que já existem) · **Etapa:** E-5, **antes de `T-14`** · **Skill:** frontend-uiux · **Por que existe:** o dono registrou em 2026-08-13 que a tela tem **cara de protótipo** — funciona e passa em todo portão de M7, mas nenhum desses portões fala de **aparência**: eles cobram toque, contraste, fps, camada e licença. O visual de hoje (fundo `#0f1115`, acento verde, fonte do sistema, cartão de raio 12px) nunca foi decidido — foi o que saiu de `T-10` para passar nos portões funcionais. Sem este card, nenhuma etapa futura volta ao assunto e o visual atual é entregue **por omissão**. · **Por que ANTES de `T-14`:** `T-14` acrescenta **três telas** sobre o mesmo `estilo.css`; redesenhar depois custa a folha **mais** as três telas refeitas. · **O que entra:** direção visual aplicada às 4 telas existentes (início, seleções, cobrança, fim) — paleta, profundidade, densidade, tratamento do campo e da bandeira, e movimento (o canvas do Phaser já está na tela de cobrança, então movimento tem casa sem asset novo). A sessão **propõe** direções; **quem escolhe é o dono** — é gosto e é decisão de produto (regra 6). · **Portão:** as 4 telas no visual novo, em 360x640 **e** no desktop, com evidência do que foi tocado e do que apareceu · os portões funcionais de M7 seguem de pé: alvo de toque ≥48px, contraste ≥4,5:1, foco visível de 3px, ≥30 fps, fluxo crítico em **2 toques** · **zero fonte remota e zero `@import` externo** (privacidade de M9) — fonte própria só embarcada em `src/assets/`, e aí com linha de procedência · **todo asset novo** (ilustração, textura, ícone, fonte) com linha em [[licenciamento]] **antes** de entrar no repositório, e **zero marca de terceiro** · animação respeita `prefers-reduced-motion` (a folha já tem o bloco) · o bundle relido de `dist/` não cruza o gatilho de `D-02` — ilustração é o que mais cresce bundle · a direção escolhida vira **`D-NN`**, porque é restrição permanente que as telas de `T-14` herdam · **a aprovação é no aparelho real do dono**, junto de `A-14` — o sandbox não tem navegador e nenhuma tela de M7 é verificável por teste ✔ **FECHADO em 2026-08-19 — o dono aprovou a direção visual no aparelho.** A aprovação é o portão que faltava: as fatias 1 e 2 já estavam no ar (`6bd7276`, `c949ab8`) e a direção virou `D-65`, mas o card exige **aprovação no aparelho real**, e nenhuma tela de M7 é verificável por teste — `vitest` roda em Node sem DOM · **Conferido junto de `A-14`, na mesma passagem em 360x640** · **O que a aprovação torna permanente:** `D-65` deixa de ser proposta e passa a ser a folha que as 3 telas de `T-14` já herdaram — noite de estádio, superfície em degradê com sombra **e** realce, verde do gramado como acento, bandeira em retângulo, capa e profundidade só de degradê (zero asset) e movimento restrito a `opacity`/`transform` · **Custo zero de bundle confirmado:** `D-65` proibia asset novo justamente para não cruzar o gatilho de `D-02`, e o bundle medido em `dist/` depois de `T-14` é de **408.094 B** — 5,1% dos 8 MB · **O que isto NÃO fecha:** E-5, cujo portão é o torneio (64 disputas, reload, procedência dos 32 SVGs, `grep` da lista-morta) e não a folha de estilo
  **DIREÇÃO DO DONO, registrada em 2026-08-13.** Referências entregues: **EA SPORTS FC 26** para os menus (profundidade, imagem grande, movimento, cartões de informação) e a **arte pixel da v1 do próprio dono** para a jogabilidade. O pedido, nas palavras dele: *2D com cores vivas, goleiro aparecendo, **cada goleiro com cor diferente por seleção**, bola visível na tela, torcida ao fundo*, e nos menus *"esse ar que o jogo passa, diversas animações, imagens bonitas"*. · **O que da referência ATRAVESSA:** [[licenciamento]] já responde, na tabela "O que é livre" — *estilo visual de UI inspirado em jogos comerciais é **livre**; estilo e layout não são propriedade*. Então densidade, hierarquia, profundidade, transição, tratamento de cartão e movimento podem ser inspirados à vontade. · **O que NÃO atravessa, e está nas próprias imagens de referência:** escudo de clube, nome e rosto de jogador real, patrocinador no uniforme, uniforme oficial identificável, e o nome das competições — uma delas é literalmente termo da lista-morta varrida pelo `grep` de M7. Copiar **arte** ou **logotipo** é a linha que a mesma tabela proíbe. · **O truque que já está pronto para "goleiro com cor por seleção":** `marcaSelecao().matiz` (M7, `rotulos.ts`) já devolve um matiz estável e **arbitrário** por código, com a documentação dizendo que ele *não representa cor nacional nenhuma*. Tingir o goleiro por ele dá 32 goleiros distintos **sem** reproduzir cor ou uniforme de seleção — que é exatamente o que a restrição proíbe. · **Assets: o precedente é `gen-audio.mjs`.** `src/ui/cena.ts` hoje tem **zero asset** (goleiro é retângulo, bola é círculo, tudo `Graphics`). Pixel art é o **primeiro asset de arte** do projeto, e o caminho limpo é o mesmo dos três áudios: **autorado pelo projeto**, com linha de procedência "do projeto". Reusar a arte da v1 só depois de o dono confirmar a origem dela — a v1 é justamente o baseline que quebrou licença com hotlink. · **Tensões reais a resolver no desenho, não depois:** (1) menu rico **não pode** virar toque a mais — o fluxo crítico fecha em **2** e o portão é esse; a animação mora dentro dos passos que já existem; (2) as referências são 16:9 de console e o alvo é **360x640 retrato** — painel lateral vira pilha vertical; (3) torcida e sprites têm de manter **≥30 fps** no celular do dono; (4) tipografia é metade do "ar" da referência e **fonte remota é proibida** — ou fonte aberta embarcada com procedência, ou pilha de sistema bem ajustada; (5) **não inventar dado**: a referência mostra estrelas, valor de elenco e títulos porque tem base licenciada — o jogo tem código, nome e bandeira, e `D-60` deixa a dificuldade constante, então nota por seleção seria número inventado (regra 5).
  **◑ FATIA 2 ENTREGUE em 2026-08-19 — o card fecha quando o dono aprovar no aparelho dele (`A-14`).** A direção virou **`D-65`**, e as 4 telas estão no visual novo. **O que mudou nos menus:** paleta de noite de estádio (fundo `#0b0f16` com refletor radial no topo), superfície em degradê com sombra **e** realce interno em vez de cor chapada, três degraus tipográficos (título `clamp`, legenda em caixa alta com barra de acento, sub apagado), **capa** na tela de início — campo visto de cima feito de `repeating-linear-gradient` mais duas bordas, com refletor que passa —, **confronto** no topo das seleções (as duas bandeiras a 61x46, espelhando escolha que já existe) e **pódio** na tela de fim (bandeira do vencedor a 87x66). **A bandeira saiu do círculo:** `object-fit: cover` em disco cortava a faixa de baixo de toda tricolor horizontal, e os SVGs de `T-19` são 4:3 — agora é retângulo, 50x38 no cartão. **Zero asset novo, zero fonte remota:** capa e profundidade são degradê, e o bundle inicial foi de 383.264 para **389.344 B** (4,87% do teto). **Achado colhido no dev server:** os `<fieldset>` de `.grupo` ainda vinham com a borda `groove` cinza e 12px de padding da folha do NAVEGADOR — prototípico, e comendo 31px da largura do cartão de seleção; zerados, o cartão foi de 148 para **164px**. **Portões conferidos no navegador do sandbox, em 360x640 e no desktop:** zero rolagem horizontal nas três telas de menu, alvo de toque em 52/48px, foco `:focus-visible` de 3px em `#ffd166`, e o fluxo crítico em **2 toques** (início → seleções → cobrança). **Contraste virou teste**, não olhômetro: o produto cartesiano da paleta (8 fundos × 6 cores de texto) passa de 4,5:1, com o pior par em 4,87. **O que NÃO foi verificado aqui:** a tela de cobrança não é alcançável no sandbox — a aba não compõe quadros, `requestAnimationFrame` nunca dispara, o Phaser não inicia e as zonas ficam desabilitadas; a tela de fim foi medida com a marcação injetada sob a folha real, não pela rota. Fps, cor e toque real são `A-14`. **`QA-21` aberto e não consertado de carona:** a `.lacuna` do rodapé ainda anuncia que as bandeiras não entraram, e elas entraram em `T-19`.
- [x] T-14 — telas do torneio: chaveamento, próxima disputa e campeão · **Módulo:** M7 · **Etapa:** E-5, **depende de T-12 (fechada)** e herda `Q-13` (lê o chaveamento pela porta de M8) · **Skill:** frontend-uiux · **O que entra:** a tabela do grupo, a próxima disputa, o campeão, e a **gravação do torneio** no `localStorage` a partir do `toJSON()` de M8 · **Portão:** jogável por toque em 360x640 · fechar e reabrir o navegador no meio do torneio continua de onde parou · torneio salvo que não desserializa é **descartado em silêncio** e o jogo abre no menu, nunca numa tela quebrada · o que vai para o `localStorage` é conferido por teste contra lista fechada de chaves, e o torneio salvo só tem código de país e inteiro · o nome **TAP GO Cup** e o `grep` da lista-morta em zero
  **◑ ENTREGUE em 2026-08-19 — o card fecha quando o dono jogar o torneio no aparelho dele (`A-14`).** As três telas são `tela_torneio_novo.ts` (qual seleção é a sua), `tela_torneio.ts` (próxima disputa + tabela do grupo) e `tela_campeao.ts`; a cobrança é a MESMA de sempre, e a volta a M8 sai da tela de fim (`report(winner)` + gravar). **O portão, item a item:** o registro salvo é `{v, humana, nivel, estado}` (**`D-68`**), varrido por teste valor a valor — só código de país e inteiro, zero texto livre · **lista fechada de chaves** conferida sobre o que foi gravado de verdade (`CHAVES_DE_M7`) · registro ilegível (6 formas de lixo) devolve `null`, não lança e **apaga a chave**, e o jogo abre no menu — conferido também no navegador, cortando o JSON pela metade e recarregando · gravar-e-restaurar antes de cada disputa do jogador chega ao **mesmo campeão** de quem não recarregou · fechar e reabrir continua de onde parou, visto no dev server em 360x640 (2ª rodada de volta com a chave intacta). **`Q-13` virou `D-67`:** a linha do jogador mostra `—` nos gols, nunca `0` — o zero dela é estrutural, porque as 3 disputas dela são justamente as sem placar; as outras três somam o que sabem, com a nota abaixo da tabela. Íntegra dos dois em [[m7_tela_notas]]. **Suíte 503 → 531/531**, `tsc` limpo, **bundle 389.344 → 408.094 B** (lido do `dist/` do dono) (M8 entrou no número, como `T-12` previu). **O que NÃO está coberto:** fps, toque e leitura em aparelho real (é `A-14`), e a fase em que o jogador foi eliminado — a porta de M8 não a entrega, e a tela do campeão não a inventa.
- [x] T-18 — catálogo real: as 32 seleções no lugar da lista de fixação · **Módulo:** M4 · **Etapa:** E-5 · **Skill:** backend-domain · **O que entra:** os 32 códigos de [[m4_lista_das_32]] (`D-51`), o `code` ampliado para aceitar `GB-ENG` (`D-52`, confirmado em `D-58`) e `CATALOG_IS_FIXTURE` virando `false` · **`flag` segue `null` até `T-19`** (`D-22`), e por isso este card não espera arquivo nenhum · **Custo medido, e é o que decide o desenho do teste:** `new Intl.DisplayNames(['pt'],{type:'region'}).of('GB-ENG')` lança `RangeError` no Node 22 — o ICU resolve região, não subdivisão. O nome da Inglaterra passa a ser o **único** literal do catálogo, e a exceção tem de ser **nomeada** (lista de exceções de tamanho 1 conferida por teste), nunca um afrouxamento do portão de `D-23` · **Portão:** exatamente 32 entradas sem código repetido · todo `code` casa `^[A-Z]{2}$` **ou** é o `GB-ENG` de `D-52`, e um segundo código fora da alfa-2 reprova · exatamente **um** literal de nome na fonte, e o teste diz qual · o resto dos nomes continua vindo do ICU · suíte verde e `tsc` limpo

  **✔ ENTREGUE em 2026-08-12.** As 32 de [[m4_lista_das_32]] entraram na ordem de `D-51`, `CATALOG_IS_FIXTURE` = **false** e a fonte não cita a origem por extenso — só `D-51` e o ponteiro para a nota · **`D-61`, a única decisão:** a exceção de `D-52` é **lista fechada** (`NAME_EXCEPTIONS`, tamanho 1) e não regra de formato, e M4 exporta o validador (`assertCatalogCode`) para o portão ser cobrado na via mais baixa — nenhuma assinatura de `D-13` muda · **Portão cumprido, e cada linha dele reprova de fato (7 sabotagens):** 32 entradas sem repetido · segundo código fora da alfa-2 **lança no carregamento**, não passa calado · exatamente **1** literal de nome na fonte, e o teste o nomeia · trocar a exceção por "aceita subdivisão" reprova · alfa-2 usada como exceção (nome digitado onde o ICU resolve) lança no carregamento · **Suíte 377/377** (eram 370), `tsc` limpo, bundle 100.735 → **101.256 B** lido de `dist/` · **O teste que `T-08` deixou armado foi REDIRECIONADO, não apagado:** a condição era `CATALOG_IS_FIXTURE` para forçar revisitar a licença em `A-04`; `A-04` fechou (`D-54`), então a guarda passou a cobrar `flag` — a 1ª bandeira que deixar de ser `null` derruba o teste e obriga a linha de procedência, que é `T-19` · **`QA-18` aberto, não consertado de carona:** o disco de 34px de M7 mostra o código, e `GB-ENG` tem 6 caracteres onde cabiam 2
- [x] T-19 — as 32 bandeiras com a licença · **Módulo:** M4 · **Etapa:** E-5 · **Skill:** backend-domain · **O que entra:** os 32 SVGs do **flag-icons** (`D-54`), **o texto da licença MIT com o aviso de copyright** (© 2013 Panayiotis Lipiridis) e uma linha por arquivo na tabela de procedência de [[licenciamento]] · **`D-22` dizia "`flag: null` até `A-04`"; `A-04` fechou e o que restava dela é este card** — a condição não mudou (bandeira sem arquivo é `null`), só o número do card que a satisfaz · **A licença entra ANTES do primeiro SVG** — asset sem procedência não entra no repositório, e o teste de `T-10` já varre `src/assets/` cobrando a linha · **Portão:** 32 arquivos locais, zero URL e zero hotlink · 32 linhas novas na tabela de procedência, e o teste de varredura verde · o texto da licença versionado · `flag` deixa de ser `null` (`D-22`) · bundle relido de `dist/` **depois** dos SVGs — o teto de 8 MB é número lido, não estimado

  **✔ ENTREGUE em 2026-08-12.** Os 32 SVGs do **flag-icons 7.5.0** (`flags/4x3/`, cópia byte a byte) estão em `src/assets/flags/`, com **SHA-256 e bytes de cada um** na tabela de procedência — origem conferível, não declarada, como nos áudios de `T-10` · **A ordem do portão está no histórico, não em prosa:** o commit `ec90b47` traz `LICENSE.txt` (aviso MIT íntegro, © 2013 Panayiotis Lipiridis) e é **anterior** ao commit dos SVGs · **`D-62`, a única decisão:** `flag` sai de `import.meta.glob` eager com `?no-inline` — o caminho vem do arquivo versionado, não de segunda lista digitada, e sem o `no-inline` 24 dos 32 virariam `data:` dentro do JS, dando **dois formatos ao mesmo campo** (a suíte não veria: ela roda em modo dev, onde nada é embutido) · **Seleção sem arquivo derruba o carregamento**, e `buildCatalog` é exportado para o teste tentar a violação na via mais baixa (mesmo motivo de `D-61`) · **Portão cumprido, com 4 sabotagens que reprovam de fato:** apagar um SVG mata o carregamento com o caminho esperado na mensagem · apagar uma linha da tabela reprova o arquivo pelo nome · arquivo estranho na pasta reprova · `<image href="https://…">` **dentro** do SVG reprova (as únicas ocorrências de `http` nos 32 são namespace XML do W3C) · **Suíte 385/385** (eram 377), `tsc` limpo, bundle **101.256 → 382.402 B** relido de `dist/` depois dos SVGs, **4,78%** do teto · **`QA-18` NÃO sumiu, ao contrário do que a linha dele previa** — M4 entrega o caminho, mas `marca()` de M7 escreve `flag` como **texto**, então o disco de 34px passou a mostrar o caminho do SVG. Isso é **`QA-19`**, é de M7 e não foi consertado de carona (regra 4); o único arquivo de M7 tocado foi o **teste** que cravava `flag === null`, redirecionado como o de M4 e não apagado

- [x] T-29 — **as 32 cores nacionais na camisa** ✔ **COM CAMPO em 2026-08-21 (`A-32`)** (`P-6`(b), aberto como card próprio em 2026-08-21 — `T-28` o declarou fora do escopo dela) · **Módulo:** M7 — `src/ui/rotulos.ts` (a tabela e `marcaSelecao`), e `src/ui/sprites.ts` só na saída (B) de `Q-16` · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **🔒 TRAVADO em `Q-16`, e a trava é de DADO, não de código:** cor nacional não é derivável do catálogo, do código ISO nem da bandeira — é a regra 5, e `P-6` já a tinha escrito ("a tabela entra como DADO revisado pelo dono") · **A tabela das 32 está montada e MEDIDA:** [[cores_nacionais]] · **O portão de contraste de `T-20` passou, e a medida diz mais do que "passou":** varredura dos **360** matizes, pior caso **7,28:1** em `h=240` contra o limite de 4,5:1 — saturação e luminosidade são fixas por papel, então o matiz **não é o canal que decide contraste** e esse portão não pode reprovar uma tabela de matizes. Ele continua de pé; só não é ele que barra nada aqui · **Duas seleções REPROVAM, e voltaram ao dono sem ajuste meu:** `GB-ENG` (branco) e `DE` (branco/preto) não são um ângulo no círculo de matiz · **O achado que derruba a premissa do card:** `P-6` dizia "tabela curada não colide por construção" — **falso, e medido**. Quatorze das 32 têm vermelho como cor nacional: a tabela dá **15** matizes distintos para 30 seleções contra os **30** do hash de hoje, e **144 de 435** pares (33,1%) ficam abaixo dos 40° de `SEPARACAO_MINIMA`, contra 101 de 496 (20,4%) do hash. Trocar o hash pela tabela **dobra** a chance de duas camisas nascerem confundíveis · **Portão quando `Q-16` fechar:** `check.py` verde, suíte, `tsc`, bundle relido de `dist/`, zero asset novo, zero cor nova na paleta de `.tapgo`, e o teste de `ui.test.ts:797` (o que hoje cobra `30` matizes distintos) reescrito para o número que a saída escolhida produzir · **Campo:** `A-32` · **✔ ENTREGUE em 2026-08-21, pela saída (B) — o dono escolheu, e `D-88` a registra.** Delta em **três** arquivos de produção (`src/ui/sprites.ts`, `src/ui/cena.ts`, `src/ui/tela_cobranca.ts`) + 12 testes. `check.py` verde, `tsc` limpo, suíte **586 → 596/596**, bundle **421.451 → 425.115 B (+3.664 B)**, **zero asset novo** e **zero cor nova na paleta de `.tapgo`** — a cor da camisa nunca foi token de CSS, então o teste cartesiano de `T-20` segue medindo exatamente o que media · **A (B) mudou o que se CEDE, e é isso que ela comprou:** `matizDistinto` separava dois lados parecidos jogando o matiz de B para o oposto do círculo — com cor de hash isso não custava nada, com cor nacional custa Espanha × Portugal de ciano. Quem separa agora é o PADRÃO: o lado B ganha listras e **mantém a cor nacional**, e o teste das 32x32 cobra as duas coisas na mesma volta · **O escopo foi CORTADO de propósito, e o corte está declarado:** a cor nacional vai só para a camisa em campo — `marcaSelecao` e o disco do placar **não foram tocados**. Desde `T-19` o disco mostra a bandeira, e o matiz fica invisível embaixo dela (medido na sessão anterior): mexer nele custaria todas as telas e não mudaria um pixel do que a pessoa vê. É também o que fez `QA-20` fechar sem virar mudança global · **Duas coisas reprovaram na medição e foram corrigidas por medida, não por gosto:** (1) o `verde` do tom médio ficava a **29,7** de distância RGB da faixa mais parecida do gramado — camisa verde sumindo em campo verde, para as CINCO seleções verdes de uma vez; no tom escurecido a distância é **72,4**, e há teste sobre as 4 faixas · (2) o `dourado` ficava a **38,7** do `amarelo`, abaixo do próprio limiar, então ele saiu e a Austrália entrou em amarelo — um nome a menos e nenhuma perda · **O limiar de 40 não é chutado:** medida contra as 8 cores, a menor distância entre cores DIFERENTES é **63,8** (vermelho × vermelho-escuro) e entre iguais é **0** por construção — qualquer valor entre 1 e 63 separa os dois casos · **`GB-ENG` e `DE` deixaram de ser buraco sem eu inventar cor:** a (B) trocou a representação de matiz por HSL completo, e branco é `s: 0` — o que a saída (A) não conseguia dizer. A cor deles (branco) é a que já estava na tabela que o dono revisou ao escolher (B) · **6 falseamentos rodados, cada um reprovando exatamente o que cobra:** desligar o desempate; clarear o verde; listrar o boneco inteiro; trocar quem cede; tirar o padrão da chave de textura; aproximar duas cores nomeadas · **LACUNA DECLARADA, e desta vez com a causa MEDIDA:** o sandbox não desenha o campo porque `document.visibilityState` é **`hidden`** mesmo com a aba fronteada — `requestAnimationFrame` não dispara, então Phaser nunca renderiza e não há pixel a ler. As sessões anteriores diziam "a pane não compõe"; a causa é esta, e ela explica de uma vez por que nenhuma tela de M7 é verificável por lá · **O que o sandbox PROVOU mesmo assim:** com Espanha × Croácia (as duas vermelhas) escolhidas por DOM, a tela de cobrança montou o campo (`campo__sem-canvas` oculto, ou seja `definirCamisas` rodou sem lançar), **zero erro de console**, e `scrollWidth == clientWidth` em 360x640 (360) e em 1280x800 (1265), com o campo em 334x241 e 641x462 — as mesmas medidas de `T-27`, nada regrediu
- [x] A-15 — **dividir o registro pela terceira vez, ou subir o teto**: `A-13` diagnosticou três registros de ciclo de vida diferente num orçamento só e moveu **um** (`QA-NN` → [[d_qa|QA]], `D-50`). O terceiro — as **questões abertas** — continua no `c_decisions.md` e custa **1.637 caracteres**, dos quais 11 linhas para **3 questões vivas**: `Q-01`, `Q-02`, `Q-04`, `Q-07`, `Q-10` e `Q-12` já foram respondidas e só ocupam espaço · **Medido em 2026-08-12:** sem essa seção o registro cai de 9.855 para **8.217**, ou seja folga de 1.383 abaixo do aviso — cerca de 6 decisões, contra as 0 de hoje · **Por que é urgente agora:** esta sessão sozinha escreveu 9 linhas (`D-51`..`D-59`) e precisou arquivar duas para caber; a próxima que responder duas questões não cabe · **Saídas:** (a) `Q-NN` vai para `a_context/e_questoes.md`, mesmo movimento de `D-50` — exige mexer no `check.py`, que só procura ID definido em DECISOES e QA; (b) subir o aviso de 9.600, que `A-13` deixou na mesa como **complemento**; (c) retirar da tabela as 6 questões já respondidas, deixando o ponteiro — barato, mas some o "quando foi decidido" de cada uma · **Portão:** a saída vira `D-NN` com o custo declarado e o registro volta abaixo do aviso, com os IDs preservados e nenhuma questão viva perdendo prazo. **É decisão do dono** — muda portão do kit, não módulo do jogo

  **✔ FECHADA em 2026-08-13 — saídas (c)+(b), em `D-63`.** As **8** questões respondidas saíram da tabela viva para [[decisions_archive]], com íntegra e ID preservados — **8, não 6:** `Q-03` e `Q-05` foram respondidas pela própria sessão que escreveu este card · **Os números do card estavam velhos:** ele mediu 9.855 e projetou 8.217 sem a seção inteira; o registro já estava em **10.998**, e a mesma operação chegava a 9.463 — 137 abaixo do aviso, não 1.383 · **Por isso (c) sozinha não fechava:** economiza 794 e para em 10.204, ainda 604 acima de 9.600 · **O aviso media um registro que não existe mais**, e subiu para **10.800** na mesma decisão · **O corte de `D-43` está esgotado:** nenhuma das 39 linhas vivas deixou de ser citada por um `.md` vivo, e o texto do aviso do `check.py` — que mandava arquivar "as mais antigas" — passou a apontar o critério de `D-43`, porque sugeria exatamente as linhas que `D-43` proíbe retirar · **Estado:** registro **10.600/12.000**, 200 abaixo do aviso, folga de **3,9 decisões** até o teto (362 caracteres cada nas 5 últimas) · **Portão cumprido:** `check.py` verde, nenhum "ID citado que não existe", e as 3 questões vivas (`Q-08`, `Q-09`, `Q-11`) intactas na fila do dono e no CONTEXT · **O que fica de aviso:** `T-12` cabe, mas por pouco — a sessão de M8 provavelmente precisa de outro corte.



## Em andamento (máx 1 — espelha "Em andamento" do [[a_context_source|CONTEXT]])
_(vazio)_

## Feito (mover para cá; detalhe no [[a_changelog|CHANGELOG]])
- [x] T-28 — **a bandeira de quem cobra primeiro, grande, e o papel legível sem texto** (`P-6`(a)+`P-8` num card, escolhidos pelo dono em 2026-08-21 — `D-87`) · **Módulo:** M7 só — `src/ui/tela_cobranca.ts`, `src/ui/estilo.css`, e possivelmente `src/ui/cena.ts` para o canal de forma em campo · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **Por que os dois pedidos são UMA peça:** a sobreposição que mostra a bandeira de quem bate primeiro já É o segundo canal que `P-6` pedia — quem cobra fica identificado por bandeira (não por cor), e o papel do aparelho segue anunciado por forma · **Não reabre `D-49`:** ele matou a TELA da moeda, que custava o 3º toque num fluxo com portão de 2; sobreposição que aparece e sai sozinha não pede toque, mesmo argumento que aprovou `D-85` · **Não reabre `D-65`:** só `opacity`/`transform`, zero asset — as 32 bandeiras já estão no bundle desde `T-19`, com licença conferida arquivo a arquivo · **Três restrições que são PORTÃO, não gosto:** (1) o toque **não pode se perder** enquanto a sobreposição está na tela — é o item (2) do portão de `A-29`, e sobreposição em tela cheia é justamente o que rouba toque: `pointer-events: none`, ou saída imediata ao primeiro toque; (2) com `prefers-reduced-motion` ligado ela não pisca nem trava — o bloco global zera duração E atraso, então o estado final tem de ser "já saiu"; (3) **cor não pode ser o único canal** (daltonismo), e cor nova entra na paleta, nunca ao lado dela, porque `T-20` cobra contraste ≥4,5:1 sobre o produto cartesiano · **O guarda de `D-85` vale igual e é o miolo:** `desenhar()` roda a cada notificação de M5, e no `online` `aoNotificacaoDeRede()` a chama até sem novidade — a sobreposição só pode nascer na transição escondido -> visível, senão recomeça sozinha antes do 1º toque · **Portão:** `check.py` verde, suíte, `tsc`, bundle relido de `dist/`, e as telas em 360x640 E 1280x800 sem rolagem horizontal · **O card de aparelho que sair daí pede UM número** (a forma de `A-30`, que funcionou) · **NÃO entra:** `P-6`(b), as 32 cores nacionais — é card próprio, com a tabela de cores entrando como DADO revisado pelo dono, e fecharia `QA-20` de quebra · **✔ COM CAMPO em 2026-08-21 (`A-31`): o dono respondeu `1`** — a bandeira grande nasce UMA vez antes do 1º toque, o toque durante ela vale, e o símbolo de papel está nos botões · **✔ ENTREGUE em 2026-08-21 — o portão de sandbox passou inteiro.** Delta em **dois** arquivos de produção (`src/ui/tela_cobranca.ts`, `src/ui/estilo.css`) + 4 testes. `check.py` verde, `tsc` limpo, suíte **582 → 586/586**, bundle **419.311 → 421.451 B (+2.140 B)**, **zero asset novo** — as 32 bandeiras já vinham de `T-19`, e `cena.ts` não foi tocado · **Os três itens de PORTÃO foram MEDIDOS, não construídos:** (1) **o toque não se perde** — com a sobreposição no pico (868 ms), `elementFromPoint` no centro das TRÊS zonas devolve o próprio `<button>` nos dois viewports; a defesa é `pointer-events: none`, escolhida sobre "sair ao primeiro toque" porque essa ainda comeria ESSE toque · (2) **`prefers-reduced-motion`**, simulado pelas três declarações do bloco global: no quadro 0 `opacity` já é **0** na moeda E no fundo, e em 120 ms o nó está **fora da árvore** — entregue já fora da tela, sem piscar, com o painel de `D-49` intacto · (3) **cor não é o único canal**: `chutar` é triângulo (17,6x12,8) com borda `rgba(74 222 128 / 55%)`, `defender` é arco (22x12, raio 13px) com `rgba(255 209 102 / 55%)` — duas silhuetas distintas, e **nenhuma cor nova na paleta** (os dois valores são `--acento` e `--atencao`, que `T-20` já mede) · **Sem rolagem horizontal em nenhum quadro:** `scrollWidth == clientWidth` em 360 (360) e em 1280x800 (1265), amostrado em t=0/250/868/1200/1400 ms · **A sobreposição pousa NO painel, e isso é medido, não chutado:** `--para-x`/`--para-y` saem do `getBoundingClientRect()` do painel já visível — em 360x640 o quadro final cai em (47,162) contra o centro do painel em (47,162); em 1280x800 fica a 8px, a folga da cascata de entrada (`translateY(10px)`) · **O canal de forma não custou altura:** `scrollHeight` do documento é **824 com e sem** `data-papel` — a forma mora dentro de `.zona`, que é absoluta dentro de `.campo` de proporção fixa · **4 falseamentos rodados, cada um reprova exatamente 1 teste:** tirar `pointer-events: none`; mover `mostrarSorteioCheio` para fora do guarda `entrando`; igualar as duas formas; trocar o verde por um tom fora da paleta · **LACUNA DECLARADA, a mesma de `T-26`:** o guarda de `D-85` no caso que ele existe para cobrir — `aoNotificacaoDeRede()` chamando `desenhar()` sem novidade no `online` — **não é alcançável no sandbox** (só o `online` o produz, e ele precisa de dois aparelhos). O que roda por lá é a varredura de fonte, que cobra a chamada única dentro de `if (entrando)`; o quadro em movimento é olho do dono, e é `A-31`
- [x] T-27 — **o desktop deixa de ser a coluna de 420px** (`P-1`+`P-7` no mesmo card, escolhidos pelo dono em 2026-08-20) · **Módulo:** M7 só — `src/ui/estilo.css`, `src/ui/tela_selecoes.ts`, `src/ui/tela_cobranca.ts`, `src/ui/tela_torneio.ts` · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **A direção é do dono, e foi escolhida antes da primeira linha de CSS:** a sessão apresentou 3 (coluna mais larga · duas colunas · trilho fixo) com custo e limite de cada uma, e o dono escolheu **duas colunas** — virou `D-86` · **Não reabre `D-65`:** zero asset novo, nenhuma cor fora da paleta, e o bloco novo não anima nada — só coloca caixa · **A regra numa frase:** a largura da folha é declarada pela TELA (`.tela--largo` 1040px em 2 colunas · `.tela--disputa` 760px · o resto 460px), e `:has(> .tela--…)` no `.tapgo` é o que deixa a filha declarar a largura da mãe · **O que fica de fora, DECLARADO e não esquecido:** início, convite, fim, campeão e torneio novo continuam coluna centrada — um menu de 4 botões não tem o que pôr em duas colunas, e alargar o botão é o que o bloco de 480px já recusava. E `P-6` (papel legível sem texto) **não** entrou: pede decisão de cor E forma, e nenhuma das 3 direções o tocava · **Portão:** `check.py` verde, suíte **582/582** (nenhum teste novo: `vitest` roda em Node sem DOM e nenhuma tela de M7 é alcançável por lá), `tsc` limpo, bundle relido de `dist/` (418.526 -> **419.200 B**, +674 B, zero asset novo) e as telas medidas no navegador em **360x640 E 1280x800**, mais 1023, 1024 e 1920x1080 · **Os números:** seleções vão de 2 para **4 cartões por linha** e de 2.719 para **1.590 px** de altura; o campo da cobrança sai de 336x243 para **642x464** com a base em 709 (dentro da dobra de 800); torneio põe a próxima disputa e a tabela lado a lado, as duas em `y = 137`; `scrollWidth == clientWidth` em **todos** os viewports medidos · **Em 360x640 nada mudou, e isso foi medido, não presumido:** 2 cartões por linha, folha de 360, campo 336x243 — tudo o que a sessão escreveu está dentro de `@media (min-width: 1024px)` · **`QA-31` achado e NÃO consertado de carona** (regra 4): o "Sair da disputa" tem 144px num bloco de 336, igual antes e depois desta sessão · **✔ COM CAMPO em 2026-08-21 (`A-30`):** o dono respondeu **4**, viu as duas grades lado a lado e nenhuma barra horizontal — `D-86` está no monitor dele, não só na medição do sandbox · **O campo achou o que o sandbox não achava, e é a lição do card:** a tela de começar o torneio usa a mesma `grade()` e ficou fora de `D-86`, porque o critério que eu escrevi foi "tem PAR?" e não "tem a mesma grade?". Os mesmos 32 cartões saíam em duas larguras. Corrigido em 2026-08-21 com `.solo` (grade sozinha a 4 colunas) e `QA-31` fechado junto · **Evidência inteira:** `D-86` e [[t27_desktop_duas_colunas]]
- [x] T-26 — **a moeda do sorteio animada dentro do painel** (`P-5`, escolhido pelo dono em 2026-08-20) · **Módulo:** M7 só — `src/ui/tela_cobranca.ts`, `src/ui/estilo.css` · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **Não reabre `D-49`:** o que ele matou foi a TELA da moeda, que custava o 3º toque num fluxo com portão de 2; o painel de `D-48` já nascia junto com a tela de cobrança e já saía sozinho, e animar a marca dentro dele não acrescenta toque nenhum — a tela de cobrança segue com 4 botões (as 3 zonas e "Sair da disputa") · **Cabe em `D-65` sem reabri-lo:** só `opacity` e `transform` — `perspective()` é FUNÇÃO de `transform`, não propriedade nova, e `scale()` só encolhe (0,7 -> 1), então a moeda nunca alarga a linha · **O guarda que o card obrigou, e ele é o miolo:** `desenhar()` roda a cada notificação de M5, e no `online` `aoNotificacaoDeRede()` a chama até sem novidade — várias vezes antes do 1º toque, com o painel na tela. A marca passou a ser reconstruída **só na transição escondido -> visível** (`sorteioNaTela`); sem isso cada notificação recomeçaria o giro do zero. Os dois textos do painel seguem sendo reescritos sempre · **Portão:** `check.py` verde, suíte **582/582** (nenhum teste novo: `vitest` roda em Node sem DOM e nenhuma tela de M7 é alcançável por lá), `tsc` limpo, bundle relido de `dist/` (418.254 -> **418.526 B**, +272 B, zero asset novo) e o painel medido no navegador em **360x640 e 1280x800** — `scrollWidth == clientWidth` durante e depois do giro · **`prefers-reduced-motion` conferido por medida, não por leitura:** com as três declarações do bloco global aplicadas à marca (duração 0,01 ms, atraso 0 s, 1 iteração) o quadro medido é o final — `matrix3d` identidade, `opacity: 1`, marca em 44,875 x 34 px: parada e opaca, nunca invisível · **LACUNA DECLARADA:** o sandbox mediu geometria e estilo computado, não quadros — a pane do navegador fica escondida e não compõe, tanto que `animation.finished` nunca resolve lá (o quadro final foi lido fixando `currentTime`). Se o giro AGRADA em movimento, e se ele não atrapalha quem vai tocar, é olho do dono no aparelho · **Evidência inteira:** `D-85` e [[t17b_sorteio_na_tela]]
- [x] T-25 — **a coluna "Pts" na tabela da TAP GO Cup** (`P-2`, escolhido pelo dono em 2026-08-20) · **Módulo:** M7 só — `src/ui/rotulos.ts`, `src/ui/tela_torneio.ts`, `src/ui/estilo.css` · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **Não é campo novo:** `pontos = 3 x vitórias`, exatamente, porque a disputa nunca empata (`D-09`) — render puro, **zero byte em `src/tournament`** · **O cuidado do card, cumprido por texto e por teste:** `NOTA_PONTOS` fica embaixo da tabela dizendo que empatar em pontos é empatar em vitórias e que o desempate segue o de `D-53` (confronto direto -> saldo -> gols -> sorteio), e um teste cobra a cascata inteira dentro da frase · **Portão:** `check.py` verde, suíte **582/582** (eram 580), `tsc` limpo, bundle relido de `dist/` (417.664 -> **418.254 B**, +590 B, zero asset novo) e a tabela conferida em 360x640 · **Um conserto de layout entrou junto, e ele é da própria coluna:** com cinco colunas, o nome mais longo do catálogo empurrava a tabela a 359px dentro de 336px — rolagem horizontal, o que o portão de `T-20` proíbe. `max-width: 0` na célula do nome faz ela ceder e o `ellipsis` que já existia resolver; medido em 360x640, sobra **0 px** de estouro no pior caso · **LACUNA DECLARADA:** o sandbox não roda a tela de cobrança (a pane do navegador não compõe quadros, e nenhuma tela de M7 tem teste), então a tabela foi conferida com o grupo **zerado** e o pior caso de largura foi injetado por DOM — tabela com pontos REAIS depois de disputas jogadas é `A-28`, do dono
- [x] T-24 — **tempo por cobrança no `online`** (`Q-15` respondida: **15 s**, e **quem demorou perde a cobrança**) · **Módulo:** M7 pela saída (b) — ver abaixo · **Etapa:** pós-E-6 · **Skill:** frontend-uiux · **Era `T-22` até 2026-08-20**, ID que já era o contador de segundos de `A-24` — fechado no mesmo dia, e ID não se recicla (`QA-28`). Pelo mesmo motivo a questão é `Q-15`, não `Q-14`, que é o STUN de `D-71` (`QA-24`) · **Bloqueado por `D-NN` do dono, e só por ele:** o orçamento destravou — `D-82` levou o teto do registro a 20.000 e ele está em 16.676, então a decisão cabe. `A-21` foi executada em 2026-08-20 e não é mais dívida
  · **O problema que a saída óbvia tem, e que reprova antes de compilar:** P2P não tem árbitro, e são DOIS relógios. Se cada aparelho contar 15 s e resolver localmente, uma jogada que chega em 15,05 s aqui e 14,90 s lá faz os dois resolverem a MESMA cobrança de formas diferentes — os `MatchState` divergem em silêncio e a disputa racha. Hoje a sincronia existe porque **só a jogada trafega** e a regra é a mesma dos dois lados (`resolverOnline`); um estouro de relógio local quebra exatamente essa premissa
  · **Saída (a) — o estouro vira evento no canal:** quem estoura avisa, e só o aviso resolve. É `Move` novo, logo **porta congelada de M6/M5 reaberta (`D-13`)** — decisão do dono, sessão de backend, e o teto de `D-02`/registro no caminho
  · **Saída (b) — ESCOLHIDA PELO DONO em 2026-08-20; falta só o `D-NN`, que desde `D-82` cabe no registro — o estouro não sai deste aparelho:** aos 15 s, o próprio aparelho **escolhe sozinho** uma zona (por `createRng(newSeed())` de M1, que M7 já importa) e a manda como jogada normal. O outro lado nunca sabe que houve estouro, então **não há o que divergir**. "Perde a cobrança" vira "cobra no escuro", que pune de fato: chute aleatório contra goleiro que escolheu. **Só M7**, zero byte em `src/net`, `D-13` intacto — mas continua sendo regra de disputa, logo continua sendo `D-NN` do dono
  · **Portão, qualquer que seja a saída:** dois aparelhos reais, um deles parado de propósito por 15 s, e as duas telas terminando a disputa com o MESMO placar
  · **✔ FECHADA em 2026-08-20 pela saída (b), adotada em `D-84`** (skill `frontend-uiux`). **Delta em 3 arquivos de produção, todos de M7:** `src/ui/rotulos.ts` ganhou o prazo e os textos como coisas PURAS (`PRAZO_COBRANCA_MS`, `SEGUNDOS_DE_PRESSA`, `textoDoPrazo`, `avisoDePressa`, `AVISO_COBRANCA_SORTEADA` — puras porque é assim que a suíte as alcança sem DOM, o caminho de `T-22`) · `src/ui/tela_cobranca.ts` ganhou o relógio (`armarRelogio`/`pararRelogio`/`pintarRelogio`/`cobrarNoEscuro`) · `src/ui/estilo.css` ganhou UMA regra (`.sub[data-tom='atencao']`), sem mudar posição de nada: linha que salta de lugar no fim da contagem empurraria os botões de zona debaixo do dedo · **`src/net` com 0 byte alterado, `D-13` intacto, portão de camada de M7 em 0 ocorrências no padrão do CI**
  · **Onde o relógio liga e desliga, que é o que faz a saída (b) valer:** arma no fim de `desenhar()`, só quando a vez é de escolher, e é idempotente por `cobrancaDoRelogio` (`estado.kicks.length`) — notificação de M5 sem novidade NÃO devolve segundos a quem já gastou 14 · para no toque aceito, porque quem deve agora é o outro lado e o relógio que o cobra corre no aparelho dele · para quando o peer some (`'waiting'`, `T-22`): ali já corre o prazo de M6 e a disputa pode acabar sem resultado (`D-35`), e dois contadores na mesma tela cobrariam pressa de quem não tem com quem jogar · para na queda (`D-35`/`D-80`/`D-81`) e na desmontagem
  · **Semente do estouro é `newSeed()`, e isto não é detalhe:** a semente da disputa é acordada entre os dois aparelhos, então reproduzi-la aqui daria ao outro lado como adivinhar a zona. O acaso do estouro nasce e morre na linha
  · **Acessibilidade:** o número tem `aria-live="off"` pelo motivo de `T-22` (região viva mudando a cada segundo faz o leitor recitar sem parar); quem anuncia é a faixa, UMA vez, a 5 s do fim (`avisoDePressa`). Teclado inalterado — o estouro usa o mesmo `escolher()` do toque
  · **Sandbox:** suíte **574 → 580/580** (6 testes novos), `tsc --noEmit` limpo, bundle **416.549 → 417.664 B (+1.115 B)** lido de `dist/`, zero asset novo · **um teste pegou defeito de verdade antes do commit:** `avisoDePressa(NaN)` escrevia "Faltam NaN segundos" na faixa — a região que o leitor de tela anuncia —, porque `Math.max(0, Math.trunc(NaN))` é `NaN`; guarda de `Number.isFinite` entrou junto
  · **O invariante que a suíte cobra e que sustenta a saída (b):** `PRAZO_COBRANCA_MS` **< `CONNECT_TIMEOUT_MS`** (15 s contra 20 s) — aos 15 s este aparelho ainda tem canal para mandar a jogada; um prazo maior faria o sorteio chegar depois de a disputa já ter acabado por abandono
  · **Não medido em sandbox, e por isso vira `A-27`:** nenhuma tela de M7 tem teste (`vitest` roda em Node sem DOM), e o portão do card é de dois aparelhos — o placar igual nas duas telas é o observável, como em `A-25` · **✔ `A-27` FECHADA em 2026-08-20: `T-24` tem campo.** Com ela o card não tem mais pendência — a lacuna do placar não anotado está declarada lá, não aqui
- [x] T-21 — **a tela de convite do modo `online`** · **Módulo:** M7 (`src/ui/`) mais **1 linha** de export em `src/session/index.ts` · **Etapa:** pós-E-6 (a lacuna que `D-72` declarou) · **Skill:** frontend-uiux · **Saída já decidida em `D-73`:** M5 reexporta `newRoomId`, a tela sorteia o ID, monta o link e **sempre** passa `roomId` a `createSession` — a porta `Session` de `D-13` não ganha o 5º método, e `src/net` não muda um byte · **Portão:** portão de camada de M7 verde no padrão do CI (`grep -rnoE "^[[:space:]]*(import|export)[^;]*(engine|cpu|net)" src/ui/` = 0 — `newRoomId` não casa com ele, conferido nesta sessão) · dois aparelhos reais conectam pelo link, e o convidado que abre o link **entra na disputa**, não em `'failed'` · suíte verde, `tsc` limpo, bundle relido de `dist/` · **`QA-22` fechada por `D-75`: nada trava.** M6 não muda; a tela sorteia o ID com `newRoomId` e só chama `createSession` quando os DOIS lados estão prontos — o relógio de 20 s passa a cobrir só o aperto de mão, como já cobria os 17/17 de `A-08` (mediana 266 ms). **Dois portões novos de `D-75`:** nenhuma chamada a `createSession` antes do toque que declara o outro lado a postos; e retentar depois de `'failed'` é sessão NOVA no MESMO `roomId` — permitido **só** se o canal nunca chegou a `'connected'`, senão é o peer fantasma de `D-41`
  · **✔ código entregue em 2026-08-20** (skill `frontend-uiux`): `src/session/index.ts` reexporta `newRoomId` (a 1 linha de `D-73`); tela nova `src/ui/tela_convite.ts` + `src/ui/convite.ts` (link puro e testado); `ModoJogavel` passou a ser os TRÊS modos e a cobrança ganhou o modo `online`. **Portões verdes aqui:** camada de M7 = 0 ocorrências no padrão do CI · suíte **547/547** (9 testes novos) · `tsc --noEmit` limpo · build 414.827 B
  · **os dois portões de `D-75`, onde eles moram:** `criarSessao()` de `tela_convite.ts` só é alcançada por toque (nenhuma `createSession` na montagem) · a retentativa é sessão nova no MESMO `roomId` guardada por `conectou`, e depois de conectado a queda vira `D-35` (sem resultado) em vez de retentativa
  · **falta o portão de campo: `A-22`** — dois aparelhos reais pelo link. Nenhum sandbox produz esse número
- [x] T-13 — modo `online` da sessão, sobre o canal de T-11 · **Módulo:** M5 · **Etapa:** E-4 · **Skill:** backend-bff · **Portão:** 14 testes novos (220/220 na suíte, verde em 5 rodadas seguidas) — os **três** modos produzindo o mesmo `MatchState` para a mesma sequência de zonas em 3 sementes (online conferido contra `local`, que T-09 já conferiu contra `cpu`), a ordem de chegada das duas escolhas não mudando o resultado, 10 payloads ilegais (seq futuro, seq velho, lado do próprio aparelho, zona torta, seq negativo/fracionário, objeto sem campo, `null`) todos morrendo em M5 com o `MatchState` intacto, reenvio do peer não virando segunda cobrança, `dispose()` soltando a sala na sinalização sem assinante vivo · `D-35`, `D-36`, `Q-11`
  · **`Q-04` respondida pelo dono na abertura da sessão** (`A-05`): peer que some = disputa **sem resultado**. M5 não escreve vencedor — `winner` segue `null`, `phase` não vai a `finished`, e `choose()` recusa em voz alta. As outras duas saídas eram regra de disputa e custariam uma sessão de `backend-dominio` em M2 antes desta
  · **`Q-11` nasceu aqui e está declarada, não contornada:** o `roomId` que `hostRoom()` sorteia não tem por onde sair da porta congelada em `D-13`, e M7 não pode importar `src/net`. Sem isso não há tela de convite — é o que trava a etapa de UI do online, não T-13
  · **o portão de E-4 continua aberto:** as duas medições em rede móvel são `A-08`, do dono; `Q-10` (TURN) segue aberta
- [x] T-00 — Fase 0: CONTEXT, temas de domínio e candidatas a D-NN
- [x] T-01 — Fase 1a: D-01 e D-02 congelados com gatilho de revisão; Q-01→D-09 e Q-02→D-10
- [x] T-11 — transporte P2P: canal, ID de sala, timeout e instrumento da decisão de TURN · **Módulo:** M6 · **Etapa:** E-4 · **Portão:** 29 testes (207/207 na suíte, verde em 9 rodadas seguidas) — ID de 130 bits opaco e não sequencial, `joinRoom` recusando ID malformado na hora, timeout disparando **no** valor e não antes, relógio rearmado quando o peer sai, `failed` terminal, payload sem forma de `Move` morrendo em M6, fila com ordem e teto, TURN chegando e não chegando à sinalização, e **disputa `cpu`/`local` completa com a sinalização derrubada** · `D-29`..`D-34`, `QA-06`, `Q-10`
  · **o portão de E-4 fica aberto de propósito:** as duas medições em rede móvel são `A-08`, do dono — o sandbox não as produz [Fonte: a_context/b_plan.md → M6]. A decisão de TURN é `Q-10`, e o código já aceita as duas saídas por `IceConfig`
  · **T-13 não foi tocada:** M6 não sabe o que é gol, e o modo `online` de M5 continua bloqueado por `A-05`
- [x] T-10 — M7: telas jogáveis por toque em 360x640 · **Portão:** camada e licença verdes por teste, suíte 178/178, bundle 80.604 B · **passada no celular real do dono conferida em 2026-08-07: toque e fps OK — E-3 fechada**
- [x] T-02 — Fase 1b: PLANO com M1..M9 e etapas E-1..E-6 · **Portão:** aprovado pelo dono em 2026-08-07; congelado em `D-13`
- [x] T-03 — Fase 1c: consistência entre os quatro artefatos · **Portão:** as 4 linhas passam na passagem 2
  · passagem 1 (15:43) **reprovou** — 19 achados: [[a_artifact_consistency_report_260807_1543|relatório 1]]
  · passagem 2 (16:05) **aprovou** — 19/19 fechados e 7 achados novos `AC-20`..`AC-26`, nenhum CRÍTICO, todos fechados no commit do congelamento: [[b_artifact_consistency_report_260807_1605|relatório 2]]
- [x] T-04 — núcleo: tipos e gerador com semente · **Módulo:** M1 · **Etapa:** E-1 · **Portão:** 16/16 testes, determinismo de 1.000 valores, `int(3)` com o 0 incluso, 1 ocorrência do gerador nativo em `src/` · `D-14`, `D-15`
- [x] T-06 — motor da disputa, com os invariantes como teste · **Módulo:** M2 · **Etapa:** E-2 · **Portão:** 53/53 testes 2x com o mesmo placar, um teste por invariante de [[regras_partida]], regressão dos defeitos 1/2/4/5, `Number.isInteger` em 1.000 cobranças sorteadas · `D-19`, `Q-07`
  · **entregue antes de E-1 fechar** (M2 depende só de M1); a etapa E-2 só é declarada aberta quando A-07 fechar E-1
- [x] T-07 — CPU em 3 níveis, dois histogramas, teto de 70% medido por frequência · **Módulo:** M3 · **Etapa:** E-2 · **Portão:** 35 testes (88/88 na suíte) 2x idênticos, teto conferido por igualdade em 3.000 formatos de histograma e por frequência nos dois papéis, uniforme com histórico vazio, isolamento entre papéis, `grep localStorage src/cpu/` = 0 · `D-20`, `Q-08`
  · **entregue antes de E-1 fechar** (M3 depende só de M1), mesma situação de T-06; com ele o código de E-2 está completo
- [x] T-08 — catálogo de seleções, lista de fixação até A-04 · **Módulo:** M4 · **Etapa:** E-3 · **Portão:** 22 testes (110/110 na suíte), todo `code` casando `^[A-Z]{2}$` e fora das faixas de uso do usuário do ISO 3166-1, `name` conferido contra o ICU e **ausente como literal na fonte**, zero URL e zero termo da lista-morta em `src/data/teams.ts`, catálogo congelado (escrita lança) · `D-22`, `D-23`
  · **a lista é de fixação, não é resposta a `Q-03`:** 4 códigos arbitrários por construção, `flag: null` em todos. `CATALOG_IS_FIXTURE` é exportada e um teste falha de propósito quando ela virar `false`, obrigando a revisitar o portão de licença em `A-04`
  · **limite declarado (`D-23`):** o ICU aceita código retirado (`SU`) e excepcionalmente reservado (`UK`, `EU`) — a lista oficial da ISO é dado curado e entra com o catálogo real. Ver [[m4_catalogo_notas]]
- [x] T-09 — sessão de disputa nos modos `cpu` e `local`, já reexportando os três tipos · **Módulo:** M5 · **Etapa:** E-3 · **Portão:** 30 testes (140/140 na suíte), `cpu` e `local` produzindo `MatchState` idêntico para a mesma sequência de zonas em 3 níveis × 2 lados × 4 sementes, com piso de 6 cobranças e `finished` para a igualdade não passar vazia, zona inválida morrendo em M5 com a mensagem de M5 (nunca a de M2), `dispose()` sem assinante vivo e com `subscribers.clear()` conferido na fonte, `MatchState`/`LinkStatus`/`Level` reexportados e usados pelo próprio teste sem tocar em `engine`/`cpu`/`net` · `D-24`, `D-25`, `D-26`, `Q-09`
  · **o modo `online` NÃO entra aqui:** é `T-13`, e `createSession` o recusa em voz alta em vez de degradar para `local` (`D-25`)
  · **`Q-08` sai intacta:** T-09 chama `observe`/`pick` com a semântica que T-07 entregou; `D-26` só fixa a ordem entre eles, e o teste que a cobre declara que hoje não a distingue
- [x] T-05 — esqueleto de build e publicação no Pages · **Módulo:** M9 · **Etapa:** E-1 (portão completo só em E-6) · **Portão:** `tsc --noEmit`, suíte verde, bundle inicial 4.599 B de 8 MB lido de `dist/`, 1 `Math.random` em `src/` e 0 imports de motor; página no ar por A-07 · `D-16`, `D-17`, `D-18`, `QA-04`

## Pedidos do dono — 2026-08-20 (frontend/M7, ainda SEM compromisso)

> Levantados pelo dono depois de `A-24`. **Nenhum deles é card ainda**: cada um traz o que já
> existe, o custo real e o que o trava. Vira `T-NN` quando o dono escolher a direção — a maioria
> é gosto e produto, logo `D-NN` dele (regra 6). Ordem aqui é de leitura, não de prioridade.
> **`T-24` já foi usado** no desempate de `QA-28` (2026-08-20): é o "tempo por cobrança no
> `online`" de `Q-15`, e está em "A fazer". O próximo card destes pedidos é `T-28`, e o dono escolhe entre `P-6` (papel legível sem texto, respondido por ele em 2026-08-21) e `P-8` (sorteio em tela cheia, pedido no mesmo dia): `T-25` é `P-2`, `T-26` é `P-5` e `T-27` é `P-1`+`P-7`, os três fechados — `T-27` com campo em 2026-08-21.
> **ESCOLHIDOS pelo dono em 2026-08-20, nesta ordem:** `P-2` (pontos, quase de graça) · `P-5`
> (moeda animada dentro do painel de `D-49`) · `P-1`+`P-7` **no mesmo card** (desktop e
> seleções disputam a mesma folha) · `P-6` entra junto de `P-1` se couber, senão é card
> próprio. `P-3` (chaveamento) e `P-4` (emoção) ficam para depois: os dois pedem decisão de
> contrato ou reabertura de `D-65`, e a folga do registro não comporta `D-NN` até `A-21` — **destravado por `D-82` em 2026-08-20**.

- **(P-1) Desktop é a tela do celular esticada.** Medido: `.tapgo` tem `max-width: 420px`
  (`src/ui/estilo.css:69`) e o **único** ponto de quebra de layout, `min-width: 480px`
  (`:1122`), não alarga nada — só vira cartão com borda e sombra. Em 1920px sobram ~1500px
  **por construção** · **Não é defeito de CSS, é decisão que nunca houve:** o portão de `T-20`
  cobrava "360x640 **e** no desktop", mas o que ele cobra no desktop é ausência de rolagem
  horizontal, não composição · **Custo:** nenhuma tela de M7 tem teste (`vitest` roda em Node
  sem DOM), então cada layout novo é conferência no aparelho; e `D-65` é a folha herdada, logo
  a direção larga tem de nascer dentro dela · **Trava:** direção de layout é `D-NN` do dono · **✔ VIROU `T-27` (com `P-7`) e está feito (2026-08-20)** — `D-86`: a largura passou a ser declarada pela tela, e as que emparelham vão a 1040px em duas colunas. Falta o campo de `A-30`
- **(P-2) Pontos na tabela da TAP GO Cup.** Hoje `Standing` traz `wins`, `goalsFor`,
  `goalsAgainst` (`src/tournament/tabela.ts:19`) · **É de graça e não muda ordenação:** a
  disputa nunca empata (`D-09`), então **pontos = 3 x vitórias**, exatamente — render puro em
  M7, zero byte em M8 · **O cuidado:** o desempate de `D-53` é confronto direto -> saldo -> gols
  -> sorteio, e uma coluna "Pts" não pode sugerir que empate de pontos se resolve por outra
  regra · **Trava:** nada. É o mais barato da lista · **✔ VIROU `T-25` e está feito (2026-08-20)** — a coluna existe na tela, a nota abaixo dela nomeia a cascata de `D-53` e M8 não mudou um byte
- **(P-3) Ver o chaveamento inteiro, os resultados das outras disputas e quem passou.**
  **O dado JÁ EXISTE inteiro:** `TournamentState` guarda `entrants` (as 32 já sorteadas, 4 por
  grupo), `groupOrder`, `results`, `goalsA`, `goalsB` — as 64 disputas (`src/tournament/index.ts:47`)
  · **O que falta é um leitor legítimo:** o retrato é declarado **opaco** ("M7 não interpreta
  nenhum campo daqui", `:41`) e `Tournament` expõe 5 métodos, nenhum de leitura de fase ·
  **Duas saídas, as duas `D-NN` do dono:** (a) M7 passa a interpretar `TournamentState` — barato
  em código, caro em contrato, mata a opacidade que `D-68` grava; (b) 6º método em M8, que é o
  precedente de superfície que `D-39` recusou comprar · **Limite a declarar ANTES de desenhar:**
  as disputas **do jogador** não têm placar, só vencedor — `report(winner)` é porta congelada
  (`D-13`/`D-58`) e `Q-13` é exatamente isso; a tela mostraria `—`, como `D-67` já faz
- **(P-4) Emoção: torcida comemorando/lamentando, taça, medalha de 2º e 3º, trilha nos menus.**
  **Precedente bom, e é melhor do que parece:** os 3 efeitos de áudio são **autorais e gerados
  por script** (`src/scripts/gen-audio.mjs`, `src/ui/som.ts:3`), determinísticos e com linha de
  procedência — trilha pelo mesmo caminho **não** compra licença de terceiro · **Bundle não é o
  gargalo:** 415.713 B de 8 MB (5,2%) · **O gargalo é `D-65`**, que é restrição permanente:
  "capa e profundidade feitas só de degradê (**zero asset**)" e "movimento restrito a
  `opacity`/`transform`". Torcida e taça animadas **reabrem `D-65`** -> `D-NN` do dono ·
  **E `prefers-reduced-motion` continua valendo**, e a preferência "som" já existe e desliga
- **(P-5) A moeda do sorteio, animada.** **Atenção à lista-morta:** `D-49` já recusou a
  **tela da moeda** — 3º toque num fluxo com portão de 2 toques —, e o sorteio virou painel
  dentro da cobrança · **Ângulo novo, e ele é legítimo:** animar a moeda **dentro do painel que
  já existe**, sem toque a mais e sem tela nova, não é o que `D-49` matou · **Custo:** só
  `opacity`/`transform` para caber em `D-65`; `A-14` já confirmou no aparelho que o painel
  aparece antes do 1º toque e some depois · **✔ VIROU `T-26` e está feito (2026-08-20)** — a marca gira uma vez
  dentro do painel, só `opacity`/`transform`, e a tela de cobrança continua com os mesmos 4 botões
- **(P-6) O papel (ataca/defende) tem de ser lido numa olhada, sem texto.** **NÃO é `QA-NN`:**
  o dono esclareceu em 2026-08-20 que não chegou a confundir em partida — o defeito é de
  intuição, não de correção. Hoje o papel vem **só de texto**: a faixa (`tela_cobranca.ts:290`)
  e o `aria-label` das zonas (`:265`); o destaque no placar aponta o **lado deste aparelho**,
  não o papel · **O pedido:** cor e/ou imagem carregando o papel, para não ser preciso parar e
  ler · **Duas restrições que a proposta tem de respeitar, e elas são portão, não gosto:**
  (1) **cor não pode ser o único canal** — daltonismo; precisa vir com forma, ícone ou rótulo
  junto; (2) o contraste ≥4,5:1 é **cobrado por teste** sobre o produto cartesiano da paleta
  (`T-20`), então cor nova entra na paleta, não ao lado dela · **Onde encostar sem inventar
  tela:** as três zonas já mudam de `aria-label` por papel, e o painel de `D-49` já existe ·
  **RESPONDIDO pelo dono em 2026-08-21, e ele abriu o pedido:** concorda que cor não pode ser o
  único canal, e propõe — sprites melhores, animação mais fluida, jogadores "seguindo as cores
  corretas dos países" (Brasil amarelo, Espanha vermelho, Holanda laranja), a bandeira de quem
  está batendo, ou uma animação rápida. Pediu recomendação: *"veja o que melhor conecta"* ·
  **O que a checagem de licença devolveu, e ela DESTRAVA metade do pedido:** [[licenciamento]]
  lista "Cores nacionais e padrões genéricos (listras, faixas)" como **livre**, com uma condição —
  "não reproduzir uniforme oficial identificável". Então camisa amarela para o Brasil é permitida;
  o proibido é a camisa RECONHECÍVEL (escudo, gola, patrocínio, listra exata). O comentário de
  `sprites.ts` que diz "o matiz não representa cor nacional nenhuma" descreve uma ESCOLHA de
  `T-10`, não uma proibição — e é bom que fique escrito, porque a próxima sessão leria aquilo
  como portão · **A recomendação, e ela se divide em duas de custo muito diferente:** (a) **a
  bandeira de quem cobra, grande, mais um segundo canal de FORMA** — as 32 bandeiras já estão no
  bundle por `T-19`, já têm licença conferida, e forma+bandeira resolve daltonismo sem cor nova
  na paleta: é o mais barato e o que mais conecta; (b) **a camisa na cor nacional** é legítima mas
  pede tabela curada de 32 cores (dado novo, não derivável — regra 5), muda a marca da seleção em
  TODA tela, e cada cor tem de passar no teste de contraste de `T-20`, que cobre o produto
  cartesiano da paleta · **De quebra, (b) fecharia `QA-20`:** o matiz de hoje é hash e dá 30 cores
  para 32 seleções (`FR`/`NL` e `MA`/`EG` colidem) — tabela curada não colide por construção ·
  **Trava:** as duas são `D-NN` do dono. (a) cabe num card só; (b) é card próprio, com a tabela
  das 32 cores entrando como DADO revisado por ele · **✔ (a) VIROU `T-28` e está feito, com campo
  em `A-31`** · **◐ (b) VIROU `T-29` em 2026-08-21, e a tabela está montada e medida em
  [[cores_nacionais]] — mas o card parou em `Q-16`, porque a MEDIÇÃO derrubou a premissa que este
  pedido escreveu:** "tabela curada não colide por construção" é **falso**. Quatorze das 32 têm
  vermelho como cor nacional, e a tabela dá **15** matizes distintos para 30 seleções contra os
  **30** do hash de hoje — 33,1% dos pares abaixo dos 40° de `SEPARACAO_MINIMA`, contra 20,4%. Ela
  **não fecha `QA-20`; piora o número dele** · **O portão de contraste que este pedido ergueu é
  vazio para uma tabela de matizes:** varredura dos 360 matizes, pior caso 7,28:1 contra o limite
  de 4,5:1 — saturação e luminosidade são fixas por papel, e o matiz não decide contraste ·
  **Duas seleções reprovam e voltaram ao dono sem ajuste meu:** `GB-ENG` e `DE`, que são branco e
  branco/preto, não um ângulo de matiz
- **(P-7) Seleção de time melhor organizada no desktop.** Filha de (P-1): a grade é
  `repeat(2, 1fr)` (`estilo.css:318`) dentro da mesma coluna de 420px. Decidir junto com (P-1),
  senão são duas mudanças disputando a mesma folha · **✔ VIROU `T-27` junto de `P-1` e está feito
  (2026-08-20)** — a grade continua `repeat(2, 1fr)`, mas as DUAS grades ficam lado a lado no
  desktop: 4 cartões por linha e metade da altura
- **(P-8) O sorteio de quem bate primeiro em TELA CHEIA, encolhendo para o painel no fim.**
  Pedido do dono em 2026-08-21: *"devia ser grande e ocupar a tela inteira para ser mais visível
  para todos os usuários, aí quando acabar a animação pode ficar pequeno no canto que já está"* ·
  **NÃO é o que `D-49` matou, e o argumento é o mesmo de `D-85`:** `D-49` recusou a **tela da
  moeda** porque ela custava o 3º toque num fluxo com portão de 2. Uma sobreposição que aparece
  sozinha, roda e sai sozinha não pede toque nenhum — o fluxo continua fechando em 2 · **O que já
  existe e serve de base:** `D-85` gira a marca uma vez dentro do painel, com o guarda de
  transição escondido -> visível que impede recomeço a cada `desenhar()` (o caso do `online`) ·
  **Três restrições que a proposta tem de respeitar, e são portão, não gosto:** (1) só
  `opacity`/`transform`, senão reabre `D-65`; (2) o toque **não pode se perder** durante a
  animação — é o item (2) do portão de `A-29`, e sobreposição em tela cheia é justamente o que
  rouba toque, então ela precisa de `pointer-events: none` ou de saída imediata ao primeiro
  toque; (3) com `prefers-reduced-motion` ligado a sobreposição não pode piscar nem travar a
  tela — o bloco global zera duração E atraso, então o estado final tem de ser "já saiu" ·
  **Trava:** é `D-NN` do dono — muda a primeira coisa que se vê em toda disputa

> **Licenciamento, sobre a imagem de referência que o dono anexou:** o **layout** de chaveamento
> é imitável à vontade. O que **não** entra, por restrição inegociável do CONTEXT e por
> [[licenciamento]]: a **taça** (marca de federação), o nome "Copa do Mundo"/"FIFA" e a marca do
> veículo. O torneio do jogo é a **TAP GO Cup** (`D-55`), e as bandeiras já entraram por `D-54`.

## Ideias (não comprometidas)
- **Sala de 8 — torneio no modo `online`. Adiada por `D-56`**, não rejeitada: exigiria o chaveamento como estado compartilhado entre aparelhos, o que muda a camada 3 do PLANO. Volta à mesa como `D-NN` novo, nunca como "só ligar o online no torneio"
- Ranking global (exige servidor autoritativo — hoje colide com "custo R$ 0"; ver [[online_p2p]])
- Personalização de escudo pelo jogador (contorna licença e vira conteúdo próprio)
- Modo treino: mesma zona repetida, para medir leitura do goleiro
